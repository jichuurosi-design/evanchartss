<!DOCTYPE html>
<html lang="es" id="htmlRoot">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>EVANcharts | Lee Heeseung Solo Era</title>
<meta name="description" content="Fan streaming tracker para EVAN (Lee Heeseung) - Solo Era. Datos en tiempo real desde YouTube API + seguimiento manual de Spotify.">
<link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24'%3E%3Cpath fill='%23c8a84b' stroke='%238a6f28' stroke-width='0.7' stroke-linejoin='round' d='M12 1.6l2.95 6.53 7.15.72-5.4 4.83 1.6 7.02L12 17.05l-6.3 3.65 1.6-7.02-5.4-4.83 7.15-.72z'/%3E%3C/svg%3E">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
<style>
/* ========== EVANCHARTS — LUXURY EDITORIAL REDESIGN ========== */
@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&family=DM+Mono:wght@400;500&display=swap');

:root {
  --bg:          #080808;
  --bg-card:     #0f0f0f;
  --bg-raised:   #161616;
  --bg-hover:    #1c1c1c;
  --gold:        #c8a84b;
  --gold-light:  #e2c97e;
  --gold-dim:    #8a6f28;
  --gold-glow:   rgba(200,168,75,0.2);
  --gold-subtle: rgba(200,168,75,0.06);
  --cream:       #ede8df;
  --cream-dim:   #9a9188;
  --cream-muted: #4a4640;
  --border:      rgba(200,168,75,0.12);
  --border-hi:   rgba(200,168,75,0.4);
  --green:       #52c77a;
  --red:         #ef3b56;
  --red-dim:     #8c1f30;
  --red-glow:    rgba(239,59,86,0.18);
  --spotify:     #1db954;
  --apple:       #fc3c44;
  --nav-h:       60px;
  --nav-bg:      rgba(8,8,8,0.94);
  --shadow:      0 20px 60px rgba(0,0,0,0.7);
  --shadow-sm:   0 4px 20px rgba(0,0,0,0.4);
  --r:           2px;
  --input-bg:    #0a0a0a;
  --admin-bg:    #050505;
  --admin-card:  #0f0f0f;
  --admin-item:  #141414;
  --hero-overlay: 8,8,8;
}
[data-theme="white"] {
  --bg:#f7f4ef; --bg-card:#ffffff; --bg-raised:#f0ece4; --bg-hover:#e8e4dc;
  --gold:#7a5c10; --gold-light:#9a7820; --gold-dim:#c8a84b; --gold-glow:rgba(122,92,16,0.12);
  --gold-subtle:rgba(122,92,16,0.07); --cream:#1a1510; --cream-dim:#3a3530;
  --cream-muted:#6a6560; --border:rgba(122,92,16,0.18); --border-hi:rgba(122,92,16,0.5);
  --nav-bg:rgba(247,244,239,0.96); --shadow:0 20px 60px rgba(0,0,0,0.1);
  --shadow-sm:0 4px 20px rgba(0,0,0,0.07); --input-bg:#faf8f4;
  --admin-bg:#f0ece4; --admin-card:#ffffff; --admin-item:#f7f4ef;
  --green:#1a7a40; --red:#d1233c; --red-dim:#f3b8bf; --red-glow:rgba(209,35,60,0.1);
  --hero-overlay: 247,244,239;
}
[data-theme="white"] .goal-song { color:#1a1510 !important; }
[data-theme="white"] .goal-stat-value { color:var(--gold) !important; }
[data-theme="white"] .goal-stat-label { color:var(--cream-muted) !important; }
[data-theme="white"] .goal-pct { color:var(--cream-muted) !important; }
[data-theme="white"] .section-title { color:#1a1510 !important; }
[data-theme="white"] .yt-stat-value { color:var(--gold) !important; }
[data-theme="white"] .ali-name { color:#1a1510 !important; }
[data-theme="white"] .wv-card-author { color:#1a1510 !important; }
[data-theme="white"] .artist-stat-card { border:1px solid var(--border); }
[data-theme="white"] .ast-value { color:#1a1510 !important; }
[data-theme="white"] .artist-chart-wrap { background:#faf8f4; }
[data-theme="white"] .yt-stat-card { background:#faf8f4; }
[data-theme="white"] #pm-close { background:rgba(0,0,0,0.08);color:#1a1510; }
[data-theme="white"] #pm-close:hover { background:rgba(0,0,0,0.15); }
[data-theme="rojo"] {
  --bg:#1a0407; --bg-card:#230609; --bg-raised:#2d0a0e; --bg-hover:#3a0d12;
  --gold:#ff1f3d; --gold-light:#ff5c73; --gold-dim:#b3122a; --gold-glow:rgba(255,31,61,0.2);
  --gold-subtle:rgba(255,31,61,0.06); --cream:#ffe9ea; --cream-dim:#e0a8ae;
  --cream-muted:#7a3540; --border:rgba(255,31,61,0.16); --border-hi:rgba(255,31,61,0.5);
  --nav-bg:rgba(26,4,7,0.96); --shadow:0 20px 60px rgba(0,0,0,0.6);
  --shadow-sm:0 4px 20px rgba(0,0,0,0.35); --input-bg:#150304;
  --admin-bg:#1a0407; --admin-card:#230609; --admin-item:#2d0a0e;
  --red:#ff8a3d; --red-dim:#c9541b; --red-glow:rgba(255,138,61,0.16);
  --hero-overlay: 26,4,7;
}
[data-theme="dom"] {
  --bg:#05101e; --bg-card:#0a1a2e; --bg-raised:#0f2240; --bg-hover:#152a4a;
  --gold:#60a5fa; --gold-light:#93c5fd; --gold-dim:#3b82f6; --gold-glow:rgba(96,165,250,0.18);
  --gold-subtle:rgba(96,165,250,0.05); --cream:#dbeafe; --cream-dim:#6ea0d0;
  --cream-muted:#2d5070; --border:rgba(96,165,250,0.14); --border-hi:rgba(96,165,250,0.45);
  --nav-bg:rgba(5,16,30,0.96); --shadow:0 20px 60px rgba(0,0,0,0.5);
  --shadow-sm:0 4px 20px rgba(0,0,0,0.3); --input-bg:#06111f;
  --admin-bg:#05101e; --admin-card:#0a1a2e; --admin-item:#0f2240;
  --hero-overlay: 5,16,30;
}

*, *::before, *::after { margin:0; padding:0; box-sizing:border-box; }

html { scroll-behavior:smooth; }

body {
  background: var(--bg);
  color: var(--cream);
  font-family: 'Inter', sans-serif;
  font-weight: 400;
  line-height: 1.7;
  min-height: 100vh;
  overflow-x: hidden;
}

/* ── SCROLLBAR ── */
::-webkit-scrollbar { width: 3px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: var(--gold-dim); border-radius: 2px; }

/* ══════════════════════════════════════════
   NAV
══════════════════════════════════════════ */
nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 3rem;
  height: var(--nav-h);
  background: var(--nav-bg);
  backdrop-filter: blur(24px) saturate(1.4);
  border-bottom: 1px solid var(--border);
  position: sticky;
  top: 0;
  z-index: 200;
  transition: background 0.3s;
}
.nav-logo {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 1.4rem;
  font-weight: 700;
  text-decoration: none;
  color: var(--cream);
  letter-spacing: -0.01em;
}
.nav-logo span { color: var(--gold); font-weight: 400; }
.nav-links {
  display: flex;
  gap: 0.2rem;
  background: var(--bg-raised);
  border: 1px solid var(--border);
  border-radius: 999px;
  padding: 0.3rem;
}
.nav-links a {
  display: flex;
  align-items: center;
  gap: 0.45rem;
  color: var(--cream-muted);
  text-decoration: none;
  font-size: 0.72rem;
  font-weight: 500;
  letter-spacing: 0.01em;
  border-radius: 999px;
  padding: 0.5rem 1rem;
  transition: color 0.2s, background 0.2s, color 0.2s;
  cursor: pointer;
  white-space: nowrap;
}
.nav-links a .nav-ico { font-size: 0.85rem; line-height: 1; }
.nav-links a:hover { color: var(--cream); background: var(--bg-hover); }
.nav-links a.active {
  color: #fff;
  background: linear-gradient(135deg, var(--red), var(--red-dim));
  box-shadow: 0 4px 14px var(--red-glow);
}
.nav-links a.active:hover { background: linear-gradient(135deg, var(--red), var(--red-dim)); }
.nav-right { display:flex; align-items:center; gap:1rem; }
.admin-indicator { display:flex;align-items:center;gap:0.4rem;font-size:0.65rem;color:var(--cream-muted); }
.admin-dot { width:5px;height:5px;border-radius:50%;background:var(--cream-muted); }
.admin-dot.on { background:var(--gold);box-shadow:0 0 6px var(--gold); }
.nav-mobile-btn { display:none;background:none;border:none;color:var(--cream-dim);font-size:1.3rem;cursor:pointer; }

/* ══════════════════════════════════════════
   HERO
══════════════════════════════════════════ */
.hero {
  position: relative;
  min-height: 95vh;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  justify-content: center;
  text-align: left;
  padding: 6rem 3rem 5rem;
  overflow: hidden;
  background-size: cover;
  background-position: center 20%;
  background-repeat: no-repeat;
}
.hero-content { max-width: 720px; width: 100%; }
.hero::before {
  content: '';
  position: absolute; inset: 0;
  background: linear-gradient(100deg,
    rgba(var(--hero-overlay),0.94) 0%,
    rgba(var(--hero-overlay),0.75) 32%,
    rgba(var(--hero-overlay),0.35) 62%,
    rgba(var(--hero-overlay),0.55) 100%);
  z-index: 0;
  pointer-events: none;
}
/* thin gold line at bottom */
.hero::after {
  content: '';
  position: absolute;
  bottom: 0; left: 10%; right: 10%;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--gold), transparent);
  opacity: 0.5;
  z-index: 1;
  pointer-events: none;
}
.hero > * { position: relative; z-index: 2; }
.hero-bg-video {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center 20%;
  z-index: -1;
  display: none;
  pointer-events: none;
}

.hero-tag {
  font-size: 0.58rem;
  font-weight: 500;
  color: var(--gold);
  border: 1px solid var(--border-hi);
  padding: 0.35rem 1.8rem;
  letter-spacing: 5px;
  text-transform: uppercase;
  background: rgba(200,168,75,0.04);
  backdrop-filter: blur(4px);
  margin-bottom: 2.5rem;
  display: inline-block;
}
.hero h1 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: clamp(4rem, 12vw, 9rem);
  font-weight: 700;
  line-height: 0.88;
  color: var(--cream);
  letter-spacing: -0.03em;
  margin-bottom: 0;
}
.hero h1 .accent {
  display: block;
  font-size: 0.22em;
  font-family: 'Inter', sans-serif;
  font-weight: 400;
  font-style: normal;
  color: var(--gold);
  letter-spacing: 8px;
  text-transform: uppercase;
  margin-top: 1.2rem;
}
.hero-sub {
  margin-top: 1.5rem;
  font-size: 0.65rem;
  color: var(--cream-muted);
  letter-spacing: 4px;
  text-transform: uppercase;
}
.hero-debut {
  display: inline-flex;
  align-items: center;
  gap: 0.8rem;
  margin-top: 2.5rem;
  border: 1px solid var(--border);
  padding: 0.55rem 1.8rem;
  font-size: 0.7rem;
  color: var(--cream-dim);
  backdrop-filter: blur(4px);
  background: rgba(var(--hero-overlay),0.5);
  letter-spacing: 1px;
}
.dot-live {
  width: 6px; height: 6px;
  background: var(--gold);
  border-radius: 50%;
  box-shadow: 0 0 8px var(--gold);
  animation: pulse 2.5s ease-in-out infinite;
  flex-shrink: 0;
}
@keyframes pulse { 0%,100%{opacity:0.3; transform:scale(1)} 50%{opacity:1; transform:scale(1.2)} }

.stream-cta {
  display: flex; gap: 0.6rem;
  justify-content: flex-start;
  margin-top: 2.8rem;
  flex-wrap: wrap;
}
.stream-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.45rem;
  padding: 0.55rem 1.3rem;
  font-size: 0.68rem;
  font-weight: 500;
  text-decoration: none;
  border: 1px solid var(--border);
  color: var(--cream-dim);
  background: rgba(var(--hero-overlay),0.6);
  backdrop-filter: blur(8px);
  letter-spacing: 1px;
  transition: all 0.3s;
}
.stream-btn:hover {
  border-color: var(--gold);
  color: var(--gold);
  background: var(--gold-subtle);
  transform: translateY(-2px);
}

/* ══════════════════════════════════════════
   SECTIONS & LAYOUT
══════════════════════════════════════════ */
section {
  max-width: 1100px;
  margin: 0 auto;
  padding: 4rem 2rem;
}
.section-header {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  margin-bottom: 2.5rem;
}
.section-title {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 1.35rem;
  font-weight: 600;
  color: var(--cream);
  white-space: nowrap;
  letter-spacing: -0.01em;
}
.section-line {
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, var(--border-hi), transparent);
}

/* ══════════════════════════════════════════
   GOALS
══════════════════════════════════════════ */
.goal-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 16px;
  overflow: hidden;
  position: relative;
  display: flex;
  flex-direction: column;
  transition: transform 0.25s ease, border-color 0.25s ease;
}
.goal-card:hover { transform: translateY(-3px); border-color: var(--border-hi); }
.goal-cover-photo { position: relative; width: 100%; aspect-ratio: 4/3; background: var(--bg-raised); overflow: hidden; }
.goal-cover-photo img { width:100%;height:100%;object-fit:cover;display:block; }
.goal-cover-placeholder { width:100%;height:100%;display:flex;align-items:center;justify-content:center;font-size:2rem;color:var(--cream-muted); }
.goal-badge {
  position: absolute;
  top: 0.85rem; left: 0.85rem;
  background: rgba(8,8,8,0.55);
  backdrop-filter: blur(6px);
  border: 1px solid rgba(255,255,255,0.18);
  color: var(--cream);
  font-size: 0.58rem;
  font-weight: 600;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  padding: 0.3rem 0.85rem;
  border-radius: 999px;
}
.goal-body { padding: 1.1rem 1.2rem 1.3rem; display:flex; flex-direction:column; gap:0.7rem; }
.goal-song { font-family:'Space Grotesk',sans-serif; font-size:1.05rem; font-weight:700; color:var(--cream); line-height:1.25; }
.goal-numbers { font-family:'DM Mono',monospace; font-size:0.95rem; color:var(--red); font-weight:500; letter-spacing:-0.01em; }
.goal-numbers .goal-target-num { opacity:0.5; }
.goal-bar-wrap { height:6px; background:rgba(255,255,255,0.07); overflow:hidden; border-radius:999px; }
.goal-bar { height:100%; background:linear-gradient(90deg, var(--red-dim), var(--red)); border-radius:999px; transition:width 1.2s cubic-bezier(0.4,0,0.2,1); }
.goal-bottom { display:flex; align-items:center; justify-content:space-between; }
.goal-remaining { font-size:0.66rem; color:var(--cream-muted); display:flex; align-items:center; gap:0.35rem; }
.goal-remaining span { color:var(--cream-dim); font-family:'DM Mono',monospace; font-weight:500; }
.goal-pct { font-family:'DM Mono',monospace; font-size:0.85rem; color:var(--red); font-weight:700; }
.goal-stat { text-align:center; }
.goal-stat-value { font-family:'DM Mono',monospace;font-size:1.5rem;color:var(--gold);line-height:1; }
.goal-stat-label { font-size:0.58rem;color:var(--cream-muted);text-transform:uppercase;letter-spacing:2px;margin-top:0.4rem; }
#goals-list { display:grid; grid-template-columns:repeat(auto-fill,minmax(230px,1fr)); gap:1.2rem; }

/* ══════════════════════════════════════════
   PLAYLISTS
══════════════════════════════════════════ */
.playlist-grid { display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:1px;border:1px solid var(--border);background:var(--border); }
.playlist-card { background:var(--bg-card);padding:1.4rem 1.6rem;text-decoration:none;transition:background 0.2s;display:block; }
.playlist-card:hover { background:var(--bg-raised); }
.playlist-icon { font-size:1.2rem;margin-bottom:0.8rem; }
.playlist-name { font-size:0.85rem;font-weight:500;color:var(--cream);margin-bottom:0.3rem; }
.playlist-desc { font-size:0.7rem;color:var(--cream-muted);line-height:1.5; }
.playlist-platform { font-size:0.6rem;color:var(--gold);text-transform:uppercase;letter-spacing:1.5px;margin-top:0.6rem; }

/* ══════════════════════════════════════════
   ARTIST CHART (Kworb)
══════════════════════════════════════════ */
.artist-chart-wrap { background:var(--bg-card);border:1px solid var(--border);padding:1.8rem;margin-bottom:2rem; }
.artist-chart-header { display:flex;align-items:center;justify-content:space-between;margin-bottom:1.5rem;flex-wrap:wrap;gap:0.6rem; }
.artist-chart-title { font-size:0.68rem;font-weight:600;color:var(--spotify);display:flex;align-items:center;gap:0.5rem;letter-spacing:2px;text-transform:uppercase; }
.artist-chart-title::before { content:'';display:inline-block;width:6px;height:6px;background:var(--spotify);border-radius:50%; }
.kworb-meta { font-size:0.63rem;color:var(--cream-muted);display:flex;align-items:center;gap:0.6rem; }
.kworb-refresh-btn { background:transparent;border:1px solid var(--border);color:var(--cream-dim);font-size:0.65rem;padding:0.25rem 0.8rem;cursor:pointer;transition:0.2s;font-family:'Inter',sans-serif;display:flex;align-items:center;gap:0.3rem; }
.kworb-refresh-btn:hover { border-color:var(--spotify);color:var(--spotify); }
.kworb-refresh-btn.spinning svg { animation:spin 0.7s linear infinite; }
.artist-chart-grid { display:grid;grid-template-columns:repeat(3,1fr);gap:1px;background:var(--border); }
.artist-stat-card { background:var(--bg-card);padding:1.3rem 1.5rem;position:relative;overflow:hidden;transition:background 0.2s; }
.artist-stat-card:hover { background:var(--bg-raised); }
.artist-stat-card::after { content:'';position:absolute;top:0;left:0;right:0;height:1px;background:linear-gradient(90deg,var(--spotify),transparent); }
.ast-label { font-size:0.58rem;color:var(--cream-muted);text-transform:uppercase;letter-spacing:2px;margin-bottom:0.6rem; }
.ast-value { font-family:'DM Mono',monospace;font-size:1.5rem;font-weight:400;color:var(--cream);line-height:1; }
.ast-live { display:inline-flex;align-items:center;gap:0.3rem;font-size:0.6rem;color:var(--green);margin-top:0.5rem;font-weight:500; }
.ast-live-dot { width:4px;height:4px;background:var(--green);border-radius:50%;animation:pulse 1.5s infinite; }
.ast-daily { font-size:0.65rem;color:var(--green);margin-top:0.4rem; }
.kworb-error { background:var(--gold-subtle);border:1px solid var(--border);border-left:2px solid var(--gold);padding:0.9rem 1.1rem;font-size:0.76rem;color:var(--cream-dim);line-height:1.7; }
.kworb-skeleton { display:grid;grid-template-columns:repeat(3,1fr);gap:1px;background:var(--border); }
.skeleton-card { background:var(--bg-card);padding:1.2rem; }
.skeleton-line { height:6px;background:var(--bg-raised);margin-bottom:0.7rem;animation:shimmer 1.4s infinite; }
.skeleton-line.big { height:22px;width:55%; }
@keyframes shimmer { 0%,100%{opacity:0.3}50%{opacity:0.8} }

/* ══════════════════════════════════════════
   TABLES
══════════════════════════════════════════ */
.spotify-table, .yt-video-table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 0.5rem;
}
.spotify-table th, .yt-video-table th {
  font-size: 0.57rem;
  font-weight: 600;
  text-align: left;
  padding: 0.7rem 0.8rem;
  color: var(--gold);
  letter-spacing: 2.5px;
  text-transform: uppercase;
  border-bottom: 1px solid var(--border);
}
.spotify-table td, .yt-video-table td {
  padding: 0.9rem 0.8rem;
  border-bottom: 1px solid rgba(255,255,255,0.03);
  font-size: 0.78rem;
  color: var(--cream-dim);
  vertical-align: middle;
}
.spotify-table tr:hover td, .yt-video-table tr:hover td { background: rgba(200,168,75,0.02); }
.yt-video-title { font-weight:500;color:var(--cream); }

/* ── RANK BADGE ── */
.rank-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  background: var(--gold-subtle);
  border: 1px solid var(--border);
  color: var(--gold);
  font-family: 'DM Mono', monospace;
  font-size: 0.65rem;
  font-weight: 500;
  min-width: 34px;
  padding: 2px 8px;
}

/* ══════════════════════════════════════════
   YOUTUBE STATS
══════════════════════════════════════════ */
.yt-stats-grid { display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:1px;background:var(--border);border:1px solid var(--border);margin-bottom:2.5rem; }
.yt-stat-card { background:var(--bg-card);padding:1.8rem;text-align:center;transition:background 0.2s; }
.yt-stat-card:hover { background:var(--bg-raised); }
.yt-stat-value { font-family:'DM Mono',monospace;font-size:2.2rem;font-weight:400;color:var(--gold);line-height:1; }
.yt-stat-label { font-size:0.58rem;color:var(--cream-muted);text-transform:uppercase;letter-spacing:2.5px;margin-top:0.5rem; }
.yt-stat-change { font-size:0.65rem;margin-top:0.4rem;color:var(--green); }

/* ══════════════════════════════════════════
   CHARTS
══════════════════════════════════════════ */
.charts-table { width:100%;border-collapse:collapse; }
.charts-table th { font-size:0.57rem;font-weight:600;letter-spacing:2.5px;text-transform:uppercase;color:var(--gold);padding:0 0.8rem 0.8rem;text-align:left; }
.charts-table td { padding:0.8rem;border-top:1px solid rgba(255,255,255,0.03);font-size:0.8rem;color:var(--cream-dim); }
.charts-table td:first-child { font-family:'DM Mono',monospace;color:var(--gold); }
.song-name { color:var(--cream);font-weight:500; }
.chart-badge { display:inline-block;background:var(--gold-subtle);color:var(--gold);border:1px solid var(--border);padding:0.12rem 0.6rem;font-size:0.62rem;font-family:'DM Mono',monospace; }

/* ── TABS ── */
.tabs { display:flex;gap:0;border-bottom:1px solid var(--border);margin-bottom:2rem; }
.tab { background:transparent;border:none;padding:0.7rem 1.4rem;font-size:0.65rem;font-weight:500;color:var(--cream-muted);cursor:pointer;transition:0.2s;letter-spacing:1.5px;text-transform:uppercase;border-bottom:2px solid transparent; }
.tab.active { color:var(--gold);border-bottom-color:var(--gold); }
.tab:hover:not(.active) { color:var(--cream-dim); }

/* ══════════════════════════════════════════
   BUTTONS
══════════════════════════════════════════ */
.btn {
  background: var(--gold);
  border: none;
  padding: 0.6rem 1.6rem;
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: 0.72rem;
  color: #080808;
  cursor: pointer;
  transition: all 0.25s;
  letter-spacing: 0.5px;
}
.btn:hover { filter:brightness(1.12); transform:translateY(-1px); }
.btn-ghost { background:transparent;border:1px solid var(--border);color:var(--cream-dim); }
.btn-ghost:hover { border-color:var(--gold);color:var(--gold);background:var(--gold-subtle); }
.btn-danger { background:transparent;border:1px solid var(--border);color:var(--cream-muted); }
.btn-danger:hover { border-color:var(--red);color:var(--red); }
.btn-sm { padding:0.3rem 0.8rem;font-size:0.67rem; }
.btn-success { background:#166534 !important;color:#fff !important; }

/* ══════════════════════════════════════════
   INPUTS
══════════════════════════════════════════ */
.input, select.input {
  background: var(--input-bg);
  border: 1px solid var(--border);
  padding: 0.7rem 1rem;
  color: var(--cream);
  font-family: 'Inter', sans-serif;
  font-size: 0.82rem;
  outline: none;
  transition: border-color 0.25s;
  width: 100%;
}
.input:focus, select.input:focus { border-color: var(--gold); }
.input::placeholder { color: var(--cream-muted); }

/* ══════════════════════════════════════════
   MISC
══════════════════════════════════════════ */
.tip-box { background:var(--gold-subtle);border-left:2px solid var(--gold);padding:1rem 1.4rem;margin-top:1.5rem; }
.tip-box p { font-size:0.76rem;color:var(--cream-dim);line-height:1.7; }
.page { display:none; }
.page.active { display:block; }
.loading { text-align:center;padding:3.5rem;color:var(--cream-muted); }
.loading .spin { display:inline-block;width:24px;height:24px;border:1px solid var(--border);border-top-color:var(--gold);border-radius:50%;animation:spin 0.9s linear infinite; }
@keyframes spin { to{transform:rotate(360deg)} }
.empty { text-align:center;padding:3rem;color:var(--cream-muted);font-size:0.82rem; }

/* ══════════════════════════════════════════
   SPOTIFY CHARTS
══════════════════════════════════════════ */
.sp-charts-wrap { background:var(--bg-card);border:1px solid rgba(29,185,84,0.15);padding:1.8rem;margin-bottom:1.5rem; }
.sp-charts-header { display:flex;align-items:center;gap:0.7rem;margin-bottom:1.5rem; }
.sp-charts-logo { width:26px;height:26px;background:var(--spotify);border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0; }
.sp-charts-title { font-family:'Space Grotesk',sans-serif;font-size:1.1rem;font-weight:600;color:var(--spotify); }
.sp-charts-section-label { font-size:0.6rem;color:var(--cream-muted);text-transform:uppercase;letter-spacing:2.5px;font-weight:600;margin-bottom:0.6rem; }
.sp-type-tabs { display:flex;gap:0.4rem;flex-wrap:wrap;margin-bottom:1.2rem; }
.sp-type-tab { padding:0.35rem 1rem;font-size:0.68rem;font-weight:500;cursor:pointer;border:1px solid var(--border);color:var(--cream-muted);background:transparent;font-family:'Inter',sans-serif;transition:all 0.2s;letter-spacing:0.5px; }
.sp-type-tab:hover { border-color:var(--gold);color:var(--gold); }
.sp-type-tab.active { background:var(--gold);color:#080808;border-color:var(--gold); }
.sp-charts-region-box { background:var(--bg-raised);border:1px solid var(--border);padding:1rem;margin-bottom:1.2rem; }
.sp-region-tabs { display:flex;flex-wrap:wrap;gap:0.4rem; }
.sp-region-tab { padding:0.25rem 0.7rem;font-size:0.65rem;font-weight:500;cursor:pointer;border:1px solid rgba(255,255,255,0.07);color:var(--cream-muted);background:transparent;font-family:'Inter',sans-serif;transition:all 0.2s; }
.sp-region-tab:hover { border-color:rgba(29,185,84,0.35);color:var(--cream-dim); }
.sp-region-tab.active { background:var(--spotify);color:white;border-color:var(--spotify); }
.sp-table-header { display:flex;align-items:center;justify-content:space-between;margin-bottom:0.8rem;flex-wrap:wrap;gap:0.5rem; }
.sp-table-title { font-size:0.78rem;font-weight:600;color:var(--cream);display:flex;align-items:center;gap:0.5rem; }
.sp-count-badge { background:var(--gold-subtle);color:var(--gold);border:1px solid var(--border);padding:0.1rem 0.55rem;font-size:0.58rem;font-weight:600; }
.sp-updated-label { font-size:0.6rem;color:var(--cream-muted); }
.sp-chart-table { width:100%;border-collapse:collapse; }
.sp-chart-table th { font-size:0.55rem;font-weight:600;color:var(--gold);letter-spacing:2.5px;text-align:left;padding:0.6rem 0.8rem;border-bottom:1px solid var(--border); }
.sp-chart-table td { padding:0.7rem 0.8rem;border-bottom:1px solid rgba(255,255,255,0.025);font-size:0.78rem;color:var(--cream-dim);vertical-align:middle; }
.sp-rank-badge { background:var(--gold-subtle);color:var(--gold);border:1px solid var(--border);padding:2px 7px;font-family:'DM Mono',monospace;font-size:0.67rem;font-weight:500;white-space:nowrap; }
.sp-song-name { color:var(--cream);font-weight:500;font-size:0.8rem; }
.sp-artist-name { color:var(--cream-muted);font-size:0.65rem;margin-top:2px; }
.sp-daily-val { color:var(--spotify);font-family:'DM Mono',monospace;font-weight:500;font-size:0.77rem; }
.sp-change-up { color:var(--green);font-weight:600;font-size:0.73rem; }
.sp-change-dn { color:var(--red);font-weight:600;font-size:0.73rem; }
.sp-empty { text-align:center;padding:2.5rem;color:var(--cream-muted);font-size:0.78rem; }

/* ══════════════════════════════════════════
   APPLE MUSIC
══════════════════════════════════════════ */
.apple-wrap { background:var(--bg-card);border:1px solid var(--border);padding:1.8rem;margin-bottom:1rem; }
.apple-top-bar { display:flex;align-items:center;justify-content:space-between;margin-bottom:1.5rem;flex-wrap:wrap;gap:.6rem; }
.apple-logo-title { display:flex;align-items:center;gap:.5rem;font-family:'Space Grotesk',sans-serif;font-size:1.1rem;font-weight:600;color:var(--cream); }
.apple-meta { display:flex;align-items:center;gap:.6rem;font-size:.6rem;color:var(--cream-muted); }
.apple-refresh-btn { background:transparent;border:1px solid var(--border);color:var(--cream-dim);font-size:.65rem;padding:.22rem .7rem;cursor:pointer;transition:.2s;font-family:'Inter',sans-serif;display:flex;align-items:center;gap:.3rem; }
.apple-refresh-btn:hover { border-color:var(--apple);color:var(--apple); }
.apple-refresh-btn.spinning svg { animation:spin .7s linear infinite; }
.apple-reach-grid { display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:1px;background:var(--border);border:1px solid var(--border);margin-bottom:1.5rem; }
.apple-reach-card { background:var(--bg-card);padding:1.3rem;position:relative;overflow:hidden;transition:background 0.2s; }
.apple-reach-card:hover { background:var(--bg-raised); }
.apple-reach-card::after { content:'';position:absolute;top:0;left:0;right:0;height:1px;background:linear-gradient(90deg,var(--apple),transparent); }
.apple-reach-label { font-size:.58rem;color:var(--cream-muted);text-transform:uppercase;letter-spacing:2px;margin-bottom:.5rem; }
.apple-reach-value { font-family:'DM Mono',monospace;font-size:1.5rem;font-weight:400;color:var(--cream);line-height:1; }
.apple-reach-sub { font-size:.65rem;color:var(--green);margin-top:.4rem;display:flex;align-items:center;gap:.3rem; }
.apple-section-label { font-size:.63rem;font-weight:600;color:var(--cream-muted);text-transform:uppercase;letter-spacing:2.5px;margin-bottom:.8rem; }
.apple-table { width:100%;border-collapse:collapse; }
.apple-table th { font-size:.55rem;font-weight:600;text-align:left;padding:.6rem .7rem;color:var(--gold);border-bottom:1px solid var(--border);letter-spacing:2px;text-transform:uppercase; }
.apple-table th.right { text-align:right; }
.apple-table td { padding:.65rem .7rem;border-bottom:1px solid rgba(255,255,255,.025);font-size:.76rem;color:var(--cream-dim);vertical-align:middle; }
.apple-table tr:last-child td { border-bottom:none; }
.apple-table tr:hover td { background:rgba(252,60,68,.02); }
.apple-rank { font-family:'DM Mono',monospace;font-size:.67rem;color:var(--gold);background:var(--gold-subtle);border:1px solid var(--border);padding:.12rem .5rem;display:inline-block; }
.apple-song-name { font-weight:500;color:var(--cream);font-size:.8rem; }
.apple-song-artist { font-size:.65rem;color:var(--cream-muted);margin-top:.12rem; }
.apple-chart-badge { background:rgba(252,60,68,.07);color:var(--apple);border:1px solid rgba(252,60,68,.18);font-size:.62rem;font-family:'DM Mono',monospace;padding:.1rem .5rem;margin-right:.3rem;display:inline-block; }
.apple-country-count { font-family:'DM Mono',monospace;font-size:.7rem;color:var(--cream-dim); }
.apple-empty { text-align:center;padding:2.5rem;color:var(--cream-muted);font-size:.76rem; }
.apple-error { background:var(--gold-subtle);border:1px solid var(--border);border-left:2px solid var(--gold);padding:.9rem 1.1rem;font-size:.74rem;color:var(--cream-dim);line-height:1.75; }

/* ══════════════════════════════════════════
   THEME SELECTOR
══════════════════════════════════════════ */
.theme-selector { position:relative; }
/* ===== LANGUAGE SELECTOR ===== */
.lang-selector { position:relative; }
.lang-btn { display:flex;align-items:center;gap:0.45rem;background:transparent;border:1px solid var(--border);padding:0.32rem 0.8rem;color:var(--cream-dim);font-size:0.67rem;cursor:pointer;font-family:'Inter',sans-serif;transition:border-color 0.2s;white-space:nowrap; }
.lang-btn:hover { border-color:var(--gold); }
.lang-flag { font-size:0.85rem;line-height:1; }
.lang-dropdown { display:none;position:absolute;left:0;top:calc(100% + 6px);background:var(--bg-card);border:1px solid var(--border);min-width:128px;z-index:100;overflow:hidden; }
.lang-selector:hover .lang-dropdown,.lang-dropdown.open { display:block; }
.lang-option { display:flex;align-items:center;gap:0.6rem;padding:0.6rem 1rem;font-size:0.75rem;color:var(--cream-dim);cursor:pointer;transition:background 0.15s; }
.lang-option:hover,.lang-option.active { background:var(--bg-raised);color:var(--gold); }
.theme-btn { display:flex;align-items:center;gap:0.5rem;background:transparent;border:1px solid var(--border);padding:0.32rem 0.8rem;color:var(--cream-dim);font-size:0.67rem;cursor:pointer;font-family:'Inter',sans-serif;transition:border-color 0.2s; }
.theme-btn:hover { border-color:var(--gold); }
.theme-dot,.td { width:8px;height:8px;border-radius:50%;display:inline-block; }
.td-black { background:#111;border:1px solid #555; }
.td-white { background:#f5f5f5;border:1px solid #bbb; }
.td-rojo { background:#ff1f3d; }
.td-dom { background:#60a5fa; }
.theme-dropdown { display:none;position:absolute;right:0;top:calc(100% + 6px);background:var(--bg-card);border:1px solid var(--border);min-width:128px;z-index:100;overflow:hidden; }
.theme-selector:hover .theme-dropdown,.theme-dropdown.open { display:block; }
.theme-option { display:flex;align-items:center;gap:0.6rem;padding:0.6rem 1rem;font-size:0.75rem;color:var(--cream-dim);cursor:pointer;transition:background 0.15s; }
.theme-option:hover,.theme-option.active { background:var(--bg-raised);color:var(--gold); }

/* ══════════════════════════════════════════
   TOAST
══════════════════════════════════════════ */
.toast { position:fixed;bottom:2rem;left:50%;transform:translateX(-50%) translateY(80px);background:var(--bg-card);border:1px solid var(--border);border-top:2px solid var(--gold);padding:0.8rem 1.6rem;font-size:0.75rem;color:var(--cream);opacity:0;transition:0.35s;z-index:999;white-space:nowrap; }
.toast.show { transform:translateX(-50%) translateY(0);opacity:1; }

/* ══════════════════════════════════════════
   ADMIN PANEL
══════════════════════════════════════════ */
.admin-overlay { display:none;position:fixed;inset:0;background:rgba(0,0,0,0.85);backdrop-filter:blur(10px);z-index:300;align-items:center;justify-content:center; }
.admin-overlay.open { display:flex; }
.admin-modal { background:var(--bg-card);border:1px solid var(--border);width:min(420px,90vw);padding:2rem; }
.admin-modal h2 { font-family:'Space Grotesk',sans-serif;color:var(--gold);font-size:1.3rem;font-weight:600;margin-bottom:1.2rem; }
.admin-panel { display:none;position:fixed;inset:0;background:var(--admin-bg);z-index:250;overflow-y:auto; }
.admin-panel.open { display:block; }
.admin-nav { background:var(--bg-card);border-bottom:1px solid var(--border);padding:1rem 2rem;display:flex;justify-content:space-between;align-items:center;position:sticky;top:0;z-index:10; }
.admin-nav h1 { font-family:'Space Grotesk',sans-serif;font-size:1.15rem;font-weight:700;color:var(--gold); }
.admin-tabs { display:flex;gap:0.4rem;padding:1.5rem 2rem 0;flex-wrap:wrap; }
.admin-tab { background:transparent;border:1px solid var(--border);padding:0.38rem 1rem;color:var(--cream-muted);cursor:pointer;font-size:0.7rem;transition:all 0.2s;font-family:'Inter',sans-serif;letter-spacing:0.3px; }
.admin-tab.active { background:var(--gold);color:#080808;border-color:var(--gold);font-weight:600; }
.admin-tab:hover:not(.active) { border-color:var(--gold);color:var(--gold); }
.admin-content { padding:1.5rem 2rem; }
.admin-section { display:none; }
.admin-section.active { display:block; }
.admin-card { background:var(--admin-card);border:1px solid var(--border);padding:1.5rem;margin-bottom:1.2rem; }
.admin-card h3 { font-family:'Space Grotesk',sans-serif;font-size:0.95rem;font-weight:600;color:var(--cream);margin-bottom:1rem; }
.admin-list-item { background:var(--admin-item);padding:0.75rem 1rem;display:flex;align-items:center;gap:1rem;margin-bottom:0.4rem;border:1px solid transparent;transition:border-color 0.15s; }
.admin-list-item:hover { border-color:var(--border); }
.ali-info { flex:1;min-width:0; }
.ali-name { font-size:0.8rem;font-weight:500;color:var(--cream); }
.ali-sub { font-size:0.65rem;color:var(--cream-muted);margin-top:0.15rem; }
.ali-actions { display:flex;gap:0.4rem; }
.form-group { margin-bottom:1rem; }
.form-group label { display:block;font-size:0.62rem;font-weight:500;color:var(--cream-muted);margin-bottom:0.4rem;text-transform:uppercase;letter-spacing:1.5px; }
.img-grid { display:grid;grid-template-columns:repeat(auto-fill,minmax(130px,1fr));gap:0.8rem; }
.img-item { position:relative;aspect-ratio:1;background:var(--bg-raised);overflow:hidden; }
.img-item img { width:100%;height:100%;object-fit:cover; }
.img-del { position:absolute;top:4px;right:4px;background:rgba(0,0,0,0.8);border:none;color:white;padding:2px 6px;cursor:pointer;font-size:0.6rem; }

/* ══════════════════════════════════════════
   AVISOS
══════════════════════════════════════════ */
.gallery-grid { display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:1px;background:var(--border);border:1px solid var(--border); }
.wv-grid { display:grid; grid-template-columns:repeat(auto-fill, minmax(280px, 1fr)); gap:1rem; }
.wv-card { background:var(--bg-card);border:1px solid var(--border);border-radius:14px;overflow:hidden;cursor:pointer;transition:background 0.2s,border-color 0.2s;display:flex;flex-direction:column; }
.wv-card:hover { background:var(--bg-raised);border-color:var(--gold-dim); }
.wv-card-header { display:flex;align-items:center;gap:0.75rem;padding:0.9rem 1rem 0.6rem; }
.wv-avatar { width:36px;height:36px;border-radius:50%;background:linear-gradient(135deg,var(--gold-dim),var(--gold));display:flex;align-items:center;justify-content:center;font-size:0.62rem;font-weight:700;color:#080808;font-family:'DM Mono',monospace;flex-shrink:0; }
.wv-card-author { font-size:0.78rem;font-weight:600;color:var(--cream);display:flex;align-items:center;gap:0.3rem; }
.wv-verified { color:var(--gold);font-size:0.62rem; }
.wv-card-date { font-size:0.62rem;color:var(--cream-muted);margin-top:1px; }
.wv-card-caption { padding:0.3rem 1rem 0.5rem;font-size:0.78rem;color:var(--cream-dim);line-height:1.5; }
.wv-card-img { width:100%;aspect-ratio:1;object-fit:cover;display:block; }
.wv-card-footer { padding:0.6rem 1rem;display:flex;align-items:center;gap:0.5rem;border-top:1px solid var(--border); }
.wv-tag { font-size:0.57rem;color:var(--cream-muted);text-transform:uppercase;letter-spacing:2px;font-weight:500; }

/* ── POST MODAL ── */
#post-modal-overlay { position:fixed;inset:0;background:rgba(0,0,0,0.92);backdrop-filter:blur(16px);z-index:400;display:none;align-items:center;justify-content:center; }
#post-modal-overlay.open { display:flex; }
#post-modal { position:relative;background:var(--bg-card);border:1px solid var(--border);max-width:900px;width:95vw;max-height:90vh;display:flex;overflow:hidden; }
#pm-inner { background:var(--bg-card);border:1px solid var(--border);max-width:900px;width:95vw;max-height:90vh;display:flex;position:relative;overflow:hidden; }
#pm-image-col { flex:1;background:#000;display:flex;align-items:center;justify-content:center;min-height:400px;position:relative; }
#pm-image-col img { width:100%;height:100%;object-fit:contain;max-height:80vh; }
#pm-prev,#pm-next { position:absolute;top:50%;transform:translateY(-50%);background:rgba(0,0,0,0.5);border:1px solid rgba(255,255,255,0.08);color:white;font-size:1.1rem;cursor:pointer;width:34px;height:34px;display:flex;align-items:center;justify-content:center;transition:0.2s;z-index:2; }
#pm-prev { left:.6rem; } #pm-next { right:.6rem; }
#pm-prev:hover,#pm-next:hover { background:var(--gold-subtle);border-color:var(--gold); }
#pm-detail-col { width:300px;flex-shrink:0;display:flex;flex-direction:column;border-left:1px solid var(--border);overflow:hidden; }
#pm-header { display:flex;align-items:center;gap:.75rem;padding:1rem;border-bottom:1px solid var(--border);flex-shrink:0; }
#pm-avatar { width:34px;height:34px;border-radius:50%;background:linear-gradient(135deg,var(--gold-dim),var(--gold));display:flex;align-items:center;justify-content:center;font-size:0.6rem;font-weight:700;color:#080808;font-family:'DM Mono',monospace;flex-shrink:0; }
#pm-author { font-size:0.8rem;font-weight:600;color:var(--cream); }
#pm-author-handle { font-size:0.65rem;color:var(--cream-muted);margin-top:1px; }
#pm-close { position:absolute;top:.8rem;right:.8rem;background:rgba(255,255,255,.05);border:none;color:white;font-size:0.9rem;cursor:pointer;border-radius:50%;width:30px;height:30px;display:flex;align-items:center;justify-content:center;transition:.2s;z-index:3; }
#pm-close:hover { background:var(--gold-subtle); }
#pm-body { flex:1;overflow-y:auto;padding:0; }
#pm-caption-wrap { padding:.85rem 1rem;border-bottom:1px solid var(--border);font-size:.78rem;color:var(--cream-dim);line-height:1.65; }
#pm-caption-author { font-weight:600;color:var(--cream);margin-right:.3rem; }
#pm-comments-list { padding:.5rem .8rem; }
.pm-comment { display:flex;gap:.6rem;padding:.6rem .2rem;border-bottom:1px solid rgba(255,255,255,.03); }
.pm-comment:last-child { border-bottom:none; }
.pm-comment-avatar { width:26px;height:26px;border-radius:50%;flex-shrink:0;background:var(--bg-raised);display:flex;align-items:center;justify-content:center;font-size:.55rem;font-weight:600;color:var(--cream-muted);border:1px solid var(--border); }
.pm-comment-avatar.admin-av { background:linear-gradient(135deg,var(--gold-dim),var(--gold));color:#080808;border-color:var(--gold); }
.pm-comment-body { flex:1;min-width:0; }
.pm-comment-name { font-size:.7rem;font-weight:600;color:var(--cream); }
.pm-comment-name .verified-badge { color:var(--gold);font-size:.58rem;margin-left:.25rem; }
.pm-comment-text { font-size:.73rem;color:var(--cream-dim);line-height:1.5;margin-top:.15rem;word-break:break-word; }
.pm-comment-date { font-size:.58rem;color:var(--cream-muted);margin-top:.22rem; }
.pm-no-comments { text-align:center;padding:1.8rem 1rem;color:var(--cream-muted);font-size:.73rem; }
#pm-footer { border-top:1px solid var(--border);flex-shrink:0; }
#pm-likes-row { display:flex;align-items:center;gap:.6rem;padding:.7rem 1rem .5rem; }
#pm-like-btn { background:none;border:none;font-size:1.1rem;cursor:pointer;color:var(--cream-muted);transition:.2s;padding:0; }
#pm-like-btn.liked { color:var(--red); }
#pm-like-count { font-size:.75rem;color:var(--cream-muted);font-family:'DM Mono',monospace; }
#pm-comment-form { padding:.5rem 1rem .8rem;display:none;flex-direction:column;gap:.5rem; }
#pm-comment-form.visible { display:flex; }
#pm-comment-input { background:var(--input-bg);border:1px solid var(--border);padding:.55rem .8rem;color:var(--cream);font-family:'Inter',sans-serif;font-size:.75rem;outline:none;resize:none;height:60px; }
#pm-comment-input:focus { border-color:var(--gold); }

/* ══════════════════════════════════════════
   STREAM TABLE (bulk edit)
══════════════════════════════════════════ */
.stream-cell { background:transparent;border:none;outline:none;color:var(--cream);font-family:'DM Mono',monospace;font-size:0.75rem;text-align:right;width:100%;padding:0.5rem 0.6rem;transition:background .15s; }
.stream-cell:focus { background:var(--gold-subtle);outline:1px solid var(--gold); }
.stream-row { border-bottom:1px solid rgba(255,255,255,0.03);transition:background .15s; }
.stream-row:hover { background:rgba(255,255,255,0.01); }
.stream-row.modified .stream-cell { color:var(--green); }
.stream-row td { padding:0 !important; }
.stream-row td:first-child { padding:0.4rem 0.8rem !important; }
.stream-title { font-size:0.78rem;font-weight:500;color:var(--cream); }
.stream-prev { font-size:0.6rem;color:var(--cream-muted);margin-top:2px; }

/* ══════════════════════════════════════════
   FOOTER
══════════════════════════════════════════ */
footer {
  border-top: 1px solid var(--border);
  padding: 3rem 2rem;
  text-align: center;
  font-size: 0.65rem;
  color: var(--cream-muted);
  letter-spacing: 0.5px;
}
footer a { color:var(--cream-muted);text-decoration:none;transition:color 0.2s; }
footer a:hover { color:var(--gold); }
.footer-links { display:flex;gap:2rem;justify-content:center;margin-top:0.8rem; }

/* ══════════════════════════════════════════
   RESPONSIVE
══════════════════════════════════════════ */
@media (max-width:700px) {
  nav { padding:0 1.2rem; }
  .nav-links { display:none;flex-direction:column;position:absolute;top:var(--nav-h);left:0;right:0;background:var(--bg-card);border-bottom:1px solid var(--border);border-radius:0;padding:1rem;gap:0.3rem; }
  .nav-links a { justify-content:flex-start;border-radius:8px; }
  .nav-links.open { display:flex; }
  .nav-mobile-btn { display:block; }
  .hero { min-height:auto;padding:4.5rem 1.5rem 3.5rem; }
  section { padding:2.5rem 1.2rem; }
  .goal-stats { grid-template-columns:1fr 1fr; }
  .artist-chart-grid { grid-template-columns:1fr;background:none; }
  .artist-stat-card { border:1px solid var(--border);margin-bottom:1px; }
  .admin-tabs { padding:1rem; }
  .admin-content { padding:1rem; }
  .yt-stats-grid { grid-template-columns:1fr; }
  .yt-video-table th,.yt-video-table td,.spotify-table th,.spotify-table td { padding:0.5rem 0.3rem;font-size:0.68rem; }
  .sp-chart-table th:nth-child(4),.sp-chart-table td:nth-child(4) { display:none; }
  .playlist-grid { grid-template-columns:1fr; }
  .gallery-grid { grid-template-columns:1fr 1fr; }
  .kworb-skeleton { grid-template-columns:1fr; }
}
/* ══════════════════════════════════════════
   ERA ANNOUNCEMENT (mensajes flotantes de nueva era)
══════════════════════════════════════════ */
#era-announce-overlay {
  position: fixed; inset: 0; z-index: 9999;
  background: rgba(5,2,3,0.6);
  backdrop-filter: blur(2px);
  opacity: 0; transition: opacity 0.5s ease;
  cursor: pointer;
  overflow: hidden;
}
#era-announce-overlay.show { opacity: 1; }
#era-announce-overlay.hide { opacity: 0; }
.era-center {
  position: absolute; top: 50%; left: 50%;
  transform: translate(-50%,-50%);
  text-align: center;
  z-index: 2;
}
.era-center .era-kicker {
  font-family: 'DM Mono', monospace;
  font-size: 0.75rem; letter-spacing: 4px;
  color: var(--gold); text-transform: uppercase;
  margin-bottom: 0.8rem;
}
.era-center .era-title {
  font-family: 'Space Grotesk', sans-serif;
  font-weight: 700;
  font-size: clamp(2.4rem, 8vw, 5rem);
  color: var(--era-accent, var(--cream));
  letter-spacing: -0.02em;
  line-height: 1;
  text-shadow: 0 10px 40px rgba(0,0,0,0.6);
}
.era-pill {
  position: absolute;
  background: rgba(237,232,223,0.96);
  color: var(--era-accent, var(--red));
  font-family: 'Space Grotesk', sans-serif;
  font-weight: 700;
  padding: 0.5rem 1.3rem;
  border-radius: 999px;
  font-size: clamp(0.85rem, 1.6vw, 1.15rem);
  white-space: nowrap;
  box-shadow: 0 10px 30px rgba(0,0,0,0.45);
  animation: era-float 5s ease-in-out infinite;
  z-index: 1;
}
@keyframes era-float {
  0%, 100% { transform: translateY(0) rotate(var(--r, -4deg)); }
  50% { transform: translateY(-18px) rotate(var(--r, -4deg)); }
}
#era-announce-close {
  position: absolute; top: 1.4rem; right: 1.4rem;
  width: 38px; height: 38px; border-radius: 50%;
  background: rgba(237,232,223,0.14);
  border: 1px solid var(--border-hi);
  color: var(--cream);
  display: flex; align-items: center; justify-content: center;
  font-size: 1.1rem; cursor: pointer; z-index: 3;
}
</style>
<body>

<nav>
  <a class="nav-logo" href="#" onclick="showPage('home')">EVAN<span>charts</span></a>
  <div class="nav-links" id="nav-links">
    <a class="active" onclick="showPage('home',this)"><span class="nav-ico">🏠</span>Home</a>
    <a onclick="showPage('spotify',this)"><span class="nav-ico">🎧</span>Spotify</a>
    <a onclick="showPage('apple',this)"><span class="nav-ico">🍎</span>Apple Music</a>
    <a onclick="showPage('youtube',this)"><span class="nav-ico">▶</span>YouTube</a>
    <a onclick="showPage('charts',this)"><span class="nav-ico">📊</span>Charts</a>
  </div>
  <div class="nav-right">
    <div class="admin-indicator" id="admin-indicator" style="display:none">
      <div class="admin-dot" id="admin-dot"></div>
      <span id="admin-label" style="display:none">Admin</span>
    </div>
    <!-- Language Selector -->
    <div class="lang-selector" id="langSel">
      <button class="lang-btn" id="langBtn" onclick="toggleLangDropdown()">
        <span class="lang-flag" id="langFlag">🇺🇸</span>
        <span id="langLabel">EN</span> ▾
      </button>
      <div class="lang-dropdown" id="langDropdown">
        <div class="lang-option active" id="lopt-en" onclick="setLang('en')"><span class="lang-flag">🇺🇸</span> English</div>
        <div class="lang-option" id="lopt-es" onclick="setLang('es')"><span class="lang-flag">🇪🇸</span> Español</div>
        <div class="lang-option" id="lopt-ko" onclick="setLang('ko')"><span class="lang-flag">🇰🇷</span> 한국어</div>
      </div>
    </div>
    <!-- Theme Selector -->
    <div class="theme-selector" id="themeSel">
      <button class="theme-btn" onclick="toggleThemeDropdown()" id="themeBtnLabel">
        <span class="theme-dot td-black" id="themeDot"></span>
        <span id="themeLabel">Black</span> ▾
      </button>
      <div class="theme-dropdown">
        <div class="theme-option active" id="topt-black" onclick="setTheme('black')"><span class="td td-black"></span> Black</div>
        <div class="theme-option" id="topt-white" onclick="setTheme('white')"><span class="td td-white"></span> White</div>
        <div class="theme-option" id="topt-rojo" onclick="setTheme('rojo')"><span class="td td-rojo"></span> Rojo</div>
        <div class="theme-option" id="topt-dom" onclick="setTheme('dom')"><span class="td td-dom"></span> DOM</div>
      </div>
    </div>
    <button class="btn btn-ghost btn-sm" id="admin-btn" onclick="openAdminLogin()" style="display:none">⚙️ Admin</button>
    <button class="nav-mobile-btn" onclick="toggleMobileNav()">☰</button>
  </div>
</nav>

<!-- HOME -->
<div id="home" class="page active">
  <div class="hero">
    <video class="hero-bg-video" id="hero-bg-video" autoplay muted loop playsinline></video>
    <div class="hero-content">
      <div class="hero-tag" id="hero-tag">EVAN // LEE HEESEUNG · SOLO ERA</div>
      <h1>EVAN CHARTS<span class="accent">Solo Era · Lee Heeseung</span></h1>
      <p class="hero-sub">Streaming hub | Fan page</p>
      <div class="hero-debut">
        <div class="dot-live"></div>
        <span id="debut-text">Lee Heeseung · Solo Era · Fan Tracker</span>
      </div>
      <div class="stream-cta">
        <a href="#" id="btn-spotify" target="_blank" class="stream-btn">🎵 Spotify</a>
        <a href="#" id="btn-youtube" target="_blank" class="stream-btn">▶ YouTube</a>
        <a href="#" id="btn-applemusic" target="_blank" class="stream-btn">🎶 Apple Music</a>
        <a href="https://weverse.io/enhypen/feed" id="btn-weverse" target="_blank" class="stream-btn">💬 Weverse</a>
        <a href="https://www.instagram.com/enhypen" id="btn-instagram" target="_blank" class="stream-btn">📸 Instagram</a>
      </div>
    </div>
  </div>
  <section>
    <div class="section-header"><div class="section-title" data-lang="sec_goal">Metas y Objetivos</div><div class="section-line"></div></div>
    <div id="goals-list"><div class="loading"><div class="spin"></div><br>Cargando...</div></div>
    <div class="tip-box"><p><span data-lang="tip_streaming">🔥 <strong>Ritual de streaming:</strong> Cada reproducción cuenta. Usa playlists oficiales, evita mute y reproduce al menos 30 segundos. ¡Juntos rompemos récords!</span></p></div>
  </section>
  <section>
    <div class="section-header"><div class="section-title">Playlists destacadas</div><div class="section-line"></div></div>
    <div id="home-playlists"><div class="loading"><div class="spin"></div></div></div>
  </section>
</div>

<!-- SPOTIFY (con tabla de canciones + playlists) -->
<div id="spotify" class="page">
  <section>
    <!-- ARTIST CHART (Kworb auto) -->
    <div class="section-header" style="margin-top:0.5rem"><div class="section-title">Artist Chart</div><div class="section-line"></div></div>
    <div class="artist-chart-wrap">
      <div class="artist-chart-header">
        <div class="artist-chart-title">Spotify · EVAN</div>
        <div class="kworb-meta">
          <span id="kworb-updated">—</span>
          <button class="kworb-refresh-btn" id="kworb-btn" onclick="loadKworbArtist(true)">
            <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M1 4v6h6"/><path d="M23 20v-6h-6"/><path d="M20.49 9A9 9 0 005.64 5.64L1 10m22 4l-4.64 4.36A9 9 0 013.51 15"/></svg>
            Actualizar
          </button>
        </div>
      </div>
      <div id="artist-chart-content">
        <div class="kworb-skeleton">
          <div class="skeleton-card"><div class="skeleton-line"></div><div class="skeleton-line big"></div></div>
          <div class="skeleton-card"><div class="skeleton-line"></div><div class="skeleton-line big"></div></div>
          <div class="skeleton-card"><div class="skeleton-line"></div><div class="skeleton-line big"></div></div>
        </div>
      </div>
    </div>
    <div class="section-header"><div class="section-title">Top Canciones - Spotify</div><div class="section-line"></div></div>
    <div id="spotify-songs-table"><div class="loading"><div class="spin"></div><br>Cargando canciones...</div></div>
    <div class="tip-box"><p><span data-lang="tip_spotify">📊 Datos actualizados manualmente por el staff. Los streams diarios se calculan automáticamente al actualizar el total.</span></p></div>
  </section>
  <section>
    <div class="section-header"><div class="section-title">Album / EP Chart</div><div class="section-line"></div></div>
    <div id="spotify-albums-table"><div class="loading"><div class="spin"></div><br>Cargando álbumes...</div></div>
  </section>
  <section>
    <div class="section-header"><div class="section-title">Spotify Playlists</div><div class="section-line"></div></div>
    <div id="spotify-playlists"><div class="loading"><div class="spin"></div></div></div>
    <div class="tip-box"><p>🎧 Consejo: Escucha las listas en modo aleatorio y sin silenciar. El algoritmo ama la consistencia.</p></div>
  </section>
</div>

<!-- APPLE MUSIC -->
<div id="apple" class="page">
  <section>
    <div class="section-header" style="margin-top:0.5rem">
      <div class="section-title">Apple Music</div>
      <div class="section-line"></div>
    </div>
    <div class="apple-wrap">
      <div class="apple-top-bar">
        <div class="apple-logo-title">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="white"><path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.8-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z"/></svg>
          Apple Music · EVAN
        </div>
        <div class="apple-meta">
          <span id="apple-updated">—</span>
          <button class="apple-refresh-btn" id="apple-refresh-btn" onclick="loadAppleMusic(true)">
            <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M1 4v6h6"/><path d="M23 20v-6h-6"/><path d="M20.49 9A9 9 0 005.64 5.64L1 10m22 4l-4.64 4.36A9 9 0 013.51 15"/></svg>
            Actualizar
          </button>
        </div>
      </div>
      <div id="apple-artist-reach">
        <div class="kworb-skeleton" style="display:grid;grid-template-columns:repeat(2,1fr);gap:1rem">
          <div class="skeleton-card"><div class="skeleton-line"></div><div class="skeleton-line big"></div></div>
          <div class="skeleton-card"><div class="skeleton-line"></div><div class="skeleton-line big"></div></div>
        </div>
      </div>
      <div class="apple-section-label" style="margin-top:1.4rem">Global Top 100 Songs</div>
      <div id="apple-top-songs"><div class="loading"><div class="spin"></div></div></div>
    </div>
    <div class="section-header" style="margin-top:2rem">
      <div class="section-title">Apple Music Playlists</div>
      <div class="section-line"></div>
    </div>
    <div id="apple-playlists"><div class="empty"><p>🎶 Playlists de Apple Music se cargarán aquí</p></div></div>
    <div class="tip-box"><p>🍎 Usa Apple Music para sumar streams internacionales. Cada reproducción cuenta igual.</p></div>
  </section>
</div>

<!-- YOUTUBE -->
<div id="youtube" class="page">
  <section><div class="section-header"><div class="section-title">YouTube Channel Overview</div><div class="section-line"></div></div>
    <div id="yt-stats-container"><div class="loading"><div class="spin"></div></div></div>
    <div class="section-header" style="margin-top:2rem"><div class="section-title">Music Video Chart</div><div class="section-line"></div></div>
    <div id="yt-videos-container"><div class="loading"><div class="spin"></div></div></div>
    <div class="tip-box"><p>📊 Datos actualizados automáticamente desde YouTube API cada hora. ¡Apoya los videos recientes!</p></div>
  </section>
</div>

<!-- CHARTS -->
<div id="charts" class="page">
  <section>
    <div class="section-header"><div class="section-title">Spotify Charts — EVAN</div><div class="section-line"></div></div>

    <!-- Spotify Charts UI -->
    <div class="sp-charts-wrap">

      <!-- Header -->
      <div class="sp-charts-header">
        <div class="sp-charts-logo">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="white"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm4.65 14.4c-.2.33-.61.43-.94.23-2.58-1.58-5.83-1.93-9.65-1.06-.37.08-.73-.15-.81-.52-.08-.37.15-.73.52-.81 4.18-.95 7.77-.54 10.65 1.22.33.2.43.61.23.94zm1.24-2.77c-.25.4-.77.52-1.17.27-2.95-1.82-7.45-2.34-10.94-1.28-.45.14-.92-.12-1.06-.57-.14-.45.12-.92.57-1.06 3.98-1.21 8.93-.62 12.33 1.47.4.25.52.77.27 1.17zm.11-2.89C14.5 8.78 8.91 8.54 5.69 9.52c-.54.17-1.11-.13-1.28-.67-.17-.54.13-1.11.67-1.28 3.72-1.13 9.91-.91 13.81 1.49.49.3.65.94.35 1.43-.3.49-.94.65-1.43.35z"/></svg>
        </div>
        <span class="sp-charts-title">Spotify Charts</span>
        <div style="margin-left:auto;display:flex;align-items:center;gap:0.5rem">
          <div class="dot-live" style="width:6px;height:6px"></div>
          <span style="font-size:0.65rem;color:var(--text-muted)">Actualizado por el staff</span>
        </div>
      </div>

      <!-- Chart Type -->
      <div class="sp-charts-section-label">Chart Type</div>
      <div class="sp-type-tabs">
        <button class="sp-type-tab active" onclick="spSetType(this,'daily-songs')">🎵 Daily Songs</button>
        <button class="sp-type-tab" onclick="spSetType(this,'weekly-songs')">📅 Weekly Songs</button>
        <button class="sp-type-tab" onclick="spSetType(this,'daily-artists')">🎤 Daily Artists</button>
        <button class="sp-type-tab" onclick="spSetType(this,'weekly-albums')">💿 Weekly Albums</button>
      </div>

      <!-- Region -->
      <div class="sp-charts-region-box">
        <div class="sp-charts-section-label" style="margin-bottom:0.6rem">Region</div>
        <div class="sp-region-tabs">
          <button class="sp-region-tab active" onclick="spSetRegion(this,'global','Global','🌐')">🌐 Global</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'usa','USA','🇺🇸')">🇺🇸 USA</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'uk','UK','🇬🇧')">🇬🇧 UK</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'kr','Korea','🇰🇷')">🇰🇷 Korea</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'fr','France','🇫🇷')">🇫🇷 France</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'de','Germany','🇩🇪')">🇩🇪 Germany</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'br','Brazil','🇧🇷')">🇧🇷 Brazil</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'mx','Mexico','🇲🇽')">🇲🇽 Mexico</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'jp','Japan','🇯🇵')">🇯🇵 Japan</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'th','Thailand','🇹🇭')">🇹🇭 Thailand</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'au','Australia','🇦🇺')">🇦🇺 Australia</button>
          <button class="sp-region-tab" onclick="spSetRegion(this,'ca','Canada','🇨🇦')">🇨🇦 Canada</button>
        </div>
      </div>

      <!-- Table Header -->
      <div class="sp-table-header">
        <div class="sp-table-title" id="spTableTitle">🌐 Global Daily Songs <span class="sp-count-badge" id="spItemCount">0 items</span></div>
        <div class="sp-updated-label">🕐 Updated: <span id="spUpdatedTime">—</span></div>
      </div>

      <!-- Table -->
      <div id="sp-charts-table-wrap">
        <div class="loading"><div class="spin"></div><br>Cargando charts...</div>
      </div>

    </div>

    <!-- Goals table (ahora debajo de Charts) -->
    <div class="section-header" style="margin-top:2rem"><div class="section-title">Progreso de metas</div><div class="section-line"></div></div>
    <div id="charts-goals-table"><div class="loading"><div class="spin"></div></div></div>
  </section>
</div>


<!-- POST MODAL (estilo Weverse) -->
<div id="post-modal-overlay" onclick="closePostModal(event)">
  <div id="post-modal" onclick="event.stopPropagation()">
    <!-- Left: Image -->
    <div id="pm-image-col">
      <button id="pm-prev" onclick="pmNav(-1)">‹</button>
      <img id="pm-img" src="" alt="">
      <button id="pm-next" onclick="pmNav(1)">›</button>
    </div>
    <!-- Right: Detail -->
    <div id="pm-detail-col">
      <div id="pm-header">
        <div id="pm-avatar">SV</div>
        <div>
          <div id="pm-author">Admin</div>
          <div id="pm-author-handle">@EVANchartsTeam</div>
        </div>
        <button id="pm-close" onclick="closePostModal()">✕</button>
      </div>
      <div id="pm-body">
        <div id="pm-caption-wrap">
          <span id="pm-caption-author-name"></span>
          <span id="pm-caption-text"></span>
        </div>
        <div id="pm-comments-list"></div>
      </div>
      <div id="pm-footer">
        <div id="pm-likes-row">
          <button id="pm-like-btn" onclick="toggleLike()">🤍</button>
          <span id="pm-like-count">0</span>
          <span id="pm-date-post"></span>
        </div>
        <div id="pm-comment-form">
          <input id="pm-comment-name" class="input" placeholder="Tu nombre (opcional)" style="font-size:0.72rem;padding:0.4rem 0.7rem;margin-bottom:0.4rem">
          <textarea id="pm-comment-input" rows="2" placeholder="Escribe un comentario..."></textarea>
          <div id="pm-comment-actions">
            <button class="btn btn-ghost btn-sm" onclick="cancelComment()">Cancelar</button>
            <button class="btn btn-sm" onclick="submitComment()">Publicar</button>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- ADMIN LOGIN MODAL -->
<div class="admin-overlay" id="admin-overlay" onclick="closeAdminLogin(event)">
  <div class="admin-modal" onclick="event.stopPropagation()">
    <h2>🔐 Acceso Admin</h2>
    <p class="sub" style="color:var(--text-muted); margin-bottom:1.5rem">Solo staff de EVANcharts</p>
    <div class="form-group"><label>Contraseña</label><div class="pass-wrap"><input class="input" id="admin-pass" type="password" placeholder="******" onkeydown="if(event.key==='Enter')checkAdmin()"><button class="eye-btn" onclick="togglePassVis()" style="background:none; border:none; color:var(--text-muted); margin-left:0.5rem">👁️</button></div><div class="error-msg" id="login-error" style="display:none; color:var(--primary-red)">Contraseña incorrecta</div></div>
    <div style="display:flex; gap:0.8rem; justify-content:flex-end; margin-top:1rem"><button class="btn btn-ghost" onclick="closeAdminLogin()">Cancelar</button><button class="btn" onclick="checkAdmin()">Entrar</button></div>
  </div>
</div>

<!-- ADMIN PANEL (con pestaña Spotify Data) -->
<div class="admin-panel" id="admin-panel">
  <div class="admin-nav"><h1 style="font-family:Orbitron">⚙️ ADMIN · EVANcharts</h1><div><button class="btn btn-ghost btn-sm" onclick="closeAdmin()">✕ Cerrar</button></div></div>
  <div class="admin-tabs">
    <button class="admin-tab active" onclick="switchAdminTab('tab-goals',this)">📊 Metas</button>
    <button class="admin-tab" onclick="switchAdminTab('tab-playlists',this)">🎵 Playlists</button>
    <button class="admin-tab" onclick="switchAdminTab('tab-ytdata',this)">📡 YouTube API</button>
    <button class="admin-tab" onclick="switchAdminTab('tab-spotifydata',this)">🎧 Spotify Data</button>
    <button class="admin-tab" onclick="switchAdminTab('tab-albums',this)">💿 Álbumes</button>
    <button class="admin-tab" onclick="switchAdminTab('tab-charts',this)">📊 Sp. Charts</button>
    <button class="admin-tab" onclick="switchAdminTab('tab-apple',this)">🍎 Apple Music</button>
    <button class="admin-tab" onclick="switchAdminTab('tab-config',this)">⚙️ Configuración</button>
    <button class="admin-tab" onclick="switchAdminTab('tab-images',this)">🖼️ Imágenes</button>
  </div>
  <div class="admin-content">
    <!-- Metas -->
    <div class="admin-section active" id="tab-goals"><div class="admin-card"><h3>Metas actuales</h3><div id="admin-goals-list"></div></div><div class="admin-card"><h3>+ Agregar meta</h3><div class="form-group"><label>Canción</label><input class="input" id="ng-song" placeholder="ej: Bite Me"></div><div class="form-row" style="display:grid; grid-template-columns:1fr 1fr; gap:0.7rem"><div class="form-group"><label>Streams actuales</label><input class="input" id="ng-current" placeholder="ej: 125M"></div><div class="form-group"><label>Meta</label><input class="input" id="ng-target" placeholder="ej: 200M"></div></div><div class="form-group"><label>Plataforma</label><select class="input" id="ng-platform"><option>Spotify</option><option>YouTube</option><option>Apple Music</option></select></div><div class="form-group"><label>URL imagen de portada (opcional)</label><input class="input" id="ng-img" placeholder="https://i.scdn.co/image/..."></div><button class="btn" onclick="addGoal()">Agregar</button></div></div>

    <!-- Playlists -->
    <div class="admin-section" id="tab-playlists"><div class="admin-card"><h3>Playlists actuales</h3><div id="admin-playlists-list"></div></div><div class="admin-card"><h3>+ Agregar playlist</h3><div class="form-group"><label>Nombre</label><input class="input" id="np-name"></div><div class="form-group"><label>Descripción</label><input class="input" id="np-desc"></div><div class="form-group"><label>URL</label><input class="input" id="np-url"></div><div class="form-row" style="display:grid; grid-template-columns:1fr 1fr; gap:0.7rem"><div class="form-group"><label>Plataforma</label><select class="input" id="np-platform"><option value="spotify">Spotify</option><option value="youtube">YouTube</option><option value="apple">Apple Music</option><option value="stationhead">Stationhead</option></select></div><div class="form-group"><label>Icono (emoji)</label><input class="input" id="np-icon" placeholder="🎵"></div></div><button class="btn" onclick="addPlaylist()">Agregar</button></div></div>

    <!-- YouTube Data (completo) -->
    <div class="admin-section" id="tab-ytdata">
      <div class="admin-card">
        <h3>📡 YouTube API — Datos automáticos</h3>
        <p style="font-size:0.8rem;color:var(--text-dim);margin-bottom:1rem">Los datos se obtienen directo de YouTube API v3. No necesitas escribir nada manualmente.</p>
        <button class="btn" onclick="refreshYTFromAPI()" id="btn-refresh-yt">🔄 Actualizar ahora desde YouTube</button>
        <div id="admin-yt-status" style="margin-top:1rem;font-size:0.75rem;color:var(--text-muted)">Cache: esperando primera carga...</div>
      </div>
      <div class="admin-card">
        <h3>🎬 Videos en seguimiento</h3>
        <p style="font-size:0.75rem;color:var(--text-muted);margin-bottom:1rem">Edita los IDs en el array <code>YT_VIDEO_IDS</code> dentro del código para agregar/quitar videos.</p>
        <div id="admin-videos-list"></div>
      </div>
      <div class="admin-card">
        <h3>🆘 Fallback manual (si la API falla)</h3>
        <div class="form-group"><label>Suscriptores</label><input class="input" id="yt-subs" placeholder="17600000"></div>
        <div class="form-group"><label>Vistas totales del canal</label><input class="input" id="yt-views" placeholder="3200000000"></div>
        <div class="form-group"><label>+ Suscriptores hoy</label><input class="input" id="yt-subs-daily" placeholder="100000"></div>
        <div class="form-group"><label>+ Vistas hoy</label><input class="input" id="yt-views-daily" placeholder="3200000"></div>
        <button class="btn btn-ghost" onclick="saveYTStats()">Guardar fallback manual</button>
      </div>
    </div>

    <!-- Spotify Data -->
    <div class="admin-section" id="tab-spotifydata">
      <!-- Artist Chart Manual -->
      <div class="admin-card" style="border-color:rgba(29,185,84,0.25)">
        <h3 style="color:#1db954">🎧 Artist Chart — Oyentes y Seguidores</h3>
        <p style="font-size:0.78rem;color:var(--text-muted);margin-bottom:1.2rem">Actualiza manualmente los datos del Artist Chart. Deja en blanco los campos que no quieras cambiar.</p>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:0.8rem">
          <div class="form-group">
            <label>Monthly Listeners</label>
            <input class="input" id="sp-monthly-listeners" placeholder="ej: 9495882" style="font-family:'Orbitron',monospace">
            <span style="font-size:0.68rem;color:var(--text-muted);margin-top:0.3rem;display:block">Número sin puntos ni comas</span>
          </div>
          <div class="form-group">
            <label>Total Followers</label>
            <input class="input" id="sp-followers" placeholder="ej: 14980663" style="font-family:'Orbitron',monospace">
            <span style="font-size:0.68rem;color:var(--text-muted);margin-top:0.3rem;display:block">Número sin puntos ni comas</span>
          </div>
          <div class="form-group">
            <label>Total Streams</label>
            <input class="input" id="sp-total-streams" placeholder="ej: 7226138226" style="font-family:'Orbitron',monospace">
            <span style="font-size:0.68rem;color:var(--text-muted);margin-top:0.3rem;display:block">Déjalo vacío para calcular automático</span>
          </div>
          <div class="form-group">
            <label>+Streams Today</label>
            <input class="input" id="sp-streams-today" placeholder="ej: 4847454" style="font-family:'Orbitron',monospace">
            <span style="font-size:0.68rem;color:var(--text-muted);margin-top:0.3rem;display:block">Déjalo vacío para calcular automático</span>
          </div>
        </div>
        <button class="btn" style="background:#1a5c2e" onclick="saveArtistChartManual()">💾 Guardar Artist Chart</button>
      </div>
      <div class="admin-card">
        <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:0.8rem;margin-bottom:0.8rem">
          <h3 style="margin:0">📊 Actualización masiva de streams</h3>
          <a href="https://kworb.net/spotify/" title="Kworb — disponible cuando EVAN debute" target="_blank" class="btn btn-ghost btn-sm">🔗 Kworb</a>
        </div>
        <div style="background:var(--bg-elevated);border:1px solid var(--border-dim);border-radius:12px;padding:0.9rem;margin-bottom:1rem;font-size:0.72rem;color:var(--text-muted);line-height:1.8">
          <strong style="color:var(--text-main)">Cómo usar:</strong> Ve a Kworb → selecciona toda la tabla (Ctrl+A o arrastra) → copia (Ctrl+C) → pega abajo → Procesar → Guardar.
        </div>
        <label style="font-size:0.72rem;font-weight:600;color:var(--text-dim);margin-bottom:0.4rem;display:block">Pega la tabla de Kworb aquí</label>
        <textarea id="paste-area" rows="8" placeholder="| Song Title | Streams | Daily |&#10;| --- | --- | --- |&#10;| Bite Me | 503,978,514 | 268,528 |&#10;..."
          style="width:100%;background:var(--input-bg);border:1px solid var(--border-dim);border-radius:12px;padding:0.8rem;color:var(--text-main);font-family:monospace;font-size:0.75rem;resize:vertical;outline:none;line-height:1.6;box-sizing:border-box"
          onfocus="this.style.borderColor='var(--primary-red)'" onblur="this.style.borderColor='var(--border-dim)'"></textarea>
        <div style="display:flex;gap:0.7rem;align-items:center;flex-wrap:wrap;margin-top:0.7rem">
          <button class="btn" onclick="processPaste()">● Procesar</button>
          <button class="btn" id="paste-save-btn" onclick="savePasteData()" style="display:none;background:#1a5c2e">💾 Guardar en Supabase</button>
          <button class="btn btn-ghost btn-sm" onclick="clearPaste()">✕ Limpiar</button>
          <button class="btn btn-danger btn-sm" onclick="deleteAllSongs()" style="margin-left:auto">🗑️ Eliminar todas las canciones</button>
          <span id="paste-status" style="font-size:0.75rem;color:var(--text-muted)"></span>
          <span id="paste-save-status" style="font-size:0.75rem;color:var(--text-muted)"></span>
        </div>
        <!-- Preview -->
        <div id="paste-preview" style="display:none;margin-top:1rem">
          <div style="font-size:0.72rem;font-weight:600;color:var(--text-dim);margin-bottom:0.5rem" id="paste-preview-label"></div>
          <div style="overflow-x:auto;max-height:260px;overflow-y:auto;border:1px solid var(--border-dim);border-radius:10px">
            <table style="width:100%;border-collapse:collapse;min-width:380px">
              <thead><tr style="background:var(--bg-elevated);position:sticky;top:0">
                <th style="padding:0.5rem 0.7rem;font-size:0.6rem;color:var(--primary-red);font-family:'Orbitron',monospace;text-align:left;border-bottom:1px solid var(--border-dim)">CANCIÓN</th>
                <th style="padding:0.5rem 0.7rem;font-size:0.6rem;color:var(--primary-red);font-family:'Orbitron',monospace;text-align:right;border-bottom:1px solid var(--border-dim)">STREAMS</th>
                <th style="padding:0.5rem 0.7rem;font-size:0.6rem;color:var(--primary-red);font-family:'Orbitron',monospace;text-align:right;border-bottom:1px solid var(--border-dim)">DAILY</th>
                <th style="padding:0.5rem 0.7rem;font-size:0.6rem;color:var(--primary-red);font-family:'Orbitron',monospace;text-align:center;border-bottom:1px solid var(--border-dim)">ESTADO</th>
              </tr></thead>
              <tbody id="paste-preview-body"></tbody>
            </table>
          </div>
          <div style="display:flex;gap:0.7rem;margin-top:0.8rem;flex-wrap:wrap;align-items:center">
            <button class="btn" onclick="savePasteData()">💾 Guardar en Supabase</button>
          </div>
          <div id="paste-save-status" style="font-size:0.75rem;color:var(--text-muted);margin-top:0.5rem"></div>
        </div>
      </div>
      <div class="admin-card">
        <h3>🎧 Lista de canciones</h3>
        <div id="admin-spotify-songs-list"></div>
      </div>
      <div class="admin-card">
        <details>
          <summary style="cursor:pointer;font-weight:600;color:var(--text-dim);font-size:0.85rem">+ Agregar canción manualmente</summary>
          <div style="margin-top:1rem">
            <div class="form-group"><label>Título</label><input class="input" id="ns-title" placeholder="ej: Bite Me"></div>
            <div class="form-group"><label>Streams totales</label><input class="input" id="ns-streams" placeholder="0"></div>
            <div class="form-group"><label>URL de Spotify (opcional)</label><input class="input" id="ns-url" placeholder="https://open.spotify.com/track/..."></div>
            <button class="btn" style="margin-top:0.5rem" onclick="addSpotifySong()">Agregar canción</button>
          </div>
        </details>
      </div>
      <div class="admin-card">
        <details>
          <summary style="cursor:pointer;font-weight:600;color:var(--text-dim);font-size:0.85rem">📥 Precargar discografía completa de EVAN</summary>
          <div style="margin-top:1rem">
            <p style="font-size:0.78rem;color:var(--text-muted);margin-bottom:1rem">Carga todas las canciones de EVAN con streams en 0. Solo hazlo una vez. Si ya tienes canciones cargadas, se saltarán las duplicadas.</p>
            <button class="btn" onclick="preloadDiscography()">📥 Cargar discografía</button>
            <div id="preload-status" style="margin-top:0.8rem;font-size:0.78rem;color:var(--text-muted)"></div>
          </div>
        </details>
      </div>
    </div>

    <!-- Álbumes -->
    <div class="admin-section" id="tab-albums">
      <div class="admin-card">
        <div style="display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:0.8rem;margin-bottom:0.8rem">
          <h3 style="margin:0">📊 Actualización masiva de álbumes</h3>
          <a href="https://kworb.net/spotify/" title="Kworb — disponible cuando EVAN debute" target="_blank" class="btn btn-ghost btn-sm">🔗 Kworb</a>
        </div>
        <div style="background:var(--bg-elevated);border:1px solid var(--border-dim);border-radius:12px;padding:0.9rem;margin-bottom:1rem;font-size:0.72rem;color:var(--text-muted);line-height:1.8">
          <strong style="color:var(--text-main)">Cómo usar:</strong> Ve a Kworb Albums → copia la tabla → pega abajo → Procesar → Guardar.<br>
          Formato esperado: <code style="background:#1a1a1a;padding:1px 5px;border-radius:3px">| Album | Streams | Daily |</code> o separado por tabs/comas.
        </div>
        <label style="font-size:0.72rem;font-weight:600;color:var(--text-dim);margin-bottom:0.4rem;display:block">Pega la tabla de Kworb aquí</label>
        <textarea id="paste-albums-area" rows="7" placeholder="| Album | Streams | Daily |&#10;| --- | --- | --- |&#10;| DIMENSION : ANSWER | 1,139,963,107 | 357,608 |&#10;..."
          style="width:100%;background:var(--input-bg);border:1px solid var(--border-dim);border-radius:12px;padding:0.8rem;color:var(--text-main);font-family:monospace;font-size:0.75rem;resize:vertical;outline:none;line-height:1.6;box-sizing:border-box"
          onfocus="this.style.borderColor='var(--primary-red)'" onblur="this.style.borderColor='var(--border-dim)'"></textarea>
        <div style="display:flex;gap:0.7rem;align-items:center;flex-wrap:wrap;margin-top:0.7rem">
          <button class="btn" onclick="processAlbumsPaste()">● Procesar</button>
          <button class="btn btn-ghost btn-sm" onclick="document.getElementById('paste-albums-area').value='';document.getElementById('paste-albums-preview').style.display='none';document.getElementById('paste-albums-status').textContent=''">✕ Limpiar</button>
          <button class="btn btn-danger btn-sm" onclick="deleteAllAlbums()" style="margin-left:auto">🗑️ Eliminar todos los álbumes</button>
          <span id="paste-albums-status" style="font-size:0.75rem;color:var(--text-muted)"></span>
        </div>
        <div id="paste-albums-preview" style="display:none;margin-top:1rem">
          <div style="font-size:0.72rem;font-weight:600;color:var(--text-dim);margin-bottom:0.5rem" id="paste-albums-preview-label"></div>
          <div style="overflow-x:auto;max-height:260px;overflow-y:auto;border:1px solid var(--border-dim);border-radius:10px">
            <table style="width:100%;border-collapse:collapse;min-width:380px">
              <thead><tr style="background:var(--bg-elevated);position:sticky;top:0">
                <th style="padding:0.5rem 0.7rem;font-size:0.6rem;color:var(--primary-red);font-family:'Orbitron',monospace;text-align:left;border-bottom:1px solid var(--border-dim)">ÁLBUM</th>
                <th style="padding:0.5rem 0.7rem;font-size:0.6rem;color:var(--primary-red);font-family:'Orbitron',monospace;text-align:right;border-bottom:1px solid var(--border-dim)">STREAMS</th>
                <th style="padding:0.5rem 0.7rem;font-size:0.6rem;color:var(--primary-red);font-family:'Orbitron',monospace;text-align:right;border-bottom:1px solid var(--border-dim)">DAILY</th>
                <th style="padding:0.5rem 0.7rem;font-size:0.6rem;color:var(--primary-red);font-family:'Orbitron',monospace;text-align:center;border-bottom:1px solid var(--border-dim)">ESTADO</th>
              </tr></thead>
              <tbody id="paste-albums-preview-body"></tbody>
            </table>
          </div>
          <div style="display:flex;gap:0.7rem;margin-top:0.8rem">
            <button class="btn" onclick="saveAlbumsPasteData()">💾 Guardar en Supabase</button>
          </div>
          <div id="paste-albums-save-status" style="font-size:0.75rem;color:var(--text-muted);margin-top:0.5rem"></div>
        </div>
      </div>
      <div class="admin-card">
        <h3>💿 Lista de álbumes</h3>
        <div id="admin-albums-list"></div>
      </div>
      <div class="admin-card">
        <details>
          <summary style="cursor:pointer;font-weight:600;color:var(--text-dim);font-size:0.85rem">📥 Precargar discografía de álbumes</summary>
          <div style="margin-top:1rem">
            <p style="font-size:0.78rem;color:var(--text-muted);margin-bottom:1rem">Carga todos los álbumes/EPs de EVAN con sus streams actuales.</p>
            <button class="btn" onclick="preloadAlbums()">📥 Cargar álbumes</button>
            <div id="preload-albums-status" style="margin-top:0.8rem;font-size:0.78rem;color:var(--text-muted)"></div>
          </div>
        </details>
      </div>
      <div class="admin-card">
        <details>
          <summary style="cursor:pointer;font-weight:600;color:var(--text-dim);font-size:0.85rem">+ Agregar álbum manualmente</summary>
          <div style="margin-top:1rem">
            <div class="form-group"><label>Título</label><input class="input" id="na-title" placeholder="ej: DIMENSION : DILEMMA"></div>
            <div class="form-group"><label>Año</label><input class="input" id="na-year" placeholder="ej: 2021"></div>
            <div class="form-group"><label>Streams totales</label><input class="input" id="na-streams" placeholder="0"></div>
            <div class="form-group"><label>Daily streams</label><input class="input" id="na-daily" placeholder="0"></div>
            <div class="form-group"><label>URL de Spotify (opcional)</label><input class="input" id="na-url" placeholder="https://open.spotify.com/album/..."></div>
            <button class="btn" style="margin-top:0.5rem" onclick="addAlbum()">Agregar álbum</button>
          </div>
        </details>
      </div>
    </div>

    <!-- Spotify Charts Admin -->
    <div class="admin-section" id="tab-charts">
      <div class="admin-card" style="border-color:rgba(29,185,84,0.25)">
        <h3 style="color:#1db954">📊 Entradas de Spotify Charts</h3>
        <p style="font-size:0.78rem;color:var(--text-muted);margin-bottom:1rem">Agrega posiciones de EVAN en los charts de Spotify. Cada entrada corresponde a una combinación de tipo + región.</p>
        <div id="admin-sp-charts-list"><div class="loading"><div class="spin"></div></div></div>
      </div>
      <div class="admin-card">
        <h3>+ Agregar entrada al chart</h3>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:0.8rem">
          <div class="form-group">
            <label>Tipo</label>
            <select class="input" id="nc-type">
              <option value="daily-songs">Daily Songs</option>
              <option value="weekly-songs">Weekly Songs</option>
              <option value="daily-artists">Daily Artists</option>
              <option value="weekly-albums">Weekly Albums</option>
            </select>
          </div>
          <div class="form-group">
            <label>Región</label>
            <select class="input" id="nc-region">
              <option value="global">🌐 Global</option>
              <option value="usa">🇺🇸 USA</option>
              <option value="uk">🇬🇧 UK</option>
              <option value="kr">🇰🇷 Korea</option>
              <option value="fr">🇫🇷 France</option>
              <option value="de">🇩🇪 Germany</option>
              <option value="br">🇧🇷 Brazil</option>
              <option value="mx">🇲🇽 Mexico</option>
              <option value="jp">🇯🇵 Japan</option>
              <option value="th">🇹🇭 Thailand</option>
              <option value="au">🇦🇺 Australia</option>
              <option value="ca">🇨🇦 Canada</option>
            </select>
          </div>
        </div>
        <div class="form-group"><label>Canción / Artista</label><input class="input" id="nc-song" placeholder="ej: Knife"></div>
        <div class="form-group"><label>Artistas (subtítulo)</label><input class="input" id="nc-artist" placeholder="ej: EVAN"></div>
        <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:0.8rem">
          <div class="form-group"><label>Posición (#)</label><input class="input" id="nc-rank" placeholder="ej: 16" type="number"></div>
          <div class="form-group"><label>Daily / Weekly Streams</label><input class="input" id="nc-daily" placeholder="ej: 2920045 (no aplica para Daily Artists)"></div>
          <div class="form-group"><label>Total Streams / Yesterday's Position</label><input class="input" id="nc-total" placeholder="ej: 274066985 · o posición de ayer para Daily Artists"></div>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:0.8rem">
          <div class="form-group"><label>Cambio (+/-)</label><input class="input" id="nc-change" placeholder="ej: +3 o -5"></div>
          <div class="form-group"><label>Dirección</label><select class="input" id="nc-dir"><option value="up">▲ Subida</option><option value="down">▼ Bajada</option><option value="new">✦ Nuevo</option></select></div>
        </div>
        <button class="btn" onclick="addSpChartEntry()">Agregar entrada</button>
      </div>
    </div>

    <!-- Apple Music -->
    <div class="admin-section" id="tab-apple">
      <div class="admin-card" style="border-color:rgba(255,45,85,0.25)">
        <h3 style="color:#ff2d55">🍎 Artist &amp; Reach</h3>
        <p style="font-size:0.78rem;color:var(--text-muted);margin-bottom:1rem">Actualiza manualmente los datos de Apple Music. Déjalo vacío para no modificarlo.</p>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:0.8rem">
          <div class="form-group">
            <label>Global Daily Peak</label>
            <input class="input" id="am-peak" placeholder="ej: Top 12" style="font-family:'Orbitron',monospace">
          </div>
          <div class="form-group">
            <label>Countries Charted</label>
            <input class="input" id="am-countries" placeholder="ej: 85+" style="font-family:'Orbitron',monospace">
          </div>
        </div>
        <button class="btn" style="background:#7a1428" onclick="saveAppleReach()">💾 Guardar Reach</button>
      </div>

      <div class="admin-card">
        <h3>🎵 Global Top 100 Songs</h3>
        <p style="font-size:0.78rem;color:var(--text-muted);margin-bottom:1rem">Agrega las canciones de EVAN que aparecen en Apple Music Charts.</p>
        <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:0.7rem;margin-bottom:0.8rem">
          <div class="form-group">
            <label>Rank</label>
            <input class="input input-mono" id="am-rank" placeholder="ej: 1" type="number" min="1">
          </div>
          <div class="form-group" style="grid-column:span 2">
            <label>Canción</label>
            <input class="input" id="am-song" placeholder="ej: Bite Me">
          </div>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:0.7rem;margin-bottom:0.8rem">
          <div class="form-group">
            <label>Artista</label>
            <input class="input" id="am-artist" placeholder="ej: EVAN">
          </div>
          <div class="form-group">
            <label>Charts (países)</label>
            <input class="input" id="am-charts" placeholder="ej: Indonesia #76">
          </div>
        </div>
        <div class="form-group" style="margin-bottom:0.8rem">
          <label>Países (#)</label>
          <input class="input input-mono" id="am-countries-count" placeholder="ej: 1" type="number" min="0">
        </div>
        <button class="btn" onclick="addAppleSong()">➕ Agregar canción</button>
        <div id="am-songs-list" style="margin-top:1rem"></div>
      </div>
    </div>

    <!-- Configuración -->
    <div class="admin-section" id="tab-config"><div class="admin-card"><h3>Textos del hero</h3><div class="form-group"><label>Tag</label><input class="input" id="cfg-hero_tag" placeholder="EVAN // SOLO ERA ..."></div><div class="form-group"><label>Texto debut</label><input class="input" id="cfg-debut_text" placeholder="Lee Heeseung · Solo Era"></div><div class="form-group"><label>🖼️ Imagen de fondo del hero (URL)</label><input class="input" id="cfg-hero_bg_url" placeholder="https://...jpg"><p style="font-size:0.72rem;color:var(--text-muted);margin-top:0.4rem">Pega la URL directa de la imagen. Recomendado: 1920x1080px o más. Puedes subir a <a href="https://imgbb.com" target="_blank" style="color:var(--primary-red)">imgbb.com</a> gratis.</p></div><div id="hero-bg-preview" style="display:none;margin-top:0.8rem;border-radius:12px;overflow:hidden;border:1px solid var(--border-dim);max-height:160px"><img id="hero-bg-img-preview" style="width:100%;height:160px;object-fit:cover;object-position:top" src="" alt="preview"></div><button class="btn btn-ghost btn-sm" style="margin-top:0.6rem" onclick="previewHeroBg()">👁️ Previsualizar</button></div><div class="admin-card"><h3>🎬 Video de fondo del hero (opcional)</h3><div class="form-group"><label>Video (URL directa .mp4)</label><input class="input" id="cfg-hero_bg_video_url" placeholder="https://.../fondo.mp4"><p style="font-size:0.72rem;color:var(--text-muted);margin-top:0.4rem">Si pones un video, se reproduce en loop y silenciado sobre la imagen de fondo. Recomendado: clip corto de ~30s, formato MP4 (H.264), peso liviano (idealmente menos de 15-20MB) para que cargue rápido. Deja el campo vacío para usar solo la imagen. Puedes alojar el archivo en <a href="https://supabase.com" target="_blank" style="color:var(--primary-red)">Supabase Storage</a>, Cloudinary o cualquier host que te dé un link directo al .mp4. ⚠️ Si usás Supabase Storage, asegurate de usar el link <strong>público</strong> (contiene <code>/object/public/</code>) y no uno <strong>firmado</strong> (contiene <code>/object/sign/</code> y <code>?token=</code>), porque los firmados expiran y el video deja de verse pasado un tiempo. Y no te olvides de tocar "Guardar configuración" abajo — "Previsualizar" solo te muestra el cambio a vos, no lo guarda para los demás.</p></div><div id="hero-bg-video-preview" style="display:none;margin-top:0.8rem;border-radius:12px;overflow:hidden;border:1px solid var(--border-dim);max-height:160px"><video id="hero-bg-video-preview-el" style="width:100%;height:160px;object-fit:cover" muted loop autoplay playsinline></video></div><button class="btn btn-ghost btn-sm" style="margin-top:0.6rem" onclick="previewHeroVideo()">👁️ Previsualizar</button><button class="btn btn-ghost btn-sm" style="margin-top:0.6rem" onclick="clearHeroVideo()">🗑️ Quitar video</button></div><div class="admin-card"><h3>🎉 Anuncio de nueva era (mensajes flotantes)</h3><div class="form-group"><label>Nombre de la era / canción</label><input class="input" id="cfg-era_announcement" placeholder="DEATH OF ME"></div><div class="form-group"><label>Color de acento (hex)</label><input class="input" id="cfg-era_color" placeholder="#60a5fa"><p style="font-size:0.72rem;color:var(--text-muted);margin-top:0.4rem">Color de las burbujas y del título grande, para que combine con la era (ej: celeste #60a5fa para "Death of Me"). Dejalo vacío para usar el rojo del sitio por defecto.</p></div><p style="font-size:0.72rem;color:var(--text-muted);margin-top:0.4rem">Cuando pongas un texto acá y guardes, a cada visitante le va a aparecer una animación con mensajes flotantes anunciando esta nueva era la primera vez que entre (no se repite en cada recarga, solo cuando el texto es distinto al que ya vieron). Dejá el campo de texto vacío y guardá para desactivarlo. Usá <button type="button" class="btn btn-ghost btn-sm" style="padding:0.2rem 0.6rem;font-size:0.65rem" onclick="previewEraAnnouncement()">👁️ Previsualizar</button> para verlo sin esperar a que se guarde.</p></div><div class="admin-card"><h3>Links sociales</h3><div class="form-group"><label>🎵 Spotify URL</label><input class="input" id="cfg-spotify_url"></div><div class="form-group"><label>▶ YouTube</label><input class="input" id="cfg-youtube_url"></div><div class="form-group"><label>🎶 Apple Music</label><input class="input" id="cfg-applemusic_url"></div><div class="form-group"><label>💬 Weverse</label><input class="input" id="cfg-weverse_url"></div><div class="form-group"><label>📸 Instagram</label><input class="input" id="cfg-instagram_url"></div><div class="form-group"><label>🐦 Twitter/X</label><input class="input" id="cfg-twitter_url"></div><button class="btn" onclick="saveConfig()">Guardar configuración</button></div></div>

    <!-- Imágenes -->
    <div class="admin-section" id="tab-images">
      <div class="admin-card">
        <h3>👤 Perfil de publicación</h3>
        <p style="font-size:0.78rem;color:var(--text-muted);margin-bottom:1rem">El nombre y avatar que aparecen en cada aviso publicado.</p>
        <div class="form-group"><label>Nombre del admin</label><input class="input" id="admin-profile-name" placeholder="ej: Admin 1"></div>
        <div class="form-group"><label>Iniciales del avatar (2 letras)</label><input class="input" id="admin-profile-initials" placeholder="ej: A1" maxlength="3"></div>
        <button class="btn" onclick="saveAdminProfile()">💾 Guardar perfil</button>
        <div id="profile-save-status" style="margin-top:0.5rem;font-size:0.75rem;color:var(--text-muted)"></div>
      </div>
      <div class="admin-card">
        <h3>📸 Publicar imagen</h3>
        <div class="form-group"><label>URL de imagen</label><input class="input" id="img-url" placeholder="https://..."></div>
        <div class="form-group"><label>Caption (opcional)</label><input class="input" id="img-caption" placeholder="ej: ¡Nueva era!"></div>
        <button class="btn" onclick="addImage()">Publicar</button>
      </div>
      <div class="admin-card">
        <h3>🖼️ Publicaciones</h3>
        <div id="admin-images-grid" class="img-grid"></div>
      </div>
    </div>
  </div>
</div>

<div class="toast" id="toast"></div>
<footer><p>Hecho con 🖤 por HEEFAMs &nbsp;·&nbsp; <span style="color:var(--gold)">EVAN Solo Era</span></p><div class="footer-links" style="display:flex; gap:1rem; justify-content:center; margin-top:0.5rem"><a href="#" id="foot-twitter" target="_blank">Twitter</a><a href="#" id="foot-instagram" target="_blank">Instagram</a><a href="#" id="foot-spotify" target="_blank">Spotify</a><a href="#" id="foot-weverse" target="_blank">Weverse</a></div><p style="margin-top:0.8rem">Fan project — no oficial. EVAN (Lee Heeseung) aún no tiene canciones solistas oficiales. Spotify y YouTube se activarán cuando debute con música propia.</p></footer>

<script>
const { createClient } = supabase;
const sb = createClient('https://zvgyomaewqnzmwhzwspc.supabase.co', 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inp2Z3lvbWFld3Fuem13aHp3c3BjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzk3ODEwNzYsImV4cCI6MjA5NTM1NzA3Nn0.kM6UtBbRmzO4jxLrzhM5qAtV3Q6TLpccUxcfRrtqcS4');
const ADMIN_PASS = '@Evan152026';
let isAdmin = false;

// Helper functions
function fmt(n){ if(!n) return '0'; if(n>=1e9) return (n/1e9).toFixed(2)+'B'; if(n>=1e6) return (n/1e6).toFixed(1)+'M'; if(n>=1e3) return (n/1e3).toFixed(0)+'K'; return n.toLocaleString(); }
function showToast(msg,err){ let t=document.getElementById('toast'); t.textContent=(err?'❌ ':'✅ ')+msg; t.classList.add('show'); setTimeout(()=>t.classList.remove('show'),2800); }
function platformIcon(p){ let m={spotify:'🎵',youtube:'▶',apple:'🎶',stationhead:'📡'}; return m[(p||'').toLowerCase()]||'🎵'; }
function platformClass(p){ let m={spotify:'pl-spotify',youtube:'pl-youtube',apple:'pl-apple',stationhead:'pl-stationhead'}; return m[(p||'').toLowerCase()]||'pl-spotify'; }
function escapeHtml(str) { if (!str) return ''; return str.replace(/[&<>]/g, function(m) { if (m==='&') return '&amp;'; if (m==='<') return '&lt;'; if (m==='>') return '&gt;'; return m; }); }

// ========== CONFIGURACIÓN ==========
async function loadConfig(){
  let {data}=await sb.from('site_config').select('*'); if(!data) return;
  let cfg={}; data.forEach(r=>cfg[r.key]=r.value);
  console.log('[EVANcharts] site_config cargado:', cfg);
  console.log('[EVANcharts] hero_bg_video_url:', JSON.stringify(cfg.hero_bg_video_url));
  if(cfg.hero_tag) document.getElementById('hero-tag').innerText=cfg.hero_tag;
  if(cfg.debut_text) document.getElementById('debut-text').innerText=cfg.debut_text;
  if(cfg.spotify_url){ document.getElementById('btn-spotify').href=cfg.spotify_url; document.getElementById('foot-spotify').href=cfg.spotify_url; }
  if(cfg.applemusic_url) document.getElementById('btn-applemusic').href=cfg.applemusic_url;
  if(cfg.youtube_url) document.getElementById('btn-youtube').href=cfg.youtube_url;
  if(cfg.weverse_url){ document.getElementById('btn-weverse').href=cfg.weverse_url; document.getElementById('foot-weverse').href=cfg.weverse_url; }
  if(cfg.instagram_url){ document.getElementById('btn-instagram').href=cfg.instagram_url; document.getElementById('foot-instagram').href=cfg.instagram_url; }
  if(cfg.twitter_url) document.getElementById('foot-twitter').href=cfg.twitter_url;
  if(cfg.hero_bg_url && cfg.hero_bg_url.trim()){
    applyHeroBg(cfg.hero_bg_url.trim());
    let inp = document.getElementById('cfg-hero_bg_url');
    if(inp) inp.value = cfg.hero_bg_url.trim();
  }
  if(cfg.hero_bg_video_url && cfg.hero_bg_video_url.trim()){
    console.log('[EVANcharts] Aplicando video de fondo:', cfg.hero_bg_video_url.trim());
    applyHeroVideo(cfg.hero_bg_video_url.trim());
    let inp = document.getElementById('cfg-hero_bg_video_url');
    if(inp) inp.value = cfg.hero_bg_video_url.trim();
  } else {
    console.log('[EVANcharts] No hay hero_bg_video_url guardado (o está vacío) — se usa solo la imagen.');
  }
  let eraInp = document.getElementById('cfg-era_announcement');
  if(eraInp) eraInp.value = cfg.era_announcement || '';
  let eraColorInp = document.getElementById('cfg-era_color');
  if(eraColorInp) eraColorInp.value = cfg.era_color || '';
  if(cfg.era_announcement && cfg.era_announcement.trim()){
    const text = cfg.era_announcement.trim();
    if(localStorage.getItem('evancharts_era_seen') !== text){
      showEraAnnouncement(text, cfg.era_color && cfg.era_color.trim());
      localStorage.setItem('evancharts_era_seen', text);
    }
  }
}
function showEraAnnouncement(text, color){
  const old = document.getElementById('era-announce-overlay');
  if(old) old.remove();
  const overlay = document.createElement('div');
  overlay.id = 'era-announce-overlay';
  if(color) overlay.style.setProperty('--era-accent', color);
  const positions = [
    {top:'12%', left:'8%', rot:-6}, {top:'20%', left:'62%', rot:5},
    {top:'34%', left:'20%', rot:4}, {top:'40%', left:'70%', rot:-4},
    {top:'62%', left:'10%', rot:6}, {top:'68%', left:'60%', rot:-5},
    {top:'80%', left:'30%', rot:3}, {top:'85%', left:'75%', rot:-6},
    {top:'50%', left:'5%', rot:-3}
  ];
  let pillsHtml = positions.map((p,i) => `<div class="era-pill" style="top:${p.top};left:${p.left};--r:${p.rot}deg;animation-delay:${(i*0.35).toFixed(2)}s;animation-duration:${(4.2+ (i%3)*0.6).toFixed(2)}s">★ ${escapeHtml(text)}</div>`).join('');
  overlay.innerHTML = `
    <div id="era-announce-close" onclick="dismissEraAnnouncement()">✕</div>
    ${pillsHtml}
    <div class="era-center">
      <div class="era-kicker">Nueva era · EVAN</div>
      <div class="era-title">${escapeHtml(text)}</div>
    </div>`;
  overlay.onclick = (e) => { dismissEraAnnouncement(); };
  document.body.appendChild(overlay);
  requestAnimationFrame(() => overlay.classList.add('show'));
  overlay._autoTimer = setTimeout(dismissEraAnnouncement, 7000);
}
function dismissEraAnnouncement(){
  const overlay = document.getElementById('era-announce-overlay');
  if(!overlay) return;
  if(overlay._autoTimer) clearTimeout(overlay._autoTimer);
  overlay.classList.remove('show'); overlay.classList.add('hide');
  setTimeout(() => overlay.remove(), 550);
}
function previewEraAnnouncement(){
  const text = (document.getElementById('cfg-era_announcement').value || '').trim();
  if(!text){ showToast('Escribe un nombre de era primero', true); return; }
  const color = (document.getElementById('cfg-era_color').value || '').trim();
  showEraAnnouncement(text, color);
}
function applyHeroBg(url){ const hero = document.querySelector('.hero'); if(hero && url) hero.style.backgroundImage = 'url(' + url + ')'; }
function previewHeroBg(){
  const url = document.getElementById('cfg-hero_bg_url').value.trim();
  if(!url){ showToast('Escribe una URL primero', true); return; }
  const preview = document.getElementById('hero-bg-preview');
  const img = document.getElementById('hero-bg-img-preview');
  img.src = url; preview.style.display = 'block'; applyHeroBg(url); showToast('Vista previa aplicada al hero');
}
function applyHeroVideo(url){
  const vid = document.getElementById('hero-bg-video');
  if(!vid) return;
  if(url){
    vid.style.display = 'block';
    vid.onerror = () => { console.error('Hero video failed to load:', url); vid.style.display = 'none'; };
    vid.src = url;
    vid.play().catch(err => console.warn('Hero video play() blocked/failed:', err));
  } else {
    vid.pause(); vid.removeAttribute('src'); vid.load(); vid.style.display = 'none';
  }
}
function previewHeroVideo(){
  const url = document.getElementById('cfg-hero_bg_video_url').value.trim();
  if(!url){ showToast('Escribe una URL primero', true); return; }
  const preview = document.getElementById('hero-bg-video-preview');
  const vidPrev = document.getElementById('hero-bg-video-preview-el');
  vidPrev.src = url; preview.style.display = 'block'; applyHeroVideo(url); showToast('Vista previa de video aplicada al hero');
}
function clearHeroVideo(){
  document.getElementById('cfg-hero_bg_video_url').value = '';
  applyHeroVideo('');
  const preview = document.getElementById('hero-bg-video-preview');
  if(preview) preview.style.display = 'none';
  showToast('Video quitado, se usará la imagen de fondo');
}

// ========== GOALS ==========
async function loadGoalsPublic(){
  let {data}=await sb.from('goals').select('*').order('sort_order');
  if(!data||!data.length){ document.getElementById('goals-list').innerHTML='<div class="empty"><p>🎯 Añade metas desde el panel admin</p></div>'; document.getElementById('charts-goals-table').innerHTML='<div class="empty"><p>Sin datos</p></div>'; return; }
  let g=data[0], pct=Math.min(100,Math.round(g.current/g.target*100));
  const renderGoalCard = (g) => {
    const p = Math.min(100, Math.round(g.current / g.target * 100));
    const cover = g.image_url
      ? `<img src="${g.image_url}" onerror="this.parentElement.innerHTML='<div class=goal-cover-placeholder>🎵</div>'" alt="">`
      : `<div class="goal-cover-placeholder">🎵</div>`;
    return `<div class="goal-card">
      <div class="goal-cover-photo">
        ${cover}
        <div class="goal-badge">Goal</div>
      </div>
      <div class="goal-body">
        <div class="goal-song">${escapeHtml(g.song)}</div>
        <div class="goal-numbers">${fmt(g.current)} <span class="goal-target-num">/ ${fmt(g.target)}</span></div>
        <div class="goal-bar-wrap"><div class="goal-bar" style="width:${p}%"></div></div>
        <div class="goal-bottom">
          <div class="goal-remaining">🚩 <span>${fmt(Math.max(0,g.target-g.current))}</span> remaining</div>
          <div class="goal-pct">${p}%</div>
        </div>
      </div>
    </div>`;
  };
  document.getElementById('goals-list').innerHTML = data.map(renderGoalCard).join('');
  document.getElementById('charts-goals-table').innerHTML=`<table class="charts-table"><thead><tr><th>#</th><th>Canción</th><th>Streams</th><th>Progreso</th></tr></thead><tbody>${data.map((g,i)=>{ let p=Math.min(100,Math.round(g.current/g.target*100)); return `<tr><td>${i+1}</td><td class="song-name">${g.song}</td><td>${fmt(g.current)}</td><td><span class="chart-badge">${p}% de ${fmt(g.target)}</span></td>`; }).join('')}</tbody></tr>`;
}
async function loadPlaylistsPublic(){
  let {data}=await sb.from('playlists').select('*').order('sort_order');
  if(!data||!data.length){ document.getElementById('home-playlists').innerHTML='<div class="empty"><p>🎵 Sin playlists</p></div>'; document.getElementById('spotify-playlists').innerHTML='<div class="empty"><p>No hay playlists de Spotify</p></div>'; return; }
  let all=data.map(p=>`<a href="${p.url||'#'}" target="_blank" class="playlist-card"><div class="playlist-icon ${platformClass(p.platform)}">${p.icon||platformIcon(p.platform)}</div><div class="playlist-info"><div class="playlist-name">${p.name}</div><div class="playlist-desc">${p.description||''}</div></div></a>`).join('');
  let spot=data.filter(p=>p.platform==='spotify').map(p=>`<a href="${p.url||'#'}" target="_blank" class="playlist-card"><div class="playlist-icon pl-spotify">${p.icon||'🎵'}</div><div class="playlist-info"><div class="playlist-name">${p.name}</div><div class="playlist-desc">${p.description||''}</div></div></a>`).join('');
  let apple=data.filter(p=>p.platform==='apple').map(p=>`<a href="${p.url||'#'}" target="_blank" class="playlist-card"><div class="playlist-icon pl-apple">${p.icon||'🎶'}</div><div class="playlist-info"><div class="playlist-name">${p.name}</div><div class="playlist-desc">${p.description||''}</div></div></a>`).join('');
  document.getElementById('home-playlists').innerHTML=`<div class="playlist-grid">${all}</div>`;
  document.getElementById('spotify-playlists').innerHTML=spot?`<div class="playlist-grid">${spot}</div>`:'<div class="empty"><p>No hay playlists de Spotify</p></div>';
  if(document.getElementById('apple-playlists')) document.getElementById('apple-playlists').innerHTML=apple?`<div class="playlist-grid">${apple}</div>`:'<div class="empty"><p>No hay playlists de Apple Music</p></div>';
}

// ========== SPOTIFY SONGS (manual) ==========
async function loadSpotifySongsPublic() {
  try {
    const { data, error } = await sb.from('spotify_songs').select('*').order('total_streams', { ascending: false });
    if (error) throw error;
    if (!data || data.length === 0) {
      document.getElementById('spotify-songs-table').innerHTML = '<div class="empty"><p>🎧 No hay canciones agregadas. El admin puede añadirlas desde el panel.</p></div>';
      return;
    }
    let html = `<table class="spotify-table"><thead><tr><th>RANK</th><th>CANCIÓN</th><th>STREAMS TOTALES</th><th style="color:#4ade80">DAILY STREAMS</th><th style="color:#4ade80">CHANGES (+/-)</th><th>LINK</th></tr></thead><tbody>`;
    data.forEach((song, i) => {
      const daily = song.daily_streams > 0 ? `<span style="color:#4ade80;font-family:'Orbitron',monospace;font-weight:700">${fmt(song.daily_streams)}</span>` : '—';
      const hasChange = song.change_streams !== null && song.change_streams !== undefined && song.change_streams !== 0;
      let changes;
      if (hasChange) {
        const isDown = song.change_streams < 0;
        const color = isDown ? '#ef3b56' : '#4ade80';
        const arrow = isDown ? '▼' : '▲';
        const sign = isDown ? '-' : '+';
        changes = `<span style="color:${color};font-weight:700">${arrow} ${sign}${fmt(Math.abs(song.change_streams))}</span>`;
      } else {
        changes = '<span style="color:var(--text-muted)">—</span>';
      }
      const link = song.spotify_url ? `<a href="${song.spotify_url}" target="_blank" style="color:var(--primary-red);text-decoration:none">🎵 Escuchar</a>` : '—';
      html += `<tr><td><span class="rank-badge">#${i+1}</span></td><td><div class="yt-video-title">${escapeHtml(song.title)}</div><div style="font-size:0.7rem;color:var(--text-muted);margin-top:2px">${fmt(song.total_streams)} total streams</div></td><td style="color:var(--primary-gold);font-family:'Orbitron',monospace">${fmt(song.total_streams)}</td><td>${daily}</td><td>${changes}</td><td>${link}</td></tr>`;
    });
    html += `</tbody></table>`;
    document.getElementById('spotify-songs-table').innerHTML = html;
  } catch(e) { console.error('Spotify songs error:', e); document.getElementById('spotify-songs-table').innerHTML = '<div class="empty"><p>⚠️ Error cargando canciones</p></div>'; }
}
async function loadAdminSpotifySongs() {
  const { data } = await sb.from('spotify_songs').select('*').order('total_streams', { ascending: false });
  if (!data || data.length === 0) { document.getElementById('admin-spotify-songs-list').innerHTML = '<p>No hay canciones aún.</p>'; return; }
  let html = '';
  for (const s of data) {
    const safeUrl = (s.spotify_url || '').replace(/"/g, '&quot;');
    const safeTitle = escapeHtml(s.title);
    html += `
    <div class="admin-list-item" id="song-row-${s.id}">
      <div class="ali-info">
        <div class="ali-name">${safeTitle}</div>
        <div class="ali-sub">${fmt(s.total_streams)} streams | +${fmt(s.daily_streams)} hoy</div>
      </div>
      <div class="ali-actions">
        <button class="btn btn-sm btn-ghost" onclick="toggleSongEdit(${s.id})">✏️ Editar</button>
        <button class="btn btn-sm btn-danger" onclick="deleteSpotifySong(${s.id})">✕</button>
      </div>
    </div>
    <div id="song-edit-${s.id}" style="display:none;background:#0d1117;border:1px solid var(--border-dim);border-top:none;border-radius:0 0 10px 10px;padding:1rem;margin-bottom:0.5rem">
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:0.6rem;margin-bottom:0.6rem">
        <div class="form-group" style="grid-column:span 2">
          <label>Nombre de la canción</label>
          <input class="input" id="se-title-${s.id}" value="${safeTitle}">
        </div>
        <div class="form-group">
          <label>Total Streams</label>
          <input class="input input-mono" id="se-streams-${s.id}" value="${s.total_streams || 0}">
        </div>
        <div class="form-group">
          <label>Daily Streams</label>
          <input class="input input-mono" id="se-daily-${s.id}" value="${s.daily_streams || 0}">
        </div>
        <div class="form-group" style="grid-column:span 2">
          <label>Change (+/-) — usa signo negativo si perdió streams, ej: -4742</label>
          <input class="input input-mono" id="se-change-${s.id}" value="${s.change_streams != null ? s.change_streams : 0}">
        </div>
        <div class="form-group" style="grid-column:span 2">
          <label>URL Spotify</label>
          <input class="input" id="se-url-${s.id}" value="${safeUrl}" placeholder="https://open.spotify.com/track/...">
        </div>
      </div>
      <div style="display:flex;gap:0.6rem">
        <button class="btn btn-sm btn-success" onclick="saveSongEdit(${s.id})">💾 Guardar</button>
        <button class="btn btn-sm btn-ghost" onclick="toggleSongEdit(${s.id})">Cancelar</button>
      </div>
    </div>`;
  }
  document.getElementById('admin-spotify-songs-list').innerHTML = html;
}

function toggleSongEdit(id) {
  const row  = document.getElementById(`song-row-${id}`);
  const edit = document.getElementById(`song-edit-${id}`);
  const isOpen = edit.style.display !== 'none';
  edit.style.display = isOpen ? 'none' : 'block';
  row.style.borderRadius = isOpen ? '' : '10px 10px 0 0';
  row.style.borderBottom = isOpen ? '' : 'none';
}

async function saveSongEdit(id) {
  const title   = document.getElementById(`se-title-${id}`).value.trim();
  const streams = parseInt(document.getElementById(`se-streams-${id}`).value.replace(/\D/g,'')) || 0;
  const daily   = parseInt(document.getElementById(`se-daily-${id}`).value.replace(/\D/g,'')) || 0;
  const changeRaw = document.getElementById(`se-change-${id}`).value.trim();
  const change_streams = changeRaw === '' ? 0 : (parseInt(changeRaw.replace(/[^\d-]/g,'')) || 0);
  const url     = document.getElementById(`se-url-${id}`).value.trim() || null;
  if (!title) { showToast('El nombre no puede estar vacío', true); return; }
  const { error } = await sb.from('spotify_songs').update({
    title, total_streams: streams, daily_streams: daily, change_streams, spotify_url: url
  }).eq('id', id);
  if (error) { showToast('Error: ' + error.message, true); return; }
  const today = new Date().toISOString().slice(0,10);
  await sb.from('spotify_snapshots').upsert(
    { song_id: id, snapshot_date: today, total_streams: streams },
    { onConflict: 'song_id,snapshot_date' }
  );
  showToast(`"${title}" guardado ✅`);
  await loadAdminSpotifySongs();
  await loadSpotifySongsPublic();
}
async function addSpotifySong() {
  const title = document.getElementById('ns-title').value.trim();
  const total_streams = parseInt(document.getElementById('ns-streams').value.replace(/\D/g,'')) || 0;
  const spotify_url = document.getElementById('ns-url').value.trim();
  if (!title) { showToast('Escribe el título', true); return; }
  const { data: exist } = await sb.from('spotify_songs').select('sort_order').order('sort_order', { ascending: false }).limit(1);
  const sort_order = (exist && exist[0]) ? exist[0].sort_order + 1 : 1;
  const { data: inserted, error } = await sb.from('spotify_songs').insert({ title, total_streams, spotify_url, sort_order, daily_streams: 0 }).select();
  if (error) { showToast('Error al guardar', true); return; }
  const newId = inserted[0].id;
  const today = new Date().toISOString().slice(0,10);
  await sb.from('spotify_snapshots').insert({ song_id: newId, snapshot_date: today, total_streams });
  showToast('Canción agregada ✅');
  document.getElementById('ns-title').value = '';
  document.getElementById('ns-streams').value = '';
  document.getElementById('ns-url').value = '';
  await loadAdminSpotifySongs();
  await loadSpotifySongsPublic();
}
async function deleteSpotifySong(id) {
  if (!confirm('¿Eliminar esta canción?')) return;
  await sb.from('spotify_songs').delete().eq('id', id);
  await sb.from('spotify_snapshots').delete().eq('song_id', id);
  showToast('Canción eliminada');
  await loadAdminSpotifySongs();
  await loadSpotifySongsPublic();
}

// ========== BULK UPDATE & DISCOGRAPHY ==========
const ENHYPEN_DISCOGRAPHY = [
  { title: "Bite Me", streams: 503978514, daily: 268528, spotify_url: "https://open.spotify.com/track/7mpdNiaQvygj2rHoxkzMfa" },
  { title: "FEVER", streams: 472645309, daily: 148632, spotify_url: "https://open.spotify.com/track/7FfDVXuFy0RB86yEH0p9hP" },
  { title: "Drunk-Dazed", streams: 440333695, daily: 188968, spotify_url: "https://open.spotify.com/track/1wcr8DjnN59Awev8nnKpQ4" },
  { title: "Polaroid Love", streams: 380622064, daily: 103241, spotify_url: "https://open.spotify.com/track/4IRAHnkMlMrAGDIMQ2wFnz" },
  { title: "Given-Taken", streams: 267049673, daily: 91124, spotify_url: "https://open.spotify.com/track/1RxXIuGnfb9YvPZbUPFmGG" },
  { title: "XO (Only If You Say Yes)", streams: 213377949, daily: 202264, spotify_url: "https://open.spotify.com/track/6F09Gf0jqHdsWS2IYUKvaB" },
  { title: "Sweet Venom", streams: 201282139, daily: 92857, spotify_url: "https://open.spotify.com/track/0YDJpkTxEYuTKz2tNDL0eW" },
  { title: "SHOUT OUT", streams: 167147676, daily: 92335, spotify_url: "https://open.spotify.com/track/5Z2DNRAhs6r4VdINVkRhYY" },
  { title: "Future Perfect (Pass the MIC)", streams: 166316375, daily: 44768, spotify_url: "https://open.spotify.com/track/4iJLPqZaalWbSdtsDOEMHG" },
  { title: "No Doubt", streams: 164606792, daily: 159897, spotify_url: "https://open.spotify.com/track/44hqFxUWsADWewEJELnncj" },
  { title: "Tamed-Dashed", streams: 162560860, daily: 41258, spotify_url: "https://open.spotify.com/track/1uHvYJOAytUeYJAkDrpqnj" },
  { title: "Moonstruck", streams: 158794291, daily: 196332, spotify_url: "https://open.spotify.com/track/2yFPGQLRHMJbMbDDaPlN2A" },
  { title: "Brought The Heat Back", streams: 144285989, daily: 133386, spotify_url: "https://open.spotify.com/track/1gqvV5gDNcJlNMoHo5gJBX" },
  { title: "Blessed-Cursed", streams: 141916230, daily: 37935, spotify_url: "https://open.spotify.com/track/3X6n6rHpS7BZv7Y3yEjbBa" },
  { title: "Go Big or Go Home", streams: 140918756, daily: 76249, spotify_url: "https://open.spotify.com/track/3b4gWGVlmC2MqHELQwjlaE" },
  { title: "Fatal Trouble", streams: 113651498, daily: 121027, spotify_url: "https://open.spotify.com/track/53sDnvNZNq2vvQmD9SxvWL" },
  { title: "CRIMINAL LOVE", streams: 112106012, daily: 80352, spotify_url: "https://open.spotify.com/track/6XmPQvSbS4LT84f79wKcyt" },
  { title: "Bad Desire (With or Without You)", streams: 106361693, daily: 101969, spotify_url: "https://open.spotify.com/track/38pEX03GFZ0PfFDaOrpKOM" },
  { title: "Let Me In (20 CUBE)", streams: 104082761, daily: 25755, spotify_url: "https://open.spotify.com/track/1pUr3P0Mq3LxqMSTbMJBSS" },
  { title: "Sacrifice (Eat Me Up)", streams: 100514641, daily: 47842, spotify_url: "https://open.spotify.com/track/4RhUxLekRV0YVtqSFlxfJI" },
  { title: "ParadoXXX Invasion", streams: 94817374, daily: 29327, spotify_url: "https://open.spotify.com/track/0X1mR6gunFBMWqKR0k78QX" },
  { title: "Bills", streams: 92198966, daily: 49984, spotify_url: "https://open.spotify.com/track/6mzIMGZYaNJKKXUrjsLHOW" },
  { title: "10 Months", streams: 90472383, daily: 23939, spotify_url: "https://open.spotify.com/track/4W7sXyPpTMLZuWMO6UTBPN" },
  { title: "Not For Sale", streams: 88800069, daily: 23648, spotify_url: "https://open.spotify.com/track/1HaB7yPBTZLkY0GVNXjl0x" },
  { title: "Chaconne", streams: 88422105, daily: 49373, spotify_url: "https://open.spotify.com/track/6cVD7akJMxoMTYyP5kJHKr" },
  { title: "Just A Little Bit", streams: 82945825, daily: 22722, spotify_url: "https://open.spotify.com/track/5NKDVZpA8I1DlxKfm1HKZM" },
  { title: "Upper Side Dreamin’", streams: 77059258, daily: 23312, spotify_url: "" },
  { title: "Still Monster", streams: 70651540, daily: 46843, spotify_url: "https://open.spotify.com/track/5sn9jfiBUvJAlxlDNwQZEV" },
  { title: "Flicker", streams: 70528060, daily: 18647, spotify_url: "https://open.spotify.com/track/5DjFrXNqFBSQkCdUB9Tccd" },
  { title: "Loose", streams: 64133129, daily: 53889, spotify_url: "https://open.spotify.com/track/03HHGNtjpS5K6fRcwlX3Sx" },
  { title: "Outside", streams: 63226138, daily: 93278, spotify_url: "https://open.spotify.com/track/0IXWLMBZeK33mOb044Sxu6" },
  { title: "TFW (That Feeling When)", streams: 62607840, daily: 19674, spotify_url: "https://open.spotify.com/track/3MOqkT1nKNBVkK5lIPfFZt" },
  { title: "Knife", streams: 61027532, daily: 237505, spotify_url: "https://open.spotify.com/track/0TKCUjfV3YGuY99MxCKM5w" },
  { title: "One In A Billion", streams: 59644950, daily: 31670, spotify_url: "https://open.spotify.com/track/4mWFUuIRTFpYlhODGeB9Ew" },
  { title: "Mixed Up", streams: 59144005, daily: 16764, spotify_url: "https://open.spotify.com/track/2H8YRkSqLB2jHnEFJNAOVo" },
  { title: "Attention, please!", streams: 57816494, daily: 15365, spotify_url: "https://open.spotify.com/track/5cIJFgQtBH3tXGFSPQcRBg" },
  { title: "Bad Desire (With or Without You) (English Ver.)", streams: 57473345, daily: 80398, spotify_url: "https://open.spotify.com/track/4N8IFzGERNBWoQyzYVYEoJ" },
  { title: "Blockbuster feat. YEONJUN of TOMORROW X TOGETHER", streams: 56286001, daily: 21671, spotify_url: "" },
  { title: "Fate", streams: 56199016, daily: 47102, spotify_url: "" },
  { title: "Your Eyes Only", streams: 54062775, daily: 44231, spotify_url: "" },
  { title: "Daydream", streams: 54047017, daily: 49211, spotify_url: "" },
  { title: "Highway 1009", streams: 51228757, daily: 54660, spotify_url: "https://open.spotify.com/track/3HpJW5tQE2jxN3VQkL0ADa" },
  { title: "I NEED U - Spotify Singles", streams: 48996357, daily: 14306, spotify_url: "" },
  { title: "Hey Tayo (Tayo Opening Theme Song)", streams: 47829005, daily: 12584, spotify_url: "" },
  { title: "Helium", streams: 45134444, daily: 112702, spotify_url: "https://open.spotify.com/track/48jRAABr5TxjWHvoNWClGG" },
  { title: "BLOSSOM", streams: 44781854, daily: 31193, spotify_url: "" },
  { title: "What Makes You Beautiful", streams: 44184665, daily: 18960, spotify_url: "" },
  { title: "Paranormal", streams: 43440864, daily: 43001, spotify_url: "" },
  { title: "Sweet Venom (English Ver.)", streams: 43236299, daily: 18294, spotify_url: "" },
  { title: "Teeth", streams: 43118369, daily: 33718, spotify_url: "" },
  { title: "Hundred Broken Hearts", streams: 40774201, daily: 33496, spotify_url: "" },
  { title: "Orange Flower (You Complete Me)", streams: 40108080, daily: 24115, spotify_url: "https://open.spotify.com/track/3E6KyQ93bqlBfRZFRZCTwp" },
  { title: "I Need The Light", streams: 39827900, daily: 16904, spotify_url: "" },
  { title: "Blind", streams: 39169541, daily: 31229, spotify_url: "" },
  { title: "Karma", streams: 36931990, daily: 17204, spotify_url: "" },
  { title: "Always", streams: 36900064, daily: 11141, spotify_url: "" },
  { title: "Big Girls Don't Cry", streams: 36469546, daily: 145923, spotify_url: "https://open.spotify.com/track/0NsmpxrKw7ZWGKjnWVJtm5" },
  { title: "Foreshadow", streams: 35755687, daily: 13504, spotify_url: "https://open.spotify.com/track/4EL81sOPVVPBCd0g8iRSAw" },
  { title: "Lucifer", streams: 35176179, daily: 25708, spotify_url: "" },
  { title: "Too Close", streams: 33299199, daily: 39607, spotify_url: "https://open.spotify.com/track/6gezrIScVLEnV1szuZxhvA" },
  { title: "Royalty", streams: 33097637, daily: 28260, spotify_url: "" },
  { title: "Forget Me Not", streams: 31141132, daily: 8367, spotify_url: "" },
  { title: "Scream", streams: 31109973, daily: 23049, spotify_url: "" },
  { title: "No Way Back (Feat. So!YoON!)", streams: 30642694, daily: 116142, spotify_url: "https://open.spotify.com/track/6u7J7OH56kiN2A7Pfv5D0i" },
  { title: "XO (Only If You Say Yes) (English Ver.) Feat. JVKE", streams: 28723393, daily: 20960, spotify_url: "" },
  { title: "One and Only", streams: 27440804, daily: 11180, spotify_url: "" },
  { title: "Billy Poco", streams: 21193056, daily: 6412, spotify_url: "" },
  { title: "* Confessions", streams: 21140918, daily: 15979, spotify_url: "" },
  { title: "Flashover", streams: 19972809, daily: 23214, spotify_url: "https://open.spotify.com/track/4uj7LoS14txqhRylMR4Fj1" },
  { title: "Lost Island", streams: 19582186, daily: 64140, spotify_url: "https://open.spotify.com/track/53cWZA9OROeiYu1vHh4f4i" },
  { title: "Demons", streams: 19423177, daily: 27763, spotify_url: "https://open.spotify.com/track/30oEeJE3bxNMODjscH9Scl" },
  { title: "Echoes", streams: 19388847, daily: 37174, spotify_url: "https://open.spotify.com/track/5psV1mOAaT9WuORcjaMgQv" },
  { title: "Shine On Me", streams: 18525719, daily: 29248, spotify_url: "https://open.spotify.com/track/0sz9rW8qpeRm5AAUYEIZWL" },
  { title: "Stealer", streams: 18299698, daily: 63763, spotify_url: "https://open.spotify.com/track/5EK13IYnr1NKdWDZhLDlBl" },
  { title: "Mortal", streams: 17693612, daily: 9595, spotify_url: "" },
  { title: "Intro : Walk the Line", streams: 17292382, daily: 3406, spotify_url: "" },
  { title: "Intro : The Invitation", streams: 17005330, daily: 3612, spotify_url: "" },
  { title: "Sleep Tight", streams: 16439086, daily: 58633, spotify_url: "https://open.spotify.com/track/55Uw5HbVMjQg4iFJdKuOUC" },
  { title: "Home", streams: 16065792, daily: 13111, spotify_url: "" },
  { title: "Interlude : Question", streams: 15912900, daily: 3514, spotify_url: "" },
  { title: "Intro : Whiteout", streams: 15278153, daily: 3478, spotify_url: "" },
  { title: "WALK THE LINE", streams: 15050654, daily: 4280, spotify_url: "" },
  { title: "Given-Taken - Japanese Ver.", streams: 14215594, daily: 3228, spotify_url: "" },
  { title: "Outro : Cross the Line", streams: 13816332, daily: 2930, spotify_url: "" },
  { title: "Loose (Korean Ver.)", streams: 13501766, daily: 17099, spotify_url: "https://open.spotify.com/track/1hZqz3w2FFdQekLLLFEa0T" },
  { title: "Make the change", streams: 12969955, daily: 5940, spotify_url: "" },
  { title: "Drunk-Dazed - Japanese Ver.", streams: 12779014, daily: 5181, spotify_url: "" },
  { title: "Brought The Heat Back (Feat. Ava Max)", streams: 12504802, daily: 9449, spotify_url: "" },
  { title: "Tamed-Dashed - Japanese Ver.", streams: 11932251, daily: 3093, spotify_url: "" },
  { title: "Outro : The Wormhole", streams: 11472914, daily: 2403, spotify_url: "" },
  { title: "Let Me In (20 CUBE) - Japanese Ver.", streams: 10885279, daily: 2755, spotify_url: "" },
  { title: "Sweet Venom (feat. Bella Poarch)", streams: 10695278, daily: 3556, spotify_url: "" },
  { title: "Bite Me - Japanese Ver.", streams: 9823912, daily: 8971, spotify_url: "" },
  { title: "ZERO MOMENT (Sung by HEESEUNG, JAY, JAKE) (Prod. tearliner)", streams: 9301399, daily: 3532, spotify_url: "" },
  { title: "Outro : Day 2", streams: 8997597, daily: 2286, spotify_url: "" },
  { title: "Keep Swimmin' Through", streams: 8082086, daily: 3088, spotify_url: "" },
  { title: "Sweet Venom (Seeb Remix)", streams: 8010928, daily: 3007, spotify_url: "" },
  { title: "A Kind Of Magic - Coke Studio Session", streams: 7483387, daily: 2451, spotify_url: "" },
  { title: "Knife (SUNGHOON Ver.)", streams: 7112914, daily: 22727, spotify_url: "" },
  { title: "XO (Only If You Say Yes) (English Ver.)", streams: 6614717, daily: 6387, spotify_url: "" },
  { title: "Future Perfect (Pass the MIC) - Japanese Ver.", streams: 5406319, daily: 1895, spotify_url: "" },
  { title: "Bills - Japanese Ver.", streams: 5340911, daily: 2157, spotify_url: "" },
  { title: "No Doubt - Japanese Ver. / Digital Only", streams: 5269277, daily: 5835, spotify_url: "" },
  { title: "XO (Only If You Say Yes) - Japanese Ver. / Digital Only", streams: 4125144, daily: 5480, spotify_url: "" },
  { title: "One and Only - PIKA PIKA Remix", streams: 3990198, daily: 5053, spotify_url: "" },
  { title: "Blessed-Cursed - Japanese Ver.", streams: 3806687, daily: 1563, spotify_url: "" },
  { title: "Knife (English Ver.)", streams: 3711319, daily: 12772, spotify_url: "" },
  { title: "Bad Desire (With or Without You) - Japanese Ver.", streams: 3456927, daily: 7108, spotify_url: "" },
  { title: "도망자들 (Los Fugitivos)", streams: 2648339, daily: 7592, spotify_url: "https://open.spotify.com/track/40xgp3gReYzOtWdtsuzAhl" },
  { title: "사건의 발단 (El Inicio del Caso)", streams: 2611472, daily: 7734, spotify_url: "https://open.spotify.com/track/4Sw8Gb9uCFHgRGZjSvNDKz" },
  { title: "사건의 너머 (Más Allá del Caso)", streams: 2543897, daily: 7684, spotify_url: "https://open.spotify.com/track/79GygScCfCwPTGj3bIggLN" },
  { title: "우리가 찾던 목소리 (La Voz que Buscábamos)", streams: 2480510, daily: 7221, spotify_url: "https://open.spotify.com/track/4kUACOhtIz6muVF2wWgsdf" },
  { title: "Knife (HEESEUNG Ver.)", streams: 2387863, daily: 7560, spotify_url: "" },
  { title: "One In A Billion (Japanese Ver.)", streams: 2370226, daily: 11605, spotify_url: "https://open.spotify.com/track/7MvYfssQOnBxMd0T5T8WgG" },
  { title: "* Confessions - Spring Break Edit", streams: 2290488, daily: 4870, spotify_url: "" },
  { title: "Keep Swimmin' Through x Baby Shark (Dance Remix)", streams: 2157851, daily: 1014, spotify_url: "" },
  { title: "목격자 (Testigo)", streams: 1912369, daily: 5534, spotify_url: "https://open.spotify.com/track/3nzJHcaEMXUUAP7A0YGbJ0" },
  { title: "Knife (SUNOO Ver.)", streams: 1900306, daily: 8024, spotify_url: "" },
  { title: "Hey Tayo (Tayo Opening Theme Song) (Inst.)", streams: 1766161, daily: 511, spotify_url: "" },
  { title: "Knife (NI-KI Ver.)", streams: 1525341, daily: 5166, spotify_url: "" },
  { title: "Knife (JAY Ver.)", streams: 1462902, daily: 5194, spotify_url: "" },
  { title: "Knife (JAKE Ver.)", streams: 1445965, daily: 4741, spotify_url: "" },
  { title: "Knife (JUNGWON Ver.)", streams: 1403232, daily: 5993, spotify_url: "" },
  { title: "I Need The Light - Instrumental", streams: 1290588, daily: 512, spotify_url: "" },
  { title: "XO (Only If You Say Yes) (English Ver.) (Drive Night Remix)", streams: 1189358, daily: 687, spotify_url: "" },
  { title: "Billy Poco (Inst.)", streams: 1049681, daily: 240, spotify_url: "" },
  { title: "The Beginning", streams: 918970, daily: 2564, spotify_url: "" },
  { title: "* Confessions - Run Club Remix", streams: 891888, daily: 4461, spotify_url: "" },
  { title: "The Beyond", streams: 868703, daily: 2577, spotify_url: "" },
  { title: "What Makes You Beautiful - Instrumental", streams: 857704, daily: 378, spotify_url: "" },
  { title: "The Fugitives", streams: 836219, daily: 2188, spotify_url: "" },
  { title: "ZERO MOMENT (Instrumental)", streams: 829348, daily: 276, spotify_url: "" },
  { title: "XO (Only If You Say Yes) (English Ver.) (Club Night Remix)", streams: 822674, daily: 393, spotify_url: "" },
  { title: "XO (Only If You Say Yes) (English Ver.) (Slowed + Reverb Ver.)", streams: 793267, daily: 807, spotify_url: "" },
  { title: "The Voice", streams: 741607, daily: 2068, spotify_url: "" },
  { title: "Voice Note (HEESEUNG Ver.)", streams: 739718, daily: 2246, spotify_url: "" },
  { title: "XO (Only If You Say Yes) (English Ver.) (California Night Remix)", streams: 671861, daily: 538, spotify_url: "" },
  { title: "Witnesses", streams: 626200, daily: 1779, spotify_url: "" },
  { title: "XO (Only If You Say Yes) (English Ver.) (Sped Up Ver.)", streams: 528611, daily: 387, spotify_url: "" },
  { title: "Voice Note (SUNOO Ver.)", streams: 491903, daily: 2834, spotify_url: "" },
  { title: "Demons - Instrumental", streams: 421174, daily: 378, spotify_url: "" },
  { title: "Voice Note (NI-KI Ver.)", streams: 347093, daily: 803, spotify_url: "" },
  { title: "Voice Note (JUNGWON Ver.)", streams: 340275, daily: 1688, spotify_url: "" },
  { title: "Voice Note (JAKE Ver.)", streams: 311830, daily: 774, spotify_url: "" },
  { title: "Voice Note (SUNGHOON Ver.)", streams: 300530, daily: 863, spotify_url: "" },
  { title: "* It's My Ocean x Baby Shark (Finale) Mashup", streams: 276980, daily: 144, spotify_url: "" },
  { title: "Voice Note (JAY Ver.)", streams: 253892, daily: 824, spotify_url: "" },
  { title: "* Baby Shark (Finale)", streams: 237329, daily: 115, spotify_url: "" },
  { title: "逃亡者たち (Fugitivos - Jap.)", streams: 175978, daily: 381, spotify_url: "" },
  { title: "事件の発端 (El Inicio del Caso - Jap.)", streams: 169567, daily: 411, spotify_url: "" },
  { title: "探していた声 (La Voz que Buscábamos - Jap.)", streams: 133019, daily: 675, spotify_url: "" },
  { title: "事件的彼端 (Más Allá del Caso - Chino)", streams: 127829, daily: 298, spotify_url: "" },
  { title: "事件の彼方 (Más Allá del Caso - Jap.)", streams: 112217, daily: 346, spotify_url: "" },
  { title: "事件的起源 (El Inicio del Caso - Chino)", streams: 106457, daily: 220, spotify_url: "" },
]; // { id, title, total_streams, daily_streams }

async function preloadDiscography() {
  const statusEl = document.getElementById('preload-status');
  statusEl.textContent = '⏳ Cargando discografía...';
  const { data: existing } = await sb.from('spotify_songs').select('title');
  const existingTitles = (existing || []).map(s => s.title.toLowerCase().trim());
  const { data: sortData } = await sb.from('spotify_songs').select('sort_order').order('sort_order', { ascending: false }).limit(1);
  let maxSort = (sortData && sortData[0]) ? sortData[0].sort_order : 0;
  const today = new Date().toISOString().slice(0, 10);
  let inserted = 0, skipped = 0, errors = 0;
  for (const song of ENHYPEN_DISCOGRAPHY) {
    if (existingTitles.includes(song.title.toLowerCase().trim())) { skipped++; continue; }
    const { data: ins, error } = await sb.from('spotify_songs').insert({
      title: song.title, total_streams: song.streams || 0, daily_streams: song.daily || 0,
      spotify_url: song.spotify_url || null, sort_order: ++maxSort
    }).select('id').single();
    if (!error && ins) {
      await sb.from('spotify_snapshots').upsert({ song_id: ins.id, snapshot_date: today, total_streams: song.streams || 0 }, { onConflict: 'song_id,snapshot_date' });
      inserted++;
    } else { errors++; }
  }
  statusEl.textContent = `✅ ${inserted} canciones cargadas · ${skipped} ya existían${errors ? ` · ${errors} errores` : ''}`;
  showToast(`Discografía cargada: ${inserted} canciones ✅`);
  await loadAdminSpotifySongs();
  await loadSpotifySongsPublic();
}

// ========== PASTE TABLE ==========
let pasteParsed = [];

function processPaste() {
  const text = document.getElementById('paste-area').value.trim();
  const statusEl = document.getElementById('paste-status');
  if (!text) { statusEl.textContent = '⚠️ Pega el contenido primero.'; return; }

  const lines = text.split('\n').map(l => l.trim()).filter(l => l);
  pasteParsed = [];

  for (const line of lines) {
    let cols;
    if (line.includes('|')) {
      cols = line.split('|').map(c => c.trim()).filter(c => c);
    } else if (line.includes('\t')) {
      cols = line.split('\t').map(c => c.trim());
    } else {
      cols = line.split(/\s{2,}/).map(c => c.trim());
    }
    if (cols.length < 2) continue;

    const title = cols[0].replace(/^\d+\.?\s*/, '').trim();
    if (!title || title === '---' || title === '-' ||
        title.toLowerCase().includes('song title') ||
        title.toLowerCase().includes('title') ||
        title.toLowerCase() === 'song') continue;

    // Parse numbers — remove all non-digit chars except nothing
    const streamsRaw = (cols[1] || '').replace(/[^0-9]/g, '');
    const dailyRaw   = (cols[2] || '').replace(/[^0-9]/g, '');
    const streams = parseInt(streamsRaw) || 0;
    const daily   = parseInt(dailyRaw)   || 0;

    if (title && streams > 0) pasteParsed.push({ title, streams, daily });
  }

  if (!pasteParsed.length) {
    statusEl.textContent = '⚠️ No se pudo parsear. Formato esperado: Canción | Streams | Daily';
    document.getElementById('paste-preview').style.display = 'none';
    document.getElementById('paste-save-btn').style.display = 'none';
    return;
  }
  // Show save button immediately — don't wait for preview
  document.getElementById('paste-save-btn').style.display = 'inline-flex';
  statusEl.textContent = `✅ ${pasteParsed.length} canciones listas — presiona 💾 Guardar`;
  renderPastePreview(); // preview is optional, runs in background
}

function clearPaste() {
  document.getElementById('paste-area').value = '';
  document.getElementById('paste-preview').style.display = 'none';
  document.getElementById('paste-save-btn').style.display = 'none';
  document.getElementById('paste-status').textContent = '';
  document.getElementById('paste-save-status').textContent = '';
  pasteParsed = [];
}

async function renderPastePreview() {
  const { data: dbSongs } = await sb.from('spotify_songs').select('id,title,total_streams');
  const noMatch = pasteParsed.filter(r => !dbSongs?.find(s => titlesMatch(s.title, r.title))).length;
  document.getElementById('paste-preview-label').textContent = `${pasteParsed.length} canciones · ${pasteParsed.length - noMatch} actualizarán · ${noMatch} serán nuevas`;
  document.getElementById('paste-preview-body').innerHTML = pasteParsed.map(r => {
    const match = dbSongs?.find(s => titlesMatch(s.title, r.title));
    const st = match
      ? `<span style="color:#1db954;font-size:0.72rem">✅ match</span>`
      : `<span style="color:var(--primary-red);font-size:0.72rem">⚠ nuevo</span>`;
    return `<tr style="border-bottom:1px solid rgba(255,255,255,0.04)">
      <td style="padding:0.4rem 0.7rem;font-size:0.77rem;color:var(--text-main)">${escapeHtml(r.title)}</td>
      <td style="padding:0.4rem 0.7rem;font-family:'Orbitron',monospace;font-size:0.72rem;color:#1db954;text-align:right">${fmtCompact(r.streams)}</td>
      <td style="padding:0.4rem 0.7rem;font-family:'Orbitron',monospace;font-size:0.72rem;color:var(--text-dim);text-align:right">${r.daily ? '+' + fmtCompact(r.daily) : '—'}</td>
      <td style="padding:0.4rem 0.7rem;text-align:center">${st}</td>
    </tr>`;
  }).join('');
  document.getElementById('paste-preview').style.display = 'block';
}

async function savePasteData() {
  if (!pasteParsed.length) return;
  const statusEl = document.getElementById('paste-save-status');
  const btn = document.getElementById('paste-save-btn');
  btn.disabled = true; btn.textContent = '⏳ Guardando...';
  statusEl.textContent = 'Cargando lista...';

  // 1. Load all DB songs once
  const { data: dbSongs, error: dbErr } = await sb.from('spotify_songs').select('id,title,sort_order');
  if (dbErr) {
    statusEl.textContent = '❌ ' + dbErr.message;
    btn.disabled = false; btn.textContent = '💾 Guardar en Supabase';
    return;
  }

  let maxSort = dbSongs?.reduce((m, s) => Math.max(m, s.sort_order || 0), 0) || 0;

  // 2. Match all songs in memory
  const toUpdate = [];
  const toInsert = [];
  for (const r of pasteParsed) {
    const match = dbSongs?.find(s => s.title.toLowerCase().trim() === r.title.toLowerCase().trim())
      || dbSongs?.find(s => titlesMatch(s.title, r.title));
    if (match) toUpdate.push({ id: match.id, total_streams: r.streams, daily_streams: r.daily });
    else toInsert.push({ title: r.title, total_streams: r.streams, daily_streams: r.daily, spotify_url: null, sort_order: ++maxSort });
  }

  statusEl.textContent = `Guardando ${toUpdate.length} canciones en lotes...`;

  // 3. Batch upsert updates in chunks of 50 parallel
  let updated = 0, errors = 0;
  const chunkSize = 50;
  for (let i = 0; i < toUpdate.length; i += chunkSize) {
    const chunk = toUpdate.slice(i, i + chunkSize);
    const results = await Promise.all(
      chunk.map(r => sb.from('spotify_songs').update({ total_streams: r.total_streams, daily_streams: r.daily_streams }).eq('id', r.id))
    );
    results.forEach(({ error }) => error ? errors++ : updated++);
    statusEl.textContent = `Guardando... ${Math.min(i + chunkSize, toUpdate.length)}/${toUpdate.length}`;
  }

  // 4. Batch insert new songs
  let inserted = 0;
  if (toInsert.length) {
    const { data: ins, error } = await sb.from('spotify_songs').insert(toInsert).select('id');
    if (!error) inserted = ins?.length || toInsert.length;
    else errors += toInsert.length;
  }

  const msg = `✅ ${updated} actualizadas · ${inserted} nuevas${errors ? ` · ${errors} errores` : ''}`;
  statusEl.textContent = msg;
  showToast(msg);
  btn.disabled = false; btn.textContent = '💾 Guardar en Supabase';
  await loadAdminSpotifySongs();
  await loadSpotifySongsPublic();
}

async function deleteAllSongs() {
  if (!confirm('⚠️ ¿Eliminar TODAS las canciones? Esto no se puede deshacer.')) return;
  if (!confirm('¿Segura? Se borrarán canciones y sus snapshots históricos.')) return;
  const statusEl = document.getElementById('paste-status');
  statusEl.textContent = '⏳ Eliminando...';
  const { data } = await sb.from('spotify_songs').select('id');
  if (data && data.length) {
    const ids = data.map(s => s.id);
    await sb.from('spotify_snapshots').delete().in('song_id', ids);
    await sb.from('spotify_songs').delete().in('id', ids);
  }
  pasteParsed = [];
  statusEl.textContent = '🗑️ Todas las canciones eliminadas.';
  document.getElementById('paste-preview').style.display = 'none';
  document.getElementById('paste-area').value = '';
  document.getElementById('paste-status').textContent = '';
  showToast('Canciones eliminadas');
  await loadAdminSpotifySongs(); await loadSpotifySongsPublic();
}

// ========== ALBUMS PASTE TABLE ==========
let pasteAlbumsParsed = [];

function processAlbumsPaste() {
  const text = document.getElementById('paste-albums-area').value.trim();
  const statusEl = document.getElementById('paste-albums-status');
  if (!text) { statusEl.textContent = '⚠️ Pega el contenido primero.'; return; }

  const lines = text.split('\n').map(l => l.trim()).filter(l => l);
  pasteAlbumsParsed = [];

  for (const line of lines) {
    let cols;
    if (line.includes('|')) {
      cols = line.split('|').map(c => c.trim()).filter(c => c);
    } else if (line.includes('\t')) {
      cols = line.split('\t').map(c => c.trim());
    } else if (line.includes(';')) {
      cols = line.split(';').map(c => c.trim());
    } else {
      cols = line.split(',').map(c => c.trim());
    }
    if (cols.length < 2) continue;
    const title = cols[0];
    if (!title || title === '---' || title.toLowerCase().includes('album') || title === '-') continue;
    const streamsRaw = (cols[1] || '0').replace(/,/g, '').replace(/[^0-9]/g, '');
    const dailyRaw  = (cols[2] || '0').replace(/,/g, '').replace(/[^0-9]/g, '');
    const streams = parseInt(streamsRaw) || 0;
    const daily   = parseInt(dailyRaw)   || 0;
    if (title && streams > 0) pasteAlbumsParsed.push({ title, streams, daily });
  }

  if (!pasteAlbumsParsed.length) {
    statusEl.textContent = '⚠️ No se pudieron parsear álbumes. Verifica el formato.';
    document.getElementById('paste-albums-preview').style.display = 'none';
    return;
  }
  statusEl.textContent = `✅ ${pasteAlbumsParsed.length} álbumes detectados`;
  renderAlbumsPastePreview();
}

async function renderAlbumsPastePreview() {
  const { data: dbAlbums } = await sb.from('spotify_albums').select('id,title,total_streams');
  const noMatch = pasteAlbumsParsed.filter(r => !dbAlbums?.find(a => titlesMatch(a.title, r.title))).length;
  document.getElementById('paste-albums-preview-label').textContent = `${pasteAlbumsParsed.length} álbumes · ${pasteAlbumsParsed.length - noMatch} actualizarán · ${noMatch} serán nuevos`;
  document.getElementById('paste-albums-preview-body').innerHTML = pasteAlbumsParsed.map(r => {
    const match = dbAlbums?.find(a => titlesMatch(a.title, r.title));
    const st = match
      ? `<span style="color:#1db954;font-size:0.72rem">✅ match</span>`
      : `<span style="color:var(--primary-red);font-size:0.72rem">⚠ nuevo</span>`;
    return `<tr style="border-bottom:1px solid rgba(255,255,255,0.04)">
      <td style="padding:0.4rem 0.7rem;font-size:0.77rem;color:var(--text-main)">${escapeHtml(r.title)}</td>
      <td style="padding:0.4rem 0.7rem;font-family:'Orbitron',monospace;font-size:0.72rem;color:#1db954;text-align:right">${fmtCompact(r.streams)}</td>
      <td style="padding:0.4rem 0.7rem;font-family:'Orbitron',monospace;font-size:0.72rem;color:var(--text-dim);text-align:right">${r.daily ? '+' + fmtCompact(r.daily) : '—'}</td>
      <td style="padding:0.4rem 0.7rem;text-align:center">${st}</td>
    </tr>`;
  }).join('');
  document.getElementById('paste-albums-preview').style.display = 'block';
}

async function saveAlbumsPasteData() {
  if (!pasteAlbumsParsed.length) return;
  const statusEl = document.getElementById('paste-albums-save-status');
  statusEl.textContent = '⏳ Guardando...';
  const { data: dbAlbums } = await sb.from('spotify_albums').select('id,title,total_streams');
  const today = new Date().toISOString().slice(0, 10);
  let updated = 0, inserted = 0, errors = 0;

  for (const r of pasteAlbumsParsed) {
    const match = dbAlbums?.find(a => titlesMatch(a.title, r.title));
    if (match) {
      const { error } = await sb.from('spotify_albums').update({ total_streams: r.streams, daily_streams: r.daily }).eq('id', match.id);
      if (!error) { await sb.from('spotify_album_snapshots').upsert({ album_id: match.id, snapshot_date: today, total_streams: r.streams }, { onConflict: 'album_id,snapshot_date' }); updated++; }
      else errors++;
    } else {
      const { error } = await sb.from('spotify_albums').insert({ title: r.title, total_streams: r.streams, daily_streams: r.daily, spotify_url: null });
      if (!error) inserted++; else errors++;
    }
  }

  const msg = `✅ ${updated} actualizados · ${inserted} nuevos${errors ? ` · ${errors} errores` : ''}`;
  statusEl.textContent = msg; showToast(msg);
  await loadAdminAlbums(); await loadSpotifyAlbumsPublic();
}

async function deleteAllAlbums() {
  if (!confirm('⚠️ ¿Eliminar TODOS los álbumes? Esto no se puede deshacer.')) return;
  if (!confirm('¿Segura? Se borrarán álbumes y sus snapshots.')) return;
  const statusEl = document.getElementById('paste-albums-status');
  statusEl.textContent = '⏳ Eliminando...';
  const { data } = await sb.from('spotify_albums').select('id');
  if (data && data.length) {
    const ids = data.map(a => a.id);
    await sb.from('spotify_album_snapshots').delete().in('album_id', ids);
    await sb.from('spotify_albums').delete().in('id', ids);
  }
  pasteAlbumsParsed = [];
  statusEl.textContent = '🗑️ Todos los álbumes eliminados.';
  document.getElementById('paste-albums-preview').style.display = 'none';
  document.getElementById('paste-albums-area').value = '';
  showToast('Álbumes eliminados');
  await loadAdminAlbums(); await loadSpotifyAlbumsPublic();
}

// ========== ALBUMS ==========
const ENHYPEN_ALBUMS = [
  { title: "DIMENSION : ANSWER", year: 2022, streams: 1139963107, daily: 357608, spotify_url: "https://open.spotify.com/album/6BY9EGTM7HNkNIDAuCVBKY" },
  { title: "BORDER : CARNIVAL", year: 2021, streams: 1089017295, daily: 395872, spotify_url: "https://open.spotify.com/album/4wSHsVShwB7xW58d5rthlV" },
  { title: "ROMANCE : UNTOLD -daydream-", year: 2024, streams: 985473967, daily: 984082, spotify_url: "https://open.spotify.com/album/5IZPD1GFQqkiJNXVNXuF9t" },
  { title: "DARK BLOOD", year: 2023, streams: 877765199, daily: 491569, spotify_url: "https://open.spotify.com/album/4i7Sl4jRNAx8uTb9yEFDq9" },
  { title: "ROMANCE : UNTOLD", year: 2024, streams: 767029266, daily: 772727, spotify_url: "https://open.spotify.com/album/0BIULTWShHq1SnMMTDcmPT" },
  { title: "DIMENSION : DILEMMA", year: 2021, streams: 608570678, daily: 213242, spotify_url: "https://open.spotify.com/album/7iVp3DfqUeUhWxrAkOVcYS" },
  { title: "BORDER : DAY ONE", year: 2020, streams: 563075790, daily: 171513, spotify_url: "https://open.spotify.com/album/5sHbKmq5mMJEUuFPRqEw1s" },
  { title: "MANIFESTO : DAY 1", year: 2022, streams: 541491718, daily: 213983, spotify_url: "https://open.spotify.com/album/2YiEt2KqMZrJXS7rDFBvFv" },
  { title: "SADAME", year: 2023, streams: 520511955, daily: 146413, spotify_url: "" },
  { title: "ORANGE BLOOD", year: 2023, streams: 422610000, daily: 232160, spotify_url: "https://open.spotify.com/album/7FRdLPPELnVWqGmGdVH2ND" },
  { title: "DESIRE : UNLEASH", year: 2025, streams: 402580367, daily: 524978, spotify_url: "https://open.spotify.com/album/3y1SBqIAFMSiElxM7VkXAq" },
  { title: "DARK MOON SPECIAL ALBUM", year: 2023, streams: 394491457, daily: 320796, spotify_url: "" },
  { title: "THE SIN : VANISH (SUNGHOON Ver.)", year: 2026, streams: 196843560, daily: 740037, spotify_url: "" },
  { title: "THE SIN : VANISH", year: 2026, streams: 193935458, daily: 726353, spotify_url: "" },
  { title: "THE SIN : VANISH (HEESEUNG Ver.)", year: 2026, streams: 192571481, daily: 725229, spotify_url: "" },
  { title: "THE SIN : VANISH (SUNOO Ver.)", year: 2026, streams: 191835057, daily: 725623, spotify_url: "" },
  { title: "THE SIN : VANISH (NI-KI Ver.)", year: 2026, streams: 191320171, daily: 722156, spotify_url: "" },
  { title: "THE SIN : VANISH (JAKE Ver.)", year: 2026, streams: 191205986, daily: 721273, spotify_url: "" },
  { title: "THE SIN : VANISH (JUNGWON Ver.)", year: 2026, streams: 191189532, daily: 722462, spotify_url: "" },
  { title: "THE SIN : VANISH (JAY Ver.)", year: 2026, streams: 191164482, daily: 721656, spotify_url: "" },
  { title: "THE SIN : VANISH (English Ver.)", year: 2026, streams: 185755159, daily: 702359, spotify_url: "" },
  { title: "THE SIN : VANISH (Japanese Ver.)", year: 2026, streams: 182439810, daily: 693374, spotify_url: "" },
  { title: "THE SIN : VANISH (Chinese Ver.)", year: 2026, streams: 182273771, daily: 692097, spotify_url: "" },
  { title: "定め", year: 2023, streams: 139993132, daily: 44202, spotify_url: "" },
  { title: "XO (Only If You Say Yes) (Remixes)", year: 2024, streams: 10611289, daily: 9910, spotify_url: "" },
];

async function loadSpotifyAlbumsPublic() {
  try {
    const { data, error } = await sb.from('spotify_albums').select('*').order('total_streams', { ascending: false });
    if (error) throw error;
    const container = document.getElementById('spotify-albums-table');
    if (!container) return;
    if (!data || data.length === 0) {
      container.innerHTML = '<div class="empty"><p>💿 No hay álbumes. El admin puede cargarlos desde el panel.</p></div>';
      return;
    }
    let html = `<table class="spotify-table"><thead><tr><th>RANK</th><th>RELEASE</th><th>TOTAL STREAMS</th><th style="color:#4ade80">DAILY AVG</th><th>LINK</th></tr></thead><tbody>`;
    data.forEach((a, i) => {
      const daily = a.daily_streams > 0 ? `<span style="color:#4ade80;font-weight:700">▲ +${fmt(a.daily_streams)}</span>` : '—';
      const link = a.spotify_url ? `<a href="${a.spotify_url}" target="_blank" style="color:var(--primary-red);text-decoration:none">🎵 Escuchar</a>` : '—';
      html += `<tr>
        <td><span class="rank-badge">#${i+1}</span></td>
        <td><div class="yt-video-title">${escapeHtml(a.title)}</div><div style="font-size:0.7rem;color:var(--text-muted);margin-top:2px">${a.year || ''}</div></td>
        <td style="color:var(--primary-gold);font-family:'Orbitron',monospace">${fmt(a.total_streams)}</td>
        <td>${daily}</td>
        <td>${link}</td>
      </tr>`;
    });
    html += '</tbody></table>';
    container.innerHTML = html;
  } catch(e) { console.error('Albums error:', e); }
}

async function loadAdminAlbums() {
  const { data, error } = await sb.from('spotify_albums').select('*').order('total_streams', { ascending: false });
  const container = document.getElementById('admin-albums-list');
  if (!container) return;
  if (error) { container.innerHTML = '<p style="color:var(--text-muted);font-size:0.8rem">⚠️ Tabla spotify_albums no encontrada.</p>'; return; }
  if (!data || data.length === 0) { container.innerHTML = '<p style="color:var(--text-muted)">No hay álbumes. Usa "Precargar discografía".</p>'; return; }
  container.innerHTML = data.map(a => `
    <div class="admin-list-item" id="alb-row-${a.id}">
      <div class="ali-info">
        <div class="ali-name">${escapeHtml(a.title)}</div>
        <div class="ali-sub">${fmt(a.total_streams)} streams | +${fmt(a.daily_streams)} hoy | ${a.year||''}</div>
      </div>
      <div class="ali-actions">
        <button class="btn btn-sm btn-ghost" onclick="toggleAlbumEdit(${a.id})">✏️ Editar</button>
        <button class="btn btn-sm btn-danger" onclick="deleteAlbum(${a.id})">✕</button>
      </div>
    </div>
    <div id="alb-edit-${a.id}" style="display:none;background:#0d1117;border:1px solid var(--border-dim);border-top:none;border-radius:0 0 10px 10px;padding:1rem;margin-bottom:0.5rem">
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:0.6rem;margin-bottom:0.6rem">
        <div class="form-group" style="grid-column:span 2">
          <label>Nombre del álbum</label>
          <input class="input" id="ae-title-${a.id}" value="${escapeHtml(a.title)}">
        </div>
        <div class="form-group">
          <label>Total Streams</label>
          <input class="input input-mono" id="ae-streams-${a.id}" value="${a.total_streams||0}">
        </div>
        <div class="form-group">
          <label>Daily Streams</label>
          <input class="input input-mono" id="ae-daily-${a.id}" value="${a.daily_streams||0}">
        </div>
        <div class="form-group">
          <label>Año</label>
          <input class="input input-mono" id="ae-year-${a.id}" value="${a.year||''}">
        </div>
        <div class="form-group">
          <label>URL Spotify</label>
          <input class="input" id="ae-url-${a.id}" value="${a.spotify_url||''}" placeholder="https://open.spotify.com/album/...">
        </div>
      </div>
      <div style="display:flex;gap:0.6rem">
        <button class="btn btn-sm btn-success" onclick="saveAlbumEdit(${a.id})">💾 Guardar</button>
        <button class="btn btn-sm btn-ghost" onclick="toggleAlbumEdit(${a.id})">Cancelar</button>
      </div>
    </div>`).join('');
}

function toggleAlbumEdit(id) {
  const row = document.getElementById(`alb-row-${id}`);
  const edit = document.getElementById(`alb-edit-${id}`);
  const isOpen = edit.style.display !== 'none';
  edit.style.display = isOpen ? 'none' : 'block';
  if (!isOpen) {
    row.style.borderRadius = '10px 10px 0 0';
    row.style.borderBottom = 'none';
  } else {
    row.style.borderRadius = '';
    row.style.borderBottom = '';
  }
}

async function saveAlbumEdit(id) {
  const title    = document.getElementById(`ae-title-${id}`).value.trim();
  const streams  = parseInt(document.getElementById(`ae-streams-${id}`).value.replace(/\D/g,'')) || 0;
  const daily    = parseInt(document.getElementById(`ae-daily-${id}`).value.replace(/\D/g,'')) || 0;
  const year     = parseInt(document.getElementById(`ae-year-${id}`).value) || null;
  const url      = document.getElementById(`ae-url-${id}`).value.trim() || null;
  if (!title) { showToast('El nombre no puede estar vacío', true); return; }
  const { error } = await sb.from('spotify_albums').update({
    title, total_streams: streams, daily_streams: daily, year, spotify_url: url
  }).eq('id', id);
  if (error) { showToast('Error: ' + error.message, true); return; }
  const today = new Date().toISOString().slice(0,10);
  await sb.from('spotify_album_snapshots').upsert({ album_id: id, snapshot_date: today, total_streams: streams }, { onConflict: 'album_id,snapshot_date' });
  showToast(`"${title}" guardado ✅`);
  await loadAdminAlbums();
  await loadSpotifyAlbumsPublic();
}

async function loadBulkAlbumsList() {
  const container = document.getElementById('bulk-albums-list');
  if (!container) return;
  const { data, error } = await sb.from('spotify_albums').select('*').order('total_streams', { ascending: false });
  if (error) { container.innerHTML = '<p style="color:var(--text-muted);font-size:0.8rem">⚠️ Tabla spotify_albums no encontrada. Primero precarga los álbumes.</p>'; return; }
  if (!data || data.length === 0) { container.innerHTML = '<p style="color:var(--text-muted);font-size:0.8rem">No hay álbumes. Primero precarga la discografía.</p>'; return; }
  let html = '<div style="display:grid;gap:0.5rem">';
  data.forEach(a => {
    html += `<div style="display:grid;grid-template-columns:1fr auto;align-items:center;gap:0.8rem;background:#111;border-radius:12px;padding:0.6rem 1rem;border:1px solid var(--border-dim)">
      <div>
        <div style="font-size:0.82rem;font-weight:600;color:var(--text-main)">${escapeHtml(a.title)}</div>
        <div style="font-size:0.7rem;color:var(--text-muted);margin-top:2px">Actual: ${fmt(a.total_streams)} streams</div>
      </div>
      <input class="input" id="balb-${a.id}" type="text" value="${a.total_streams || 0}" placeholder="streams" style="width:130px;text-align:right;font-family:'Orbitron',monospace;font-size:0.82rem" data-id="${a.id}">
    </div>`;
  });
  html += '</div>';
  container.innerHTML = html;
}

async function saveBulkAlbums() {
  const inputs = document.querySelectorAll('#bulk-albums-list input[data-id]');
  if (!inputs.length) { showToast('No hay álbumes', true); return; }
  const today = new Date().toISOString().slice(0,10);
  const yesterday = new Date(); yesterday.setDate(yesterday.getDate()-1);
  const yStr = yesterday.toISOString().slice(0,10);
  let saved = 0;
  for (const input of inputs) {
    const id = parseInt(input.dataset.id);
    const newStreams = parseInt(input.value.replace(/\D/g,'')) || 0;
    const { data: snap } = await sb.from('spotify_album_snapshots').select('total_streams').eq('album_id', id).eq('snapshot_date', yStr).maybeSingle();
    const old = snap ? snap.total_streams : 0;
    const daily = (old && newStreams > old) ? newStreams - old : 0;
    await sb.from('spotify_albums').update({ total_streams: newStreams, daily_streams: daily }).eq('id', id);
    await sb.from('spotify_album_snapshots').upsert({ album_id: id, snapshot_date: today, total_streams: newStreams }, { onConflict: 'album_id,snapshot_date' });
    saved++;
  }
  showToast(`✅ ${saved} álbumes actualizados`);
  await loadAdminAlbums();
  await loadSpotifyAlbumsPublic();
}

async function preloadAlbums() {
  const status = document.getElementById('preload-albums-status');
  status.textContent = '⏳ Cargando...';
  const { data: existing } = await sb.from('spotify_albums').select('title');
  const existingTitles = new Set((existing || []).map(a => a.title.toLowerCase()));
  const toInsert = ENHYPEN_ALBUMS.filter(a => !existingTitles.has(a.title.toLowerCase()));
  if (toInsert.length === 0) { status.textContent = '✅ Todos los álbumes ya están cargados.'; return; }
  let added = 0;
  const today = new Date().toISOString().slice(0,10);
  for (const album of toInsert) {
    const { data: ins, error } = await sb.from('spotify_albums').insert({ title: album.title, year: album.year, total_streams: album.streams || 0, daily_streams: album.daily || 0, spotify_url: album.spotify_url || null }).select();
    if (!error && ins && ins[0]) {
      await sb.from('spotify_album_snapshots').upsert({ album_id: ins[0].id, snapshot_date: today, total_streams: album.streams || 0 }, { onConflict: 'album_id,snapshot_date' });
      added++;
    }
  }
  status.textContent = `✅ ${added} álbumes cargados con datos de Kworb (27 May 2026).`;
  showToast(`Álbumes cargados: ${added}`);
  await loadAdminAlbums();
  await loadBulkAlbumsList();
  await loadSpotifyAlbumsPublic();
}

async function addAlbum() {
  const title = document.getElementById('na-title').value.trim();
  const year = parseInt(document.getElementById('na-year').value) || null;
  const total_streams = parseInt(document.getElementById('na-streams').value.replace(/\D/g,'')) || 0;
  const daily_streams = parseInt(document.getElementById('na-daily').value.replace(/\D/g,'')) || 0;
  const spotify_url = document.getElementById('na-url').value.trim();
  if (!title) { showToast('Escribe el título', true); return; }
  const { error } = await sb.from('spotify_albums').insert({ title, year, total_streams, daily_streams, spotify_url: spotify_url || null });
  if (error) { showToast('Error al guardar', true); return; }
  showToast('Álbum agregado ✅');
  ['na-title','na-year','na-streams','na-daily','na-url'].forEach(id => document.getElementById(id).value = '');
  await loadAdminAlbums(); await loadBulkAlbumsList(); await loadSpotifyAlbumsPublic();
}

async function deleteAlbum(id) {
  if (!confirm('¿Eliminar álbum?')) return;
  await sb.from('spotify_albums').delete().eq('id', id);
  await sb.from('spotify_album_snapshots').delete().eq('album_id', id);
  showToast('Álbum eliminado');
  await loadAdminAlbums(); await loadBulkAlbumsList(); await loadSpotifyAlbumsPublic();
}

// ========== YOUTUBE API (todo tu código original, sin modificaciones) ==========
const YT_API_KEY    = 'AIzaSyBIv_PcUf_kr3fmMFgcxKucOOGCTSJPv4U';
const YT_CHANNEL_ID = ''; // EVAN no tiene canal propio aún. Se actualizará cuando debute.
const YT_VIDEO_IDS = [
  { id: "BXwL-cBVXDQ", title: "EVAN (에반) 'Overflow' Lyric Video | 전하지 못할 네게 이 편지를 써봐" },
  { id: "CsGENgLpyaw", title: "EVAN (에반) 'Ride or Die' Official MV" },
  { id: "Qw3T9NBnOyc", title: "EVAN - RIDE OR DIE" },
  { id: "LIEOXiEAq5E", title: "EVAN (에반) Ride or Die Official MV Teaser" },
  { id: "3SHd1xhjI9s", title: "EVAN (에반) 'Overflow' [LIVE]" },
  { id: "sS9XGAFoRM0", title: "EVAN (에반) 'Ride or Die' MV Shoot Sketch | Archiv:E" },
  { id: "h-5mI2K9jfU", title: "EVAN (에반) 'Ride or Die' Dance Practice | Stag:E" },
  { id: "8tTzR8oViEg", title: "EVAN (에반) 친한친구 방송반 DJ 비하인드 | Archiv:E" },
  { id: "TruS_QursBw", title: "에반 오늘 뭐 하지? 에반정식과 숨바꼭질 | EVAN (에반) | Sid:E" },
  { id: "uDCONRmyjds", title: "[Cover] EVAN (에반) - 灰色と青 (Kenshi Yonezu, Masaki Suda) | TOKYO Tap:E" },
  { id: "0U6sXMUYtRM", title: "작업실을 꾸며보는 건 처음입니다 | EVAN (에반) | Sid:E" },
  { id: "V7dCFEJ1xrc", title: "EVAN (에반) GLOBAL OFFICIAL FANCLUB EVER MEMBERSHIP Welcome Message" },
  { id: "TB1bu9TxJzQ", title: "공연을 위해 한강에서 부산까지 | EVAN (에반) | Sid:E log" },
  { id: "yEV-DNKmwuY", title: "EVAN (에반) 'Ride or Die' 챌린지 비하인드 | Archiv:E" },
  { id: "eyCjCFY_nlQ", title: "EVAN (에반) 'RIDE OR DIE' 음악방송 비하인드 | Archiv:E" },
  { id: "bHELiN_9pCY", title: "'RIDE OR DIE' 활동을 마쳤습니다 | EVAN (에반) | Sid:E" },
  { id: "Sy28D2DXEdQ", title: "도쿄 미니 투어 재밌다 ㅎㅎ (+댄스 워크숍) | EVAN (에반) | Sid:E log" }
]
let ytCache = { channel: null, videos: null, snapshots: null, fetchedAt: 0 };
const YT_CACHE_MS = 60 * 60 * 1000;
function todayStr() { return new Date().toISOString().slice(0, 10); }
function yesterdayStr() { const d = new Date(); d.setDate(d.getDate() - 1); return d.toISOString().slice(0, 10); }
function isLocalFile() { return location.protocol === 'file:'; }
async function fetchYTChannel() {
  if (isLocalFile()) return null;
  try {
    const url = `https://www.googleapis.com/youtube/v3/channels?part=statistics&id=${YT_CHANNEL_ID}&key=${YT_API_KEY}`;
    const res = await fetch(url);
    if (!res.ok) throw new Error('HTTP '+res.status);
    const data = await res.json();
    const s = data.items?.[0]?.statistics;
    if (!s) throw new Error('No stats');
    return { subscribers: parseInt(s.subscriberCount)||0, totalViews: parseInt(s.viewCount)||0, videoCount: parseInt(s.videoCount)||0 };
  } catch(e) { console.warn('YT Channel failed:', e.message); return null; }
}
async function fetchYTVideos(ids) {
  if (isLocalFile()) return [];
  try {
    const url = `https://www.googleapis.com/youtube/v3/videos?part=statistics,snippet&id=${ids.join(',')}&key=${YT_API_KEY}`;
    const res = await fetch(url);
    if (!res.ok) throw new Error('Videos API ' + res.status);
    const data = await res.json();
    return data.items || [];
  } catch(e) { console.warn('YT Videos failed:', e.message); return []; }
}
async function saveSnapshot(videoStats) {
  const today = todayStr();
  try {
    for (const v of videoStats) {
      if (!v.video_id || !v.views) continue;
      await sb.from('yt_snapshots').upsert({ video_id: v.video_id, snapshot_date: today, total_views: v.views }, { onConflict: 'video_id,snapshot_date' });
    }
  } catch(e) { console.warn('Snapshot save failed:', e.message); }
}
async function getYesterdaySnapshots() {
  try {
    const yesterday = yesterdayStr();
    const { data } = await sb.from('yt_snapshots').select('video_id, total_views').eq('snapshot_date', yesterday);
    return data || [];
  } catch(e) { console.warn('Snapshot fetch failed:', e.message); return []; }
}
async function getDailyViews(videoId, currentViews) {
  try {
    const { data } = await sb.from('yt_snapshots').select('total_views, snapshot_date').eq('video_id', videoId).lt('snapshot_date', todayStr()).order('snapshot_date', { ascending: false }).limit(1);
    if (data && data[0]) { const diff = currentViews - parseInt(data[0].total_views); return diff > 0 ? diff : null; }
    return null;
  } catch(e) { return null; }
}
async function loadYTDataPublic() {
  const now = Date.now();
  const useCache = ytCache.channel && ytCache.videos && (now - ytCache.fetchedAt < YT_CACHE_MS);
  let channelStats, videoItems, yesterdaySnaps;
  if (useCache) {
    channelStats = ytCache.channel; videoItems = ytCache.videos; yesterdaySnaps = ytCache.snapshots || [];
  } else {
    const isLocal = location.protocol === 'file:';
    const loadMsg = isLocal ? '<div class="empty"><p>⚠️ YouTube API no funciona al abrir el archivo directo. <strong>Sube el archivo a tu hosting</strong> para ver los datos en vivo.</p></div>' : '<div class="loading"><div class="spin"></div><br>Conectando con YouTube API...</div>';
    document.getElementById('yt-stats-container').innerHTML = isLocal ? loadMsg : '<div class="loading"><div class="spin"></div><br>Conectando con YouTube API...</div>';
    document.getElementById('yt-videos-container').innerHTML = isLocal ? loadMsg : '<div class="loading"><div class="spin"></div><br>Calculando vistas diarias...</div>';
    if (isLocal) return;
    const ids = YT_VIDEO_IDS.map(v => v.id);
    [channelStats, videoItems, yesterdaySnaps] = await Promise.all([ fetchYTChannel(), fetchYTVideos(ids), getYesterdaySnapshots() ]);
    if (channelStats || videoItems.length) ytCache = { channel: channelStats, videos: videoItems, snapshots: yesterdaySnaps, fetchedAt: now };
  }
  if (channelStats) {
    document.getElementById('yt-stats-container').innerHTML = `<div class="yt-stats-grid"><div class="yt-stat-card"><div class="yt-stat-value">${fmt(channelStats.subscribers)}</div><div class="yt-stat-label">Suscriptores</div><div class="yt-stat-change" style="color:#4ade80">▶ En vivo · YT API</div></div><div class="yt-stat-card"><div class="yt-stat-value">${fmt(channelStats.totalViews)}</div><div class="yt-stat-label">Vistas totales del canal</div><div class="yt-stat-change" style="color:#4ade80">▶ En vivo · YT API</div></div><div class="yt-stat-card"><div class="yt-stat-value">${channelStats.videoCount.toLocaleString()}</div><div class="yt-stat-label">Videos publicados</div><div class="yt-stat-change" style="color:#4ade80">▶ En vivo · YT API</div></div></div>`;
  } else {
    const {data:cfg} = await sb.from('site_config').select('*');
    let subs=0, views=0, subsD=0, viewsD=0;
    if(cfg){ subs=parseInt(cfg.find(c=>c.key==='yt_subs')?.value)||subs; views=parseInt(cfg.find(c=>c.key==='yt_views')?.value)||views; subsD=parseInt(cfg.find(c=>c.key==='yt_subs_daily')?.value)||subsD; viewsD=parseInt(cfg.find(c=>c.key==='yt_views_daily')?.value)||viewsD; }
    const subsDisplay = subs > 0 ? fmt(subs) : '—';
    const viewsDisplay = views > 0 ? fmt(views) : '—';
    const subsDChange = subsD > 0 ? `<div class="yt-stat-change">+${fmt(subsD)} hoy</div>` : '';
    const viewsDChange = viewsD > 0 ? `<div class="yt-stat-change">+${fmt(viewsD)} hoy</div>` : '';
    document.getElementById('yt-stats-container').innerHTML = `<div class="yt-stats-grid"><div class="yt-stat-card"><div class="yt-stat-value">${subsDisplay}</div><div class="yt-stat-label">Suscriptores</div>${subsDChange}</div><div class="yt-stat-card"><div class="yt-stat-value">${viewsDisplay}</div><div class="yt-stat-label">Vistas totales</div>${viewsDChange}</div></div><p style="font-size:0.7rem;color:var(--text-muted);margin-top:0.5rem">⚠️ YouTube API no disponible — datos manuales</p>`;
  }
  if (!videoItems || videoItems.length === 0) {
    const {data:vids} = await sb.from('yt_videos').select('*').order('sort_order');
    if(!vids||!vids.length){ document.getElementById('yt-videos-container').innerHTML='<div class="empty"><p>No hay videos registrados.</p></div>'; return; }
    let h=`<table class="yt-video-table"><thead><tr><th>VIDEO</th><th>TOTAL VIEWS</th><th>+24H</th><th>LIKES</th></tr></thead><tbody>`;
    vids.forEach(v=>{ h+=`<tr><td class="yt-video-title">${v.title}</td><td>${fmt(v.total_views)}</td><td style="color:#4ade80">+${fmt(v.daily_views)}</td><td>${fmt(v.likes)}</td>`; });
    h+=`</tbody></table><p style="font-size:0.7rem;color:var(--text-muted);margin-top:0.5rem">⚠️ YouTube API no disponible — datos Supabase</p>`;
    document.getElementById('yt-videos-container').innerHTML=h; return;
  }
  const apiMap = {};
  videoItems.forEach(item => { apiMap[item.id] = { title: item.snippet?.title || '', views: parseInt(item.statistics?.viewCount)||0, likes: parseInt(item.statistics?.likeCount)||0, comments: parseInt(item.statistics?.commentCount)||0, thumb: item.snippet?.thumbnails?.medium?.url||'' }; });
  // Leer daily24h desde site_config (guardados por Edge Function)
  const daily24hKeys = YT_VIDEO_IDS.map(v => 'yt_daily24h_' + v.id);
  const { data: dailyCfg } = await sb.from('site_config').select('key,value').in('key', daily24hKeys);
  const daily24hMap = {};
  (dailyCfg || []).forEach(c => { const vid = c.key.replace('yt_daily24h_', ''); daily24hMap[vid] = parseInt(c.value) || 0; });
  const snapMap = {};
  (yesterdaySnaps || []).forEach(s => { snapMap[s.video_id] = parseInt(s.total_views) || 0; });
  const videos = YT_VIDEO_IDS.map(v => { const api = apiMap[v.id] || {}; const curr = api.views||0; const prev = snapMap[v.id]||0; const cfDaily = daily24hMap[v.id]; const calcDaily = (prev>0 && curr>=prev) ? curr-prev : null; const daily = cfDaily > 0 ? cfDaily : calcDaily; return { ...v, ...api, views: curr, daily24h: daily }; }).sort((a,b)=> (b.views||0)-(a.views||0));
  const toSnap = videos.filter(v => v.views>0).map(v => ({ video_id: v.id, views: v.views }));
  saveSnapshot(toSnap);
  try {
    const {data:existing} = await sb.from('yt_videos').select('id,video_id');
    for (const v of videos) {
      if (!v.views) continue;
      const match = existing?.find(e => e.video_id === v.id);
      const daily = v.daily24h || 0;
      if (match) await sb.from('yt_videos').update({ total_views: v.views, daily_views: daily, likes: v.likes }).eq('id', match.id);
      else { const {data:last} = await sb.from('yt_videos').select('sort_order').order('sort_order',{ascending:false}).limit(1); const sort = (last?.[0]?.sort_order || 0) + 1; await sb.from('yt_videos').insert({ title: v.title||v.id, total_views: v.views, daily_views: daily, likes: v.likes, sort_order: sort, video_id: v.id }); }
    }
  } catch(e) { console.warn('Supabase sync skipped:', e.message); }
  const hasAnyDaily = videos.some(v => v.daily24h !== null);
  let html = `<div style="display:flex;align-items:center;gap:0.6rem;margin-bottom:1rem;flex-wrap:wrap"><div class="dot-live" style="width:6px;height:6px"></div><span style="font-size:0.7rem;color:var(--text-muted);font-family:'Orbitron',monospace;letter-spacing:1px">DATOS EN VIVO · YouTube Data API v3</span>${hasAnyDaily ? '<span style="font-size:0.7rem;color:#4ade80;margin-left:0.5rem">✅ Vistas 24h calculadas</span>' : '<span style="font-size:0.7rem;color:var(--primary-gold);margin-left:0.5rem">⏳ Vistas 24h disponibles mañana (primer día de tracking)</span>'}</div><table class="yt-video-table"><thead><tr><th>#</th><th>VIDEO</th><th>TOTAL VIEWS</th><th style="color:#4ade80">+24H VIEWS</th><th>LIKES</th><th>COMENTARIOS</th><th>LINK</th></tr></thead><tbody>`;
  videos.forEach((v, i) => { const daily24h = v.daily24h !== null ? `<span style="color:#4ade80;font-family:'Orbitron',monospace;font-weight:700">+${fmt(v.daily24h)}</span>` : `<span style="color:var(--text-muted);font-size:0.7rem">—</span>`; html += `<tr><td style="color:var(--primary-gold);font-family:'Orbitron',monospace">${i+1}</td><td>${v.thumb ? `<img src="${v.thumb}" style="width:80px;height:45px;object-fit:cover;border-radius:6px;vertical-align:middle;margin-right:8px;border:1px solid var(--border-dim)">` : ''}<span class="yt-video-title">${v.title||v.id}</span></td><td style="color:var(--primary-gold);font-family:'Orbitron',monospace">${fmt(v.views)}</td><td>${daily24h}</td><td>${fmt(v.likes)}</td><td>${fmt(v.comments)}</td><td><a href="https://youtube.com/watch?v=${v.id}" target="_blank" style="color:var(--primary-red);font-size:0.75rem;text-decoration:none">▶ Ver</a></td></tr>`; });
  html += `</tbody></table>`;
  document.getElementById('yt-videos-container').innerHTML = html;
}
async function refreshYTFromAPI() {
  ytCache = { channel: null, videos: null, snapshots: null, fetchedAt: 0 };
  const btn = document.getElementById('btn-refresh-yt');
  if(btn){ btn.disabled=true; btn.textContent='⏳ Actualizando...'; }
  showToast('Conectando con YouTube API...');
  await loadYTDataPublic();
  if(btn){ btn.disabled=false; btn.textContent='🔄 Actualizar ahora desde YouTube'; }
  const status = document.getElementById('admin-yt-status');
  if(status) status.textContent = 'Actualizado: ' + new Date().toLocaleTimeString() + ' · Cache válido 1h';
  showToast('✅ Datos de YouTube actualizados');
}
function updateAdminYTStatus(){
  const status = document.getElementById('admin-yt-status');
  if(!status) return;
  if(ytCache.fetchedAt){ const mins = Math.round((Date.now()-ytCache.fetchedAt)/60000); status.textContent = 'Última actualización hace ' + mins + ' min · ' + (ytCache.channel ? `${fmt(ytCache.channel.subscribers)} subs · ${fmt(ytCache.channel.totalViews)} vistas` : 'sin datos de canal'); }
  else status.textContent = 'Aún no se ha cargado desde la API en esta sesión.';
}
async function saveYTStats(){
  let subs=document.getElementById('yt-subs').value.replace(/\D/g,''), views=document.getElementById('yt-views').value.replace(/\D/g,''), subsDaily=document.getElementById('yt-subs-daily').value.replace(/\D/g,''), viewsDaily=document.getElementById('yt-views-daily').value.replace(/\D/g,'');
  await sb.from('site_config').upsert({key:'yt_subs', value:subs||'0'}); await sb.from('site_config').upsert({key:'yt_views', value:views||'0'}); await sb.from('site_config').upsert({key:'yt_subs_daily', value:subsDaily||'0'}); await sb.from('site_config').upsert({key:'yt_views_daily', value:viewsDaily||'0'});
  showToast('Estadísticas de YouTube guardadas'); await loadYTDataPublic();
}
async function loadAdminVideos(){ let {data}=await sb.from('yt_videos').select('*').order('sort_order'); if(!data) return; document.getElementById('admin-videos-list').innerHTML=data.length?data.map(v=>`<div class="admin-list-item"><div class="ali-info"><div class="ali-name">${v.title}</div><div class="ali-sub">${fmt(v.total_views)} vistas | +${fmt(v.daily_views)} diarias | ${fmt(v.likes)} likes</div></div><div class="ali-actions"><button class="btn btn-sm btn-ghost" onclick="editYTVideo(${v.id},'${v.title.replace(/'/g,"\\'")}',${v.total_views},${v.daily_views},${v.likes})">✏️</button><button class="btn btn-sm btn-danger" onclick="deleteYTVideo(${v.id})">✕</button></div></div>`).join(''):'<p>No hay videos agregados</p>'; }
async function addYTVideo(){ let title=document.getElementById('nv-title').value.trim(), total=parseInt(document.getElementById('nv-total').value.replace(/\D/g,''))||0, daily=parseInt(document.getElementById('nv-daily').value.replace(/\D/g,''))||0, likes=parseInt(document.getElementById('nv-likes').value.replace(/\D/g,''))||0; if(!title){ showToast('Escribe un título',true); return; } let {data:exist}=await sb.from('yt_videos').select('sort_order').order('sort_order',{ascending:false}).limit(1); let sort=exist&&exist[0]?exist[0].sort_order+1:1; let {error}=await sb.from('yt_videos').insert({title, total_views:total, daily_views:daily, likes, sort_order:sort}); if(error){ showToast('Error al guardar',true); return; } document.getElementById('nv-title').value=''; document.getElementById('nv-total').value=''; document.getElementById('nv-daily').value=''; document.getElementById('nv-likes').value=''; showToast('Video agregado'); await loadAdminVideos(); await loadYTDataPublic(); }
function editYTVideo(id,title,total,daily,likes){ let newTotal=prompt(`Vistas totales de "${title}":`, total); if(newTotal===null) return; let newDaily=prompt(`Vistas diarias:`, daily); if(newDaily===null) return; let newLikes=prompt(`Likes:`, likes); if(newLikes===null) return; let nTotal=parseInt(newTotal.replace(/\D/g,''))||0, nDaily=parseInt(newDaily.replace(/\D/g,''))||0, nLikes=parseInt(newLikes.replace(/\D/g,''))||0; sb.from('yt_videos').update({total_views:nTotal, daily_views:nDaily, likes:nLikes}).eq('id',id).then(async()=>{ showToast('Video actualizado'); await loadAdminVideos(); await loadYTDataPublic(); }); }
async function deleteYTVideo(id){ if(!confirm('¿Eliminar video?')) return; await sb.from('yt_videos').delete().eq('id',id); showToast('Video eliminado'); await loadAdminVideos(); await loadYTDataPublic(); }

// ========== ADMIN DATA LOAD (unificado) ==========
async function loadAdminData(){
  // Wrap every section independently so one failure doesn't break the rest
  try { updateAdminYTStatus(); } catch(e){}
  try { await loadAdminProfile(); } catch(e){}
  try { await loadBulkUpdateList(); } catch(e){}
  try { await loadAdminAlbums(); } catch(e){}
  try { await loadBulkAlbumsList(); } catch(e){}

  // ── Goals ──
  try {
    const {data:goals, error:ge} = await sb.from('goals').select('*').order('sort_order');
    if(ge) throw ge;
    document.getElementById('admin-goals-list').innerHTML = goals&&goals.length
      ? goals.map(g=>`<div class="admin-list-item">
          <div class="ali-info">
            <div class="ali-name">${g.song}</div>
            <div class="ali-sub">${fmt(g.current)} / ${fmt(g.target)} · ${g.platform}</div>
          </div>
          <div class="pct-badge">${Math.min(100,Math.round(g.current/g.target*100))}%</div>
          <div class="ali-actions">
            <button class="btn btn-sm btn-ghost" onclick="editGoal(${g.id},'${g.song.replace(/'/g,"\'")}',${g.current},${g.target})">✏️</button>
            <button class="btn btn-sm btn-danger" onclick="deleteGoal(${g.id})">✕</button>
          </div>
        </div>`).join('')
      : '<p style="color:var(--text-muted)">Sin metas aún</p>';
  } catch(e){ console.error('Goals admin error:',e); }

  // ── Playlists ──
  try {
    const {data:playlists, error:pe} = await sb.from('playlists').select('*').order('sort_order');
    if(pe) throw pe;
    document.getElementById('admin-playlists-list').innerHTML = playlists&&playlists.length
      ? playlists.map(p=>`<div class="admin-list-item">
          <div>${p.icon||'🎵'}</div>
          <div class="ali-info">
            <div class="ali-name">${p.name}</div>
            <div class="ali-sub">${p.description||''} · ${p.platform}</div>
          </div>
          <div class="ali-actions">
            <button class="btn btn-sm btn-ghost" onclick="editPlaylist(${p.id},'${p.name.replace(/'/g,"\'")}','${(p.description||'').replace(/'/g,"\'")}','${p.url||''}','${p.platform}')">✏️</button>
            <button class="btn btn-sm btn-danger" onclick="deletePlaylist(${p.id})">✕</button>
          </div>
        </div>`).join('')
      : '<p style="color:var(--text-muted)">Sin playlists aún</p>';
  } catch(e){ console.error('Playlists admin error:',e); }

  // ── Config ──
  try {
    const {data:cfg} = await sb.from('site_config').select('*');
    if(cfg){
      cfg.forEach(r=>{ const el=document.getElementById('cfg-'+r.key); if(el) el.value=r.value||''; });
      const get = key => cfg.find(c=>c.key===key)?.value;
      if(document.getElementById('yt-subs')) document.getElementById('yt-subs').value=get('yt_subs')||'17600000';
      if(document.getElementById('yt-views')) document.getElementById('yt-views').value=get('yt_views')||'3200000000';
      if(document.getElementById('yt-subs-daily')) document.getElementById('yt-subs-daily').value=get('yt_subs_daily')||'100000';
      if(document.getElementById('yt-views-daily')) document.getElementById('yt-views-daily').value=get('yt_views_daily')||'3200000';
    }
  } catch(e){ console.error('Config admin error:',e); }

  // ── Videos / Spotify / Charts ──
  try { await loadAdminVideos(); } catch(e){ console.error('Admin videos error:',e); }
  try { await loadAdminSpotifySongs(); } catch(e){ console.error('Admin spotify error:',e); }
  try { await loadAdminSpCharts(); } catch(e){ console.error('Admin charts error:',e); }

  // ── Images ──
  try {
    const {data:images, error:ie} = await sb.from('images').select('*').order('created_at',{ascending:false});
    if(ie) throw ie;
    document.getElementById('admin-images-grid').innerHTML = images && images.length
      ? images.map(img=>`
          <div style="position:relative;border-radius:12px;overflow:hidden;background:var(--bg-elevated);border:1px solid var(--border-dim)">
            <img src="${img.url}" style="width:100%;aspect-ratio:1;object-fit:cover;display:block" onerror="this.style.display='none'">
            <div style="padding:0.5rem 0.6rem;font-size:0.7rem;color:var(--text-dim);line-height:1.4">
              <div style="font-weight:600;color:var(--text-main)">${img.author_name||'Admin'}</div>
              ${img.caption?`<div style="color:var(--text-muted);margin-top:2px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${img.caption}</div>`:''}
              <div style="color:var(--text-muted);margin-top:2px">❤️ ${img.likes||0}</div>
            </div>
            <button onclick="deleteImage('${img.id}')" style="position:absolute;top:0.4rem;right:0.4rem;background:rgba(180,0,0,0.88);border:none;color:#fff;border-radius:6px;padding:0.25rem 0.6rem;font-size:0.72rem;cursor:pointer;font-weight:600">🗑️</button>
          </div>`).join('')
      : '<p style="color:var(--text-muted);font-size:0.82rem;grid-column:1/-1">Sin publicaciones aún</p>';
  } catch(e){ console.error('Images admin error:',e); document.getElementById('admin-images-grid').innerHTML='<p style="color:var(--text-muted)">Error al cargar imágenes</p>'; }
}

// ========== CRUD METAS, PLAYLISTS, IMÁGENES (igual que tenías) ==========
async function addGoal(){ let song=document.getElementById('ng-song').value.trim(); let current=parseInt(document.getElementById('ng-current').value.replace(/\D/g,''))||0; let target=parseInt(document.getElementById('ng-target').value.replace(/\D/g,''))||0; let platform=document.getElementById('ng-platform').value; if(!song||!target){ showToast('Completa los campos',true); return; } let {data:exist}=await sb.from('goals').select('sort_order').order('sort_order',{ascending:false}).limit(1); let sort=exist&&exist[0]?exist[0].sort_order+1:1; const ng_img=document.getElementById('ng-img')?.value.trim()||null; await sb.from('goals').insert({song,current,target,platform,sort_order:sort,image_url:ng_img}); showToast('Meta agregada'); document.getElementById('ng-song').value=''; document.getElementById('ng-current').value=''; document.getElementById('ng-target').value=''; if(document.getElementById('ng-img')) document.getElementById('ng-img').value=''; await loadAdminData(); await loadGoalsPublic(); }
function editGoal(id,song,current,target){
  let nCurrent=prompt(`Streams actuales para "${song}"`,current);
  if(nCurrent===null) return;
  let valCurrent=parseInt(nCurrent.replace(/\D/g,''));
  if(isNaN(valCurrent)){ showToast('Número inválido',true); return; }
  let nTarget=prompt(`Objetivo (meta) para "${song}"`,target);
  if(nTarget===null) return;
  let valTarget=parseInt(nTarget.replace(/\D/g,''));
  if(isNaN(valTarget)||valTarget<=0){ showToast('Objetivo inválido',true); return; }
  sb.from('goals').update({current:valCurrent,target:valTarget}).eq('id',id).then(async()=>{ showToast('Actualizado'); await loadAdminData(); await loadGoalsPublic(); });
}
async function deleteGoal(id){ if(confirm('¿Eliminar meta?')){ await sb.from('goals').delete().eq('id',id); showToast('Meta eliminada'); await loadAdminData(); await loadGoalsPublic(); } }
async function addPlaylist(){ let name=document.getElementById('np-name').value.trim(); let description=document.getElementById('np-desc').value.trim(); let url=document.getElementById('np-url').value.trim(); let platform=document.getElementById('np-platform').value; let icon=document.getElementById('np-icon').value.trim()||platformIcon(platform); if(!name){ showToast('Nombre requerido',true); return; } let {data:exist}=await sb.from('playlists').select('sort_order').order('sort_order',{ascending:false}).limit(1); let sort=exist&&exist[0]?exist[0].sort_order+1:1; await sb.from('playlists').insert({name,description,url,platform,icon,sort_order:sort}); showToast('Playlist agregada'); document.getElementById('np-name').value=''; document.getElementById('np-desc').value=''; document.getElementById('np-url').value=''; document.getElementById('np-icon').value=''; await loadAdminData(); await loadPlaylistsPublic(); }
function editPlaylist(id,name,desc,url,platform){ let newUrl=prompt(`Editar URL de "${name}"`,url); if(newUrl===null) return; sb.from('playlists').update({url:newUrl}).eq('id',id).then(async()=>{ showToast('Playlist actualizada'); await loadAdminData(); await loadPlaylistsPublic(); }); }
async function deletePlaylist(id){ if(confirm('¿Eliminar playlist?')){ await sb.from('playlists').delete().eq('id',id); showToast('Playlist eliminada'); await loadAdminData(); await loadPlaylistsPublic(); } }
async function saveConfig(){ let keys=['hero_tag','debut_text','hero_bg_url','hero_bg_video_url','era_announcement','era_color','spotify_url','youtube_url','applemusic_url','weverse_url','instagram_url','twitter_url']; for(let k of keys){ let el=document.getElementById('cfg-'+k); if(el) await sb.from('site_config').upsert({key:k,value:el.value.trim()}); } showToast('Configuración guardada'); await loadConfig(); }
async function saveAdminProfile() {
  const name = document.getElementById('admin-profile-name').value.trim();
  const initials = document.getElementById('admin-profile-initials').value.trim().toUpperCase();
  if (!name && !initials) { showToast('Escribe al menos el nombre', true); return; }
  if (name) await sb.from('site_config').upsert({ key: 'admin_profile_name', value: name });
  if (initials) await sb.from('site_config').upsert({ key: 'admin_profile_initials', value: initials });
  document.getElementById('profile-save-status').textContent = '✅ Perfil guardado';
  showToast('Perfil guardado ✅');
}

async function loadAdminProfile() {
  try {
    const { data } = await sb.from('site_config').select('key,value').in('key', ['admin_profile_name','admin_profile_initials']);
    if (!data) return;
    data.forEach(r => {
      if (r.key === 'admin_profile_name') {
        const el = document.getElementById('admin-profile-name');
        if (el) el.value = r.value;
      }
      if (r.key === 'admin_profile_initials') {
        const el = document.getElementById('admin-profile-initials');
        if (el) el.value = r.value;
      }
    });
  } catch(e) {}
}

async function addImage(){
  const url = document.getElementById('img-url').value.trim();
  const caption = document.getElementById('img-caption').value.trim();
  if (!url) { showToast('URL requerida', true); return; }
  // Get current admin profile
  let authorName = 'Admin', authorInitials = 'SV';
  try {
    const { data: cfg } = await sb.from('site_config').select('key,value').in('key', ['admin_profile_name','admin_profile_initials']);
    if (cfg) cfg.forEach(r => {
      if (r.key === 'admin_profile_name') authorName = r.value;
      if (r.key === 'admin_profile_initials') authorInitials = r.value;
    });
  } catch(e) {}
  let nextSort = 1;
  try { const {data:li}=await sb.from('images').select('sort_order').order('sort_order',{ascending:false}).limit(1); if(li&&li[0]) nextSort=(li[0].sort_order||0)+1; } catch(e){}
  const { error: imgErr } = await sb.from('images').insert({ url, caption: caption || null, author_name: authorName, author_initials: authorInitials, sort_order: nextSort, likes: 0 });
  if (imgErr) {
    console.error('Insert error:', imgErr);
    showToast('Error: ' + (imgErr.message || 'fallo al guardar'), true);
    return;
  }
  showToast('Imagen publicada ✅');
  document.getElementById('img-url').value = '';
  document.getElementById('img-caption').value = '';
  await loadAdminData();
  await loadGalleryPublic();
}
async function deleteImage(id){
  if (!id) { showToast('ID no válido', true); return; }
  if (!confirm('¿Eliminar esta imagen?')) return;
  const { error } = await sb.from('images').delete().eq('id', id);
  if (error) { showToast('Error: ' + error.message, true); console.error(error); return; }
  showToast('Imagen eliminada ✅');
  document.getElementById('post-modal-overlay')?.classList.remove('open');
  await loadAdminData();
  await loadGalleryPublic();
}

// ========== SPOTIFY CHARTS (público) ==========
let spCurrentType = 'daily-songs';
let spCurrentRegion = 'global';
let spCurrentRegionLabel = 'Global';
let spCurrentRegionFlag = '🌐';

const SP_TYPE_LABELS = {
  'daily-songs': 'Daily Songs',
  'weekly-songs': 'Weekly Songs',
  'daily-artists': 'Daily Artists',
  'weekly-albums': 'Weekly Albums'
};

function spSetType(btn, type) {
  document.querySelectorAll('.sp-type-tab').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  spCurrentType = type;
  loadSpChartsPublic();
}

function spSetRegion(btn, region, label, flag) {
  document.querySelectorAll('.sp-region-tab').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  spCurrentRegion = region;
  spCurrentRegionLabel = label;
  spCurrentRegionFlag = flag;
  loadSpChartsPublic();
}

async function loadSpChartsPublic() {
  const wrap = document.getElementById('sp-charts-table-wrap');
  if (!wrap) return;
  wrap.innerHTML = '<div class="loading"><div class="spin"></div></div>';
  try {
    const { data, error } = await sb.from('sp_charts')
      .select('*')
      .eq('chart_type', spCurrentType)
      .eq('region', spCurrentRegion)
      .order('rank_position', { ascending: true });
    const typeLabel = SP_TYPE_LABELS[spCurrentType] || spCurrentType;
    const title = `${spCurrentRegionFlag} ${spCurrentRegionLabel} ${typeLabel}`;
    const count = (data || []).length;
    document.getElementById('spTableTitle').innerHTML = `${title} <span class="sp-count-badge">${count} item${count !== 1 ? 's' : ''}</span>`;
    const now = new Date();
    document.getElementById('spUpdatedTime').textContent = now.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' });
    if (error) {
      wrap.innerHTML = `<div class="sp-empty">⚠️ Tabla sp_charts no creada aún. Créala en Supabase con el SQL del setup.</div>`;
      return;
    }
    if (!data || data.length === 0) {
      wrap.innerHTML = `<div class="sp-empty">Sin entradas para esta combinación.<br><span style="font-size:0.7rem">El admin puede agregar posiciones desde el panel ⚙️</span></div>`;
      return;
    }
    const isWeekly = spCurrentType.startsWith('weekly');
    const isDailyArtist = spCurrentType === 'daily-artists';
    let tableHead, html;
    if (isDailyArtist) {
      tableHead = `<table class="sp-chart-table"><thead><tr>
        <th>RANK</th><th>ARTIST</th>
        <th>YESTERDAY'S POSITION</th>
        <th>CHANGES</th>
      </tr></thead><tbody>`;
    } else {
      const streamHeader = isWeekly ? 'WEEKLY STREAMS' : 'DAILY STREAMS';
      tableHead = `<table class="sp-chart-table"><thead><tr>
        <th>RANK</th><th>TRACK / ARTIST</th>
        <th>${streamHeader}</th>
        <th>TOTAL STREAMS</th>
        <th>CHANGE</th>
      </tr></thead><tbody>`;
    }
    html = tableHead;
    data.forEach(r => {
      const changeClass = r.change_dir === 'up' ? 'sp-change-up' : r.change_dir === 'down' ? 'sp-change-dn' : 'sp-change-up';
      const changeArrow = r.change_dir === 'up' ? '▲' : r.change_dir === 'down' ? '▼' : '✦';
      const changeVal = r.change_val ? `${changeArrow} ${r.change_val}` : '—';
      if (isDailyArtist) {
        // RANK | ARTIST | YESTERDAY'S POSITION | CHANGES
        const yesterdayPos = r.total_streams ? `#${Number(r.total_streams).toLocaleString('en-US')}` : '—';
        html += `<tr>
          <td><span class="sp-rank-badge">#${r.rank_position}</span></td>
          <td><div class="sp-song-name">${escapeHtml(r.song_title)}</div></td>
          <td style="color:var(--text-dim)">${yesterdayPos}</td>
          <td><span class="${changeClass}">${changeVal}</span></td>
        </tr>`;
      } else {
        const totalFmt = r.total_streams ? Number(r.total_streams).toLocaleString('en-US') : '—';
        const dailyFmt = r.daily_streams ? `<span class="sp-daily-val">${Number(r.daily_streams).toLocaleString('en-US')}</span>` : '—';
        html += `<tr>
          <td><span class="sp-rank-badge">#${r.rank_position}</span></td>
          <td><div class="sp-song-name">${escapeHtml(r.song_title)}</div><div class="sp-artist-name">${escapeHtml(r.artist_name || '')}</div></td>
          <td>${dailyFmt}</td>
          <td style="color:var(--text-dim)">${totalFmt}</td>
          <td><span class="${changeClass}">${changeVal}</span></td>
        </tr>`;
      }
    });
    html += '</tbody></table>';
    wrap.innerHTML = html;
  } catch(e) {
    console.error('SpCharts error:', e);
    wrap.innerHTML = `<div class="sp-empty">⚠️ Error cargando charts. Verifica la tabla sp_charts en Supabase.</div>`;
  }
}

// ========== SPOTIFY CHARTS (admin) ==========
async function loadAdminSpCharts() {
  const container = document.getElementById('admin-sp-charts-list');
  if (!container) return;
  const { data, error } = await sb.from('sp_charts').select('*').order('chart_type').order('region').order('rank_position');
  if (error) {
    container.innerHTML = '<p style="color:var(--text-muted);font-size:0.8rem">⚠️ Tabla sp_charts no encontrada. Créala en Supabase con el SQL del setup.</p>';
    return;
  }
  if (!data || data.length === 0) {
    container.innerHTML = '<p style="color:var(--text-muted);font-size:0.8rem">No hay entradas aún. Agrega posiciones de EVAN desde el formulario de abajo.</p>';
    return;
  }
  container.innerHTML = data.map(e => `
    <div class="admin-list-item">
      <div class="ali-info">
        <div class="ali-name">#${e.rank_position} · ${escapeHtml(e.song_title)}</div>
        <div class="ali-sub">${e.chart_type} · ${e.region} · ${Number(e.daily_streams||0).toLocaleString('en-US')} streams</div>
      </div>
      <div class="ali-actions">
        <button class="btn btn-sm btn-ghost" onclick="editSpChartEntry(${e.id},${e.rank_position},'${escapeHtml(e.song_title).replace(/'/g,"\\'")}',${e.daily_streams||0},${e.total_streams||0},'${e.change_val||''}')">✏️</button>
        <button class="btn btn-sm btn-danger" onclick="deleteSpChartEntry(${e.id})">✕</button>
      </div>
    </div>`).join('');
}

async function addSpChartEntry() {
  const chart_type = document.getElementById('nc-type').value;
  const region = document.getElementById('nc-region').value;
  const song_title = document.getElementById('nc-song').value.trim();
  const artist_name = document.getElementById('nc-artist').value.trim();
  const rank_position = parseInt(document.getElementById('nc-rank').value) || 1;
  const daily_streams = parseInt(document.getElementById('nc-daily').value.replace(/\D/g,'')) || 0;
  const total_streams = parseInt(document.getElementById('nc-total').value.replace(/\D/g,'')) || 0;
  const change_val = document.getElementById('nc-change').value.trim();
  const change_dir = document.getElementById('nc-dir').value;
  if (!song_title) { showToast('Escribe el nombre de la canción', true); return; }
  const { error } = await sb.from('sp_charts').insert({ chart_type, region, song_title, artist_name, rank_position, daily_streams, total_streams, change_val, change_dir });
  if (error) { showToast('Error: ' + error.message, true); console.error(error); return; }
  showToast('Entrada agregada ✅');
  ['nc-song','nc-artist','nc-rank','nc-daily','nc-total','nc-change'].forEach(id => document.getElementById(id).value = '');
  await loadAdminSpCharts();
  await loadSpChartsPublic();
}

async function editSpChartEntry(id, rank, song, daily, total, change) {
  const newRank = prompt(`Posición de "${song}":`, rank);
  if (newRank === null) return;
  const newDaily = prompt('Daily/Weekly streams:', daily);
  if (newDaily === null) return;
  const newTotal = prompt('Total streams:', total);
  if (newTotal === null) return;
  const newChange = prompt('Cambio (ej: +3 o -5):', change);
  if (newChange === null) return;
  await sb.from('sp_charts').update({
    rank_position: parseInt(newRank) || rank,
    daily_streams: parseInt(newDaily.replace(/\D/g,'')) || 0,
    total_streams: parseInt(newTotal.replace(/\D/g,'')) || 0,
    change_val: newChange.trim()
  }).eq('id', id);
  showToast('Entrada actualizada ✅');
  await loadAdminSpCharts();
  await loadSpChartsPublic();
}

async function deleteSpChartEntry(id) {
  if (!confirm('¿Eliminar esta entrada?')) return;
  await sb.from('sp_charts').delete().eq('id', id);
  showToast('Entrada eliminada');
  await loadAdminSpCharts();
  await loadSpChartsPublic();
}

// ========== NAVEGACIÓN ==========
function openAdminLogin(){ if(isAdmin){ openAdminPanel(); return; } document.getElementById('admin-overlay').classList.add('open'); }
function closeAdminLogin(e){ if(e&&e.target!==document.getElementById('admin-overlay')) return; document.getElementById('admin-overlay').classList.remove('open'); document.getElementById('admin-pass').value=''; document.getElementById('login-error').style.display='none'; }
function togglePassVis(){ let i=document.getElementById('admin-pass'); i.type=i.type==='password'?'text':'password'; }
function checkAdmin(){ let val=document.getElementById('admin-pass').value; if(val===ADMIN_PASS){ isAdmin=true; document.getElementById('admin-overlay').classList.remove('open'); document.getElementById('admin-dot').classList.add('on'); document.getElementById('admin-label').style.display='inline'; showToast('Bienvenido al panel oscuro'); openAdminPanel(); } else { document.getElementById('login-error').style.display='block'; setTimeout(()=>document.getElementById('login-error').style.display='none',2500); } }
function openAdminPanel(){ document.getElementById('admin-panel').classList.add('open'); loadAdminData(); }
function closeAdmin(){ document.getElementById('admin-panel').classList.remove('open'); }
function showPage(id,el){ document.querySelectorAll('.page').forEach(p=>p.classList.remove('active')); document.getElementById(id).classList.add('active'); document.querySelectorAll('.nav-links a').forEach(a=>a.classList.remove('active')); if(el) el.classList.add('active'); document.getElementById('nav-links').classList.remove('open'); window.scrollTo(0,0); }
function toggleMobileNav(){ document.getElementById('nav-links').classList.toggle('open'); }
function switchChartTab(id,el){ document.querySelectorAll('#charts .tabs .tab').forEach(t=>t.classList.remove('active')); el.classList.add('active'); document.getElementById('spotify-chart').style.display=id==='spotify-chart'?'block':'none'; document.getElementById('yt-chart').style.display=id==='yt-chart'?'block':'none'; document.getElementById('apple-chart').style.display=id==='apple-chart'?'block':'none'; }
function switchAdminTab(id,el){ document.querySelectorAll('.admin-tab').forEach(t=>t.classList.remove('active')); el.classList.add('active'); document.querySelectorAll('.admin-section').forEach(s=>s.classList.remove('active')); document.getElementById(id).classList.add('active'); if(id==='tab-apple') loadAdminAppleSongs(); }

// ========== KWORB ARTIST CHART ==========
const KWORB_ARTIST_SLUG = ''; // Set this when EVAN has a Kworb page
const KWORB_CACHE_KEY   = 'enhypen_kworb_cache';
const KWORB_CACHE_TTL   = 15 * 60 * 1000;

async function fetchKworbHtml(url) {
  const proxy = `https://api.allorigins.win/get?url=${encodeURIComponent(url)}`;
  const res = await fetch(proxy);
  if (!res.ok) throw new Error('Proxy error ' + res.status);
  const json = await res.json();
  return json.contents;
}

function fmtLong(n) {
  if (!n) return '—';
  return Number(n).toLocaleString('en-US');
}

function renderArtistChart(listeners, followers, streams, streamsDaily) {
  function statCard(label, value, liveLabel, daily) {
    // For solo debut artist: only show listeners/followers if set manually (not huge ENHYPEN-scale values)
    // For solo debut: show — until admin manually sets values in this project's DB
    // All values start as null until Kworb scrapes real EVAN data
    const validValue = value && value > 0;
    const valHtml = validValue
      ? `<div class="ast-value">${fmtLong(value)}</div>`
      : `<div class="ast-value" style="font-size:0.9rem;color:var(--cream-muted)">—</div>`;
    const liveHtml = `<div class="ast-live"><div class="ast-live-dot"></div>${liveLabel}</div>`;
    const dailyHtml = daily ? `<div class="ast-daily">▲ +${fmtLong(daily)} Today</div>` : '';
    return `<div class="artist-stat-card">${valHtml}<div class="ast-label" style="margin-top:0.5rem">${label}</div>${liveHtml}${dailyHtml}</div>`;
  }
  const streamsLive = streamsDaily ? `+${fmtLong(streamsDaily)} Today` : 'Live';
  document.getElementById('artist-chart-content').innerHTML = `
    <div class="artist-chart-grid">
      ${statCard('Monthly Listeners', listeners, 'Live', null)}
      ${statCard('Total Followers',   followers, 'LIVE', null)}
      ${statCard('Total Streams',     streams,   streamsLive, null)}
    </div>`;
}

async function loadKworbArtist(forceRefresh = false) {
  if (!KWORB_ARTIST_SLUG) {
    // No artist slug set — just load saved values from DB (manual only)
    let listeners = null, followers = null, manualStreams = null, manualToday = null;
    try {
      const { data } = await sb.from('site_config').select('key,value')
        .in('key', ['sp_monthly_listeners','sp_followers','sp_total_streams','sp_streams_today']);
      (data||[]).forEach(r => {
        if (r.key === 'sp_monthly_listeners') { const v=parseInt(r.value)||0; listeners    = (v>0&&v<50000000)?v:null; }
        if (r.key === 'sp_followers')          { const v=parseInt(r.value)||0; followers    = (v>0&&v<30000000)?v:null; }
        if (r.key === 'sp_total_streams')      { const v=parseInt(r.value)||0; manualStreams = (v>0&&v<2000000000)?v:null; }
        if (r.key === 'sp_streams_today')      { const v=parseInt(r.value)||0; manualToday  = (v>0&&v<50000000)?v:null; }
      });
    } catch(e) {}
    renderArtistChart(listeners, followers, manualStreams, manualToday);
    const meta = document.getElementById('kworb-meta');
    if (meta) meta.textContent = 'Datos manuales — Kworb no disponible aún';
    return;
  }
  const btn = document.getElementById('kworb-btn');
  if (btn) { btn.classList.add('spinning'); btn.disabled = true; }

  // 1. Listeners, Followers, Total Streams y Today desde Supabase (manual)
  let listeners = null, followers = null, manualStreams = null, manualToday = null;
  // Threshold: if streams > 2B it's leftover ENHYPEN data, ignore it
  const MAX_EVAN_STREAMS = 2_000_000_000;
  try {
    const { data: cfg } = await sb.from('site_config').select('key,value')
      .in('key', ['sp_monthly_listeners','sp_followers','sp_total_streams','sp_streams_today']);
    if (cfg) {
      cfg.forEach(r => {
        if (r.key === 'sp_monthly_listeners') { const v=parseInt(r.value)||0; listeners    = (v>0 && v<50000000) ? v : null; }
        if (r.key === 'sp_followers')          { const v=parseInt(r.value)||0; followers    = (v>0 && v<30000000) ? v : null; }
        if (r.key === 'sp_total_streams')      { const v=parseInt(r.value)||0; manualStreams = (v>0 && v<2000000000) ? v : null; }
        if (r.key === 'sp_streams_today')      { const v=parseInt(r.value)||0; manualToday   = (v>0 && v<50000000)  ? v : null; }
      });
    }
    // Precargar campos del admin
    const elL = document.getElementById('sp-monthly-listeners');
    const elF = document.getElementById('sp-followers');
    const elS = document.getElementById('sp-total-streams');
    const elT = document.getElementById('sp-streams-today');
    if (elL && listeners)     elL.value = listeners;
    if (elF && followers)     elF.value = followers;
    if (elS && manualStreams) elS.value = manualStreams;
    if (elT && manualToday)   elT.value = manualToday;
  } catch(e) { console.warn('Supabase artist config error:', e); }

  // 2. Total streams: manual si está definido, si no calcular desde discografía local
  const calcStreams = ENHYPEN_DISCOGRAPHY.reduce((acc, s) => acc + (s.streams || 0), 0);
  const calcDaily   = ENHYPEN_DISCOGRAPHY.reduce((acc, s) => acc + (s.daily  || 0), 0);
  const totalStreams = manualStreams || calcStreams;
  const totalDaily   = manualToday  || calcDaily;

  // 3. Cache check
  if (!forceRefresh) {
    try {
      const cached = JSON.parse(sessionStorage.getItem(KWORB_CACHE_KEY) || 'null');
      if (cached && (Date.now() - cached.ts) < KWORB_CACHE_TTL) {
        renderArtistChart(listeners, followers, totalStreams, cached.streamsDaily || totalDaily);
        document.getElementById('kworb-updated').textContent =
          `Fuente: kworb.net · ${new Date(cached.ts).toLocaleTimeString('es-ES',{hour:'2-digit',minute:'2-digit'})} (caché)`;
        if (btn) { btn.classList.remove('spinning'); btn.disabled = false; }
        return;
      }
    } catch(e) {}
  }

  // Render inmediato con lo que hay
  renderArtistChart(listeners, followers, totalStreams, totalDaily);

  // 4. Intentar kworb solo para daily streams (no bloquea si falla)
  let streamsDaily = totalDaily;
  try {
    const url = `https://kworb.net/spotify/artist/${KWORB_ARTIST_SLUG}_songs.html`;
    const html = await fetchKworbHtml(url);
    const m = html.match(/\+\s*([\d,]+)\s*Today/i);
    if (m) streamsDaily = parseInt(m[1].replace(/,/g,'')) || totalDaily;
    sessionStorage.setItem(KWORB_CACHE_KEY, JSON.stringify({ ts: Date.now(), streamsDaily }));
    document.getElementById('kworb-updated').textContent =
      `Fuente: kworb.net · ${new Date().toLocaleTimeString('es-ES',{hour:'2-digit',minute:'2-digit'})}`;
  } catch(e) {
    console.warn('Kworb fetch failed (non-critical):', e.message);
    document.getElementById('kworb-updated').textContent = 'Streams calculados desde discografía local';
  }

  renderArtistChart(listeners, followers, totalStreams, streamsDaily);
  if (btn) { btn.classList.remove('spinning'); btn.disabled = false; }
}

async function saveArtistChartManual() {
  const listeners    = (document.getElementById('sp-monthly-listeners')?.value || '').replace(/\D/g,'');
  const followers    = (document.getElementById('sp-followers')?.value || '').replace(/\D/g,'');
  const totalStreams = (document.getElementById('sp-total-streams')?.value || '').replace(/\D/g,'');
  const streamsToday = (document.getElementById('sp-streams-today')?.value || '').replace(/\D/g,'');

  if (!listeners && !followers && !totalStreams && !streamsToday) {
    showToast('Escribe al menos un valor', true); return;
  }
  const updates = [];
  if (listeners)    updates.push(sb.from('site_config').upsert({ key: 'sp_monthly_listeners', value: listeners }));
  if (followers)    updates.push(sb.from('site_config').upsert({ key: 'sp_followers',          value: followers }));
  if (totalStreams) updates.push(sb.from('site_config').upsert({ key: 'sp_total_streams',      value: totalStreams }));
  if (streamsToday) updates.push(sb.from('site_config').upsert({ key: 'sp_streams_today',      value: streamsToday }));
  await Promise.all(updates);
  showToast('Artist Chart guardado ✅');
  await loadKworbArtist(true);
}
// ========== THEME SYSTEM ==========
const THEME_DOTS = { black: 'td-black', white: 'td-white', rojo: 'td-rojo', dom: 'td-dom' };
const THEME_LABELS = { black: 'Black', white: 'White', rojo: 'Rojo', dom: 'DOM' };

function setTheme(theme) {
  const html = document.documentElement;
  if (theme === 'black') {
    html.removeAttribute('data-theme');
  } else {
    html.setAttribute('data-theme', theme);
  }
  // Update button
  const dot = document.getElementById('themeDot');
  const label = document.getElementById('themeLabel');
  dot.className = 'theme-dot ' + THEME_DOTS[theme];
  label.textContent = THEME_LABELS[theme];
  // Update active option
  ['black','white','rojo','dom'].forEach(t => {
    const el = document.getElementById('topt-' + t);
    if (el) el.classList.toggle('active', t === theme);
  });
  // Close dropdown
  document.getElementById('themeSel').classList.remove('open');
  // Persist
  localStorage.setItem('EVANchartsTeam_theme', theme);
}

function toggleThemeDropdown() {
  document.getElementById('themeSel').classList.toggle('open');
}

// Close dropdown when clicking outside
document.addEventListener('click', e => {
  const sel = document.getElementById('themeSel');
  if (sel && !sel.contains(e.target)) sel.classList.remove('open');
});

// Load saved theme on startup
(function() {
  const saved = localStorage.getItem('EVANchartsTeam_theme') || 'black';
  if (saved !== 'black') setTheme(saved);
})();

// ========== INICIALIZACIÓN ==========
// ===== GALERÍA PÚBLICA =====
// ===== POST MODAL =====
let pmData = [];   // full image data array
let pmIndex = 0;
let pmLiked = {};  // key: image id -> bool (localStorage)
let pmCurrentId = null;

function pmTimeAgo(dateStr) {
  const d = new Date(dateStr), now = new Date();
  const diff = (now - d) / 1000;
  if (diff < 60) return 'ahora mismo';
  if (diff < 3600) return `hace ${Math.floor(diff/60)}m`;
  if (diff < 86400) return `hace ${Math.floor(diff/3600)}h`;
  if (diff < 604800) return `hace ${Math.floor(diff/86400)}d`;
  return d.toLocaleDateString('es-ES', { day:'numeric', month:'short', year:'numeric' });
}

async function openPostModal(idx) {
  pmIndex = idx;
  const img = pmData[idx];
  pmCurrentId = img.id;

  // Image
  document.getElementById('pm-img').src = img.url;

  // Header
  const authorName = img.author_name || 'Admin';
  const authorInitials = img.author_initials || 'SV';
  document.getElementById('pm-avatar').textContent = authorInitials;
  document.getElementById('pm-author').textContent = authorName;
  document.getElementById('pm-author-handle').textContent = '@EVANchartsTeam';

  // Caption
  document.getElementById('pm-caption-author-name').textContent = authorName + ' ';
  document.getElementById('pm-caption-text').textContent = img.caption || '';
  document.getElementById('pm-caption-wrap').style.display = img.caption ? 'block' : 'none';

  // Date
  const d = new Date(img.created_at || Date.now());
  document.getElementById('pm-date-post').textContent = d.toLocaleDateString('es-ES', { day:'numeric', month:'long', year:'numeric' });

  // Likes (localStorage per post)
  try { pmLiked = JSON.parse(localStorage.getItem('pm_likes') || '{}'); } catch(e) { pmLiked = {}; }
  const likeCount = img.likes || 0;
  document.getElementById('pm-like-count').textContent = likeCount.toLocaleString('es-ES');
  const likeBtn = document.getElementById('pm-like-btn');
  if (pmLiked[img.id]) {
    likeBtn.textContent = '❤️'; likeBtn.classList.add('liked');
  } else {
    likeBtn.textContent = '🤍'; likeBtn.classList.remove('liked');
  }

  // Show/hide comment form (admin only)
  const form = document.getElementById('pm-comment-form');
  form.classList.add('visible'); // anyone can comment

  // Nav arrows
  document.getElementById('pm-prev').style.display = pmData.length > 1 ? 'flex' : 'none';
  document.getElementById('pm-next').style.display = pmData.length > 1 ? 'flex' : 'none';

  // Load comments
  await loadPostComments(img.id);

  document.getElementById('post-modal-overlay').classList.add('open');
  document.body.style.overflow = 'hidden';
}

async function loadPostComments(imageId) {
  const list = document.getElementById('pm-comments-list');
  list.innerHTML = '<div style="text-align:center;padding:1rem;color:var(--text-muted);font-size:.75rem">Cargando...</div>';
  try {
    const { data, error } = await sb.from('post_comments').select('*').eq('image_id', imageId).order('created_at', { ascending: true });
    if (error) throw error;
    if (!data || !data.length) {
      list.innerHTML = '<div class="pm-no-comments">Sin comentarios aún</div>';
      return;
    }
    // Get current user's anon name to identify own comments
    const myAnonId = sessionStorage.getItem('evan_anon_id');
    const myName = document.getElementById('pm-comment-name')?.value.trim()
      || (myAnonId ? 'Anon' + myAnonId : null);

    list.innerHTML = data.map(c => {
      const isAdminComment = c.is_admin;
      const isOwn = !isAdminComment && myName && c.author_name === myName;
      const canModify = isAdmin || isOwn;
      const initials = isAdminComment ? (c.author_initials || 'SV') : (c.author_name?.slice(0,2).toUpperCase() || '??');
      const actions = canModify ? `
        <div style="display:flex;gap:0.3rem;margin-top:0.25rem">
          <button onclick="editComment(${c.id}, this)" style="background:none;border:none;color:var(--cream-muted);cursor:pointer;font-size:0.65rem;padding:0" title="Editar">✏️</button>
          <button onclick="deleteComment(${c.id})" style="background:none;border:none;color:var(--cream-muted);cursor:pointer;font-size:0.65rem;padding:0" title="Eliminar">🗑️</button>
        </div>` : '';
      return `<div class="pm-comment" data-comment-id="${c.id}">
        <div class="pm-comment-avatar ${isAdminComment ? 'admin-av' : ''}">${initials}</div>
        <div class="pm-comment-body">
          <div class="pm-comment-name">${escapeHtml(c.author_name || 'Anon')}${isAdminComment ? '<span class="verified-badge">✦</span>' : ''}</div>
          <div class="pm-comment-text" id="ctext-${c.id}">${escapeHtml(c.content)}</div>
          <div class="pm-comment-date">${pmTimeAgo(c.created_at)}</div>
          ${actions}
        </div>
      </div>`;
    }).join('');
  } catch(e) {
    console.error('loadPostComments error:', e);
    list.innerHTML = `<div class="pm-no-comments" style="color:var(--primary-red)">Error: ${e.message || e.code || 'tabla post_comments no existe'}<br><span style="font-size:.7rem;color:var(--text-muted)">Crea la tabla en Supabase SQL Editor</span></div>`;
  }
}

async function toggleLike() {
  const img = pmData[pmIndex];
  if (!img) return;
  try { pmLiked = JSON.parse(localStorage.getItem('pm_likes') || '{}'); } catch(e) { pmLiked = {}; }

  const btn = document.getElementById('pm-like-btn');
  const countEl = document.getElementById('pm-like-count');
  const wasLiked = !!pmLiked[img.id];
  const newLiked = !wasLiked;
  const delta = newLiked ? 1 : -1;
  const newCount = Math.max(0, (img.likes || 0) + delta);

  pmLiked[img.id] = newLiked;
  localStorage.setItem('pm_likes', JSON.stringify(pmLiked));
  pmData[pmIndex].likes = newCount;

  btn.textContent = newLiked ? '❤️' : '🤍';
  newLiked ? btn.classList.add('liked') : btn.classList.remove('liked');
  countEl.textContent = newCount.toLocaleString('es-ES');

  await sb.from('images').update({ likes: newCount }).eq('id', img.id);
}

function cancelComment() {
  document.getElementById('pm-comment-input').value = '';
}

async function submitComment() {
  const content = document.getElementById('pm-comment-input').value.trim();
  if (!content) { showToast('Escribe algo primero', true); return; }
  const img = pmData[pmIndex];

  // Get or generate anon identity stored in sessionStorage
  let anonId = sessionStorage.getItem('evan_anon_id');
  if (!anonId) {
    anonId = Math.floor(Math.random() * 9000) + 1000;
    sessionStorage.setItem('evan_anon_id', anonId);
  }

  // Name input (optional) — fallback to Anon + id
  const nameInput = document.getElementById('pm-comment-name')?.value.trim();
  let authorName, authorInitials;

  if (isAdmin) {
    // Admin uses their configured profile name
    authorName = img.author_name || 'Admin';
    authorInitials = img.author_initials || 'SV';
  } else if (nameInput) {
    authorName = nameInput;
    authorInitials = nameInput.slice(0,2).toUpperCase();
  } else {
    authorName = 'Anon' + anonId;
    authorInitials = 'AN';
  }

  const { error } = await sb.from('post_comments').insert({
    image_id: img.id,
    content,
    author_name: authorName,
    author_initials: authorInitials,
    is_admin: isAdmin
  });

  if (error) {
    console.error('Comment error:', error);
    showToast('Error: ' + (error.message || error.code), true);
    return;
  }

  document.getElementById('pm-comment-input').value = '';
  showToast('Comentario publicado ✅');
  await loadPostComments(img.id);
}

async function deleteComment(id) {
  if (!confirm('¿Eliminar comentario?')) return;
  const { error } = await sb.from('post_comments').delete().eq('id', id);
  if (error) { showToast('Error al eliminar', true); return; }
  showToast('Comentario eliminado');
  await loadPostComments(pmCurrentId);
}

async function editComment(id, btn) {
  const textEl = document.getElementById('ctext-' + id);
  if (!textEl) return;
  const current = textEl.textContent;
  // Inline edit: replace text with input
  const wrap = textEl.parentElement;
  textEl.style.display = 'none';
  const inp = document.createElement('textarea');
  inp.value = current;
  inp.style.cssText = 'width:100%;background:var(--input-bg);border:1px solid var(--border);color:var(--cream);font-family:Inter,sans-serif;font-size:.73rem;padding:.35rem .5rem;resize:none;height:52px;outline:none;border-radius:2px';
  const saveBtn = document.createElement('button');
  saveBtn.textContent = 'Guardar';
  saveBtn.className = 'btn btn-sm';
  saveBtn.style.cssText = 'margin-top:0.3rem;font-size:0.62rem;padding:0.2rem 0.7rem';
  const cancelBtn = document.createElement('button');
  cancelBtn.textContent = 'Cancelar';
  cancelBtn.className = 'btn btn-ghost btn-sm';
  cancelBtn.style.cssText = 'margin-top:0.3rem;margin-left:0.3rem;font-size:0.62rem;padding:0.2rem 0.7rem';
  wrap.appendChild(inp);
  wrap.appendChild(saveBtn);
  wrap.appendChild(cancelBtn);
  inp.focus();
  saveBtn.onclick = async () => {
    const newText = inp.value.trim();
    if (!newText) return;
    const { error } = await sb.from('post_comments').update({ content: newText }).eq('id', id);
    if (error) { showToast('Error al editar', true); return; }
    showToast('Editado ✅');
    await loadPostComments(pmCurrentId);
  };
  cancelBtn.onclick = () => {
    inp.remove(); saveBtn.remove(); cancelBtn.remove();
    textEl.style.display = '';
  };
}

function pmNav(dir) {
  pmIndex = (pmIndex + dir + pmData.length) % pmData.length;
  openPostModal(pmIndex);
}

function closePostModal(e) {
  if (e && e.target !== document.getElementById('post-modal-overlay')) return;
  document.getElementById('post-modal-overlay').classList.remove('open');
  document.body.style.overflow = '';
  document.getElementById('pm-comment-input').value = '';
}

document.addEventListener('keydown', e => {
  const modal = document.getElementById('post-modal-overlay');
  if (!modal.classList.contains('open')) return;
  if (e.key === 'ArrowRight') pmNav(1);
  if (e.key === 'ArrowLeft')  pmNav(-1);
  if (e.key === 'Escape') { modal.classList.remove('open'); document.body.style.overflow = ''; }
});

async function loadGalleryPublic() {
  const grid = document.getElementById('gallery-grid');
  if (!grid) return;
  const { data } = await sb.from('images').select('*').order('created_at', { ascending: false });
  if (!data || !data.length) { grid.innerHTML = '<div class="empty"><p>📸 Sin imágenes aún</p></div>'; return; }
  pmData = data;
  grid.innerHTML = data.map((img, idx) => {
    const d = new Date(img.created_at || Date.now());
    const now = new Date();
    const diffMs = now - d;
    const diffMin = Math.floor(diffMs/60000), diffH = Math.floor(diffMin/60), diffD = Math.floor(diffH/24);
    let dateStr;
    if (diffMin < 1) dateStr = 'ahora mismo';
    else if (diffMin < 60) dateStr = `hace ${diffMin}m`;
    else if (diffH < 24) dateStr = `hace ${diffH}h`;
    else if (diffD < 7) dateStr = `hace ${diffD}d`;
    else dateStr = d.toLocaleDateString('es-ES', { day:'numeric', month:'short', year:'numeric' });
    const authorName = img.author_name || 'Admin';
    const authorInitials = img.author_initials || 'SV';
    const captionHtml = img.caption ? `<div class="wv-card-caption">${escapeHtml(img.caption)}</div>` : '';
    const likes = img.likes || 0;
    return `<div class="wv-card" onclick="openPostModal(${idx})">
      <div class="wv-card-header">
        <div class="wv-avatar">${escapeHtml(authorInitials)}</div>
        <div class="wv-card-meta">
          <div class="wv-card-author">${escapeHtml(authorName)} <span class="wv-verified">✦</span></div>
          <div class="wv-card-date">${dateStr}</div>
        </div>
      </div>
      ${captionHtml}
      <img class="wv-card-img" src="${img.url}" loading="lazy" onerror="this.parentElement.style.display='none'" alt="Era image">
      <div class="wv-card-footer">
        <span class="wv-tag">EVANcharts</span>
        <span style="margin-left:auto;font-size:.7rem;color:var(--text-muted)">❤️ ${likes.toLocaleString('es-ES')}</span>
      </div>
    </div>`;
  }).join('');
}

// ========== APPLE MUSIC (Manual via Supabase) ==========
const APPLE_CACHE_KEY = 'enhypen_apple_cache';
const APPLE_CACHE_TTL = 10 * 60 * 1000;

async function loadAppleMusic(forceRefresh = false) {
  const btn = document.getElementById('apple-refresh-btn');
  if (btn) { btn.classList.add('spinning'); btn.disabled = true; }

  try {
    // Load reach data from site_config
    const { data: cfg } = await sb.from('site_config').select('key,value')
      .in('key', ['am_peak','am_countries']);
    let peak = null, countries = null;
    if (cfg) cfg.forEach(r => {
      if (r.key === 'am_peak') peak = r.value;
      if (r.key === 'am_countries') countries = r.value;
    });

    // Load songs from apple_charts table
    const { data: songs, error } = await sb.from('apple_charts')
      .select('*').order('rank_position', { ascending: true });

    renderAppleArtistReach(peak, countries);

    if (error) {
      document.getElementById('apple-top-songs').innerHTML =
        `<div class="apple-empty">⚠️ Crea la tabla <code>apple_charts</code> en Supabase.<br>
        <span style="font-size:0.72rem">${error.message}</span></div>`;
    } else {
      renderAppleTopSongs(songs || []);
    }

    const now = new Date().toLocaleTimeString('es-ES', {hour:'2-digit',minute:'2-digit'});
    document.getElementById('apple-updated').textContent = `Actualizado · ${now}`;

    // Prefill admin fields
    const elP = document.getElementById('am-peak');
    const elC = document.getElementById('am-countries');
    if (elP && peak) elP.value = peak;
    if (elC && countries) elC.value = countries;

  } catch(e) {
    document.getElementById('apple-artist-reach').innerHTML =
      `<div class="apple-error"><strong>⚠ Error</strong><br>${e.message}</div>`;
    document.getElementById('apple-top-songs').innerHTML = '';
  }

  if (btn) { btn.classList.remove('spinning'); btn.disabled = false; }
}

async function saveAppleReach() {
  const peak = document.getElementById('am-peak').value.trim();
  const countries = document.getElementById('am-countries').value.trim();
  if (!peak && !countries) { showToast('Escribe al menos un valor', true); return; }
  if (peak) await sb.from('site_config').upsert({ key: 'am_peak', value: peak });
  if (countries) await sb.from('site_config').upsert({ key: 'am_countries', value: countries });
  showToast('Apple Music Reach guardado ✅');
  await loadAppleMusic(true);
}

async function loadAdminAppleSongs() {
  const { data } = await sb.from('apple_charts').select('*').order('rank_position', { ascending: true });
  const list = document.getElementById('am-songs-list');
  if (!list) return;
  if (!data || !data.length) { list.innerHTML = '<p style="color:var(--text-muted);font-size:0.8rem">Sin canciones aún</p>'; return; }
  list.innerHTML = data.map(s => `
    <div class="admin-list-item">
      <div class="ali-info">
        <div class="ali-name">#${s.rank_position} · ${escapeHtml(s.song_title)}</div>
        <div class="ali-sub">${escapeHtml(s.artist_name||'')} · ${escapeHtml(s.charts||'')} · 🌍 ${s.countries_count||0}</div>
      </div>
      <button class="btn btn-sm btn-danger" onclick="deleteAppleSong(${s.id})">✕</button>
    </div>`).join('');
}

async function addAppleSong() {
  const rank = parseInt(document.getElementById('am-rank').value) || 0;
  const song = document.getElementById('am-song').value.trim();
  const artist = document.getElementById('am-artist').value.trim();
  const charts = document.getElementById('am-charts').value.trim();
  const countriesCount = parseInt(document.getElementById('am-countries-count').value) || 0;
  if (!song || !rank) { showToast('Rank y canción son requeridos', true); return; }
  const { error } = await sb.from('apple_charts').insert({
    rank_position: rank, song_title: song, artist_name: artist,
    charts, countries_count: countriesCount
  });
  if (error) { showToast('Error: ' + error.message, true); return; }
  document.getElementById('am-rank').value = '';
  document.getElementById('am-song').value = '';
  document.getElementById('am-artist').value = '';
  document.getElementById('am-charts').value = '';
  document.getElementById('am-countries-count').value = '';
  showToast('Canción agregada ✅');
  await loadAdminAppleSongs();
  await loadAppleMusic(true);
}

async function deleteAppleSong(id) {
  if (!confirm('¿Eliminar esta canción?')) return;
  await sb.from('apple_charts').delete().eq('id', id);
  showToast('Eliminada');
  await loadAdminAppleSongs();
  await loadAppleMusic(true);
}

function renderAppleArtistReach(peak, countries) {
  const el = document.getElementById('apple-artist-reach');
  if (!el) return;
  const peakHtml = peak
    ? `<div class="apple-reach-value">${peak}</div><div class="apple-reach-sub">▲ Peaked</div>`
    : `<div class="apple-reach-value" style="font-size:.9rem;color:var(--text-muted)">—</div>`;
  const countriesHtml = countries
    ? `<div class="apple-reach-value">${countries}</div><div class="apple-reach-sub">✓ Consistent</div>`
    : `<div class="apple-reach-value" style="font-size:.9rem;color:var(--text-muted)">—</div>`;
  el.innerHTML = `
    <div style="font-size:.8rem;font-weight:700;color:var(--text-dim);margin-bottom:.8rem;text-transform:uppercase;letter-spacing:1px">Artist &amp; Reach</div>
    <div class="apple-reach-grid">
      <div class="apple-reach-card"><div class="apple-reach-label">Global Daily Peak</div>${peakHtml}</div>
      <div class="apple-reach-card"><div class="apple-reach-label">Countries Charted</div>${countriesHtml}</div>
    </div>`;
}

function renderAppleTopSongs(songs) {
  const el = document.getElementById('apple-top-songs');
  if (!el) return;
  if (!songs.length) {
    el.innerHTML = '<div class="apple-empty">Sin canciones aún. Agrégalas desde el panel admin 🍎</div>';
    return;
  }
  el.innerHTML = `
    <table class="apple-table">
      <thead><tr><th>RANK</th><th>TRACK / ARTIST</th><th>CHARTS</th><th class="right">COUNTRIES</th></tr></thead>
      <tbody>${songs.map(s => `
        <tr>
          <td><span class="apple-rank">#${s.rank_position}</span></td>
          <td>
            <div class="apple-song-name">${escapeHtml(s.song_title)}</div>
            ${s.artist_name ? `<div class="apple-song-artist">${escapeHtml(s.artist_name)}</div>` : ''}
          </td>
          <td>${s.charts ? `<span class="apple-chart-badge">${escapeHtml(s.charts.slice(0,40))}</span>` : '—'}</td>
          <td class="right"><span class="apple-country-count">${s.countries_count ? `🌍 ${s.countries_count}` : '—'}</span></td>
        </tr>`).join('')}
      </tbody>
    </table>`;
}

async function loadAll(){ await Promise.all([loadConfig(), loadGoalsPublic(), loadPlaylistsPublic(), loadSpotifySongsPublic(), loadSpotifyAlbumsPublic(), loadYTDataPublic(), loadSpChartsPublic(), loadGalleryPublic()]); loadKworbArtist(); loadAppleMusic(); }
loadAll();


let seq=''; document.addEventListener('keydown',e=>{ seq=(seq+e.key.toLowerCase()).slice(-3); if(seq==='adm'){ document.getElementById('admin-btn').style.display='inline-flex'; document.getElementById('admin-indicator').style.display='flex'; } });
let tap=0, timer; document.querySelector('.nav-logo').addEventListener('click',()=>{ tap++; clearTimeout(timer); timer=setTimeout(()=>tap=0,600); if(tap>=5){ document.getElementById('admin-btn').style.display='inline-flex'; document.getElementById('admin-indicator').style.display='flex'; tap=0; } });

// ===== LANGUAGE SYSTEM =====
const TRANSLATIONS = {
  en: {
    nav_home: 'Home', nav_spotify: 'Spotify', nav_apple: 'Apple Music',
    nav_youtube: 'YouTube', nav_charts: 'Charts', nav_goals: 'Goals', nav_gallery: 'Avisos',
    sec_goal: 'Goals & Milestones', sec_playlists: 'Featured Playlists',
    sec_artist_chart: 'Artist Chart', sec_top_songs: 'Top Songs',
    sec_sp_playlists: 'Spotify Playlists', sec_albums: 'Album / EP Chart',
    sec_yt_overview: 'YouTube Channel Overview', sec_mv_chart: 'Music Video Chart',
    sec_sp_charts: 'Spotify Charts — EVAN', sec_goals_tracker: 'Goals Tracker',
    sec_gallery: 'Gallery · EVANcharts',
    tip_streaming: '🔥 <strong>Streaming ritual:</strong> Every play counts. Use official playlists, avoid mute and play for at least 30 seconds. Together we break records!',
    tip_spotify: '📊 Data updated manually by staff. Daily streams are calculated automatically when you update the total.',
    tip_sp_listen: '🎧 Listen in shuffle mode without muting. The algorithm loves consistency.',
    tip_yt: '📊 Data updated automatically from YouTube API. Support the recent videos!',
    subs: 'Subscribers', total_views: 'Total Channel Views', videos_pub: 'Published Videos',
    live_data: '▶ Live · YT API', manual_data: '⚠️ YouTube API unavailable — manual data',
    admin_btn: '⚙️ Admin',
  },
  es: {
    nav_home: 'Home', nav_spotify: 'Spotify', nav_apple: 'Apple Music',
    nav_youtube: 'YouTube', nav_charts: 'Charts', nav_goals: 'Metas', nav_gallery: 'Avisos',
    sec_goal: 'Metas y Objetivos', sec_playlists: '<span data-lang="sec_playlists">Playlists Destacadas</span>',
    sec_artist_chart: 'Artist Chart', sec_top_songs: 'Top Canciones',
    sec_sp_playlists: 'Playlists de Spotify', sec_albums: 'Album / EP Chart',
    sec_yt_overview: 'YouTube Channel Overview', sec_mv_chart: 'Music Video Chart',
    sec_sp_charts: 'Spotify Charts — EVAN', sec_goals_tracker: 'Tracker de Metas',
    sec_gallery: 'Galería · EVANcharts',
    tip_streaming: '🔥 <strong>Ritual de streaming:</strong> Cada reproducción cuenta. Usa playlists oficiales, evita mute y reproduce al menos 30 segundos. ¡Juntos rompemos récords!',
    tip_spotify: '📊 Datos actualizados manualmente por el staff. Los streams diarios se calculan automáticamente al actualizar el total.',
    tip_sp_listen: '<span data-lang="tip_sp_listen">🎧 Escucha las listas en modo aleatorio y sin silenciar. El algoritmo ama la consistencia.</span>',
    tip_yt: '<span data-lang="tip_yt">📊 Datos actualizados automáticamente desde YouTube API. ¡Apoya los videos recientes!</span>',
    subs: 'Suscriptores', total_views: 'Vistas Totales del Canal', videos_pub: 'Videos Publicados',
    live_data: '▶ En vivo · YT API', manual_data: '⚠️ YouTube API no disponible — datos manuales',
    admin_btn: '⚙️ Admin',
  },
  ko: {
    nav_home: '홈', nav_spotify: '스포티파이', nav_apple: '애플 뮤직',
    nav_youtube: '유튜브', nav_charts: '차트', nav_goals: '목표', nav_gallery: '공지',
    sec_goal: '목표 및 마일스톤', sec_playlists: '추천 플레이리스트',
    sec_artist_chart: '아티스트 차트', sec_top_songs: '인기 곡',
    sec_sp_playlists: '스포티파이 플레이리스트', sec_albums: '앨범 / EP 차트',
    sec_yt_overview: 'YouTube 채널 현황', sec_mv_chart: '뮤직비디오 차트',
    sec_sp_charts: 'Spotify 차트 — EVAN', sec_goals_tracker: '목표 트래커',
    sec_gallery: '갤러리 · EVANcharts',
    tip_streaming: '🔥 <strong>스트리밍 팁:</strong> 모든 재생이 중요합니다. 공식 플레이리스트를 사용하고 음소거 없이 30초 이상 재생하세요. 함께 기록을 깨요!',
    tip_spotify: '📊 스태프가 수동으로 업데이트한 데이터입니다. 일별 스트리밍은 총계 업데이트 시 자동으로 계산됩니다.',
    tip_sp_listen: '🎧 셔플 모드로 음소거 없이 들어주세요. 알고리즘은 일관성을 좋아합니다.',
    tip_yt: '📊 YouTube API에서 자동으로 업데이트된 데이터입니다. 최신 영상을 응원해주세요!',
    subs: '구독자', total_views: '채널 총 조회수', videos_pub: '업로드된 영상',
    live_data: '▶ 실시간 · YT API', manual_data: '⚠️ YouTube API 사용 불가 — 수동 데이터',
    admin_btn: '⚙️ 관리자',
  }
};

let currentLang = localStorage.getItem('evan_lang') || 'en';

function setLang(lang) {
  currentLang = lang;
  localStorage.setItem('evan_lang', lang);
  const t = TRANSLATIONS[lang];
  const flags = { en: '🇺🇸', es: '🇪🇸', ko: '🇰🇷' };
  const labels = { en: 'EN', es: 'ES', ko: 'KO' };

  // Update button
  document.getElementById('langFlag').textContent = flags[lang];
  document.getElementById('langLabel').textContent = labels[lang];

  // Update active option
  ['en','es','ko'].forEach(l => {
    const el = document.getElementById('lopt-'+l);
    if (el) el.classList.toggle('active', l === lang);
  });

  // Nav links
  const navKeys = ['home','spotify','apple','youtube','charts'];
  const navEls = document.querySelectorAll('.nav-links a');
  navEls.forEach((el, i) => { if(navKeys[i]) el.textContent = t['nav_'+navKeys[i]]; });

  // Section titles (data-lang attributes)
  document.querySelectorAll('[data-lang]').forEach(el => {
    const key = el.getAttribute('data-lang');
    if (t[key]) el.innerHTML = t[key];
  });

  // Close dropdown
  document.getElementById('langDropdown').classList.remove('open');
}

function toggleLangDropdown() {
  document.getElementById('langDropdown').classList.toggle('open');
  // Close theme dropdown if open
  const td = document.querySelector('.theme-dropdown');
  if (td) td.classList.remove('open');
}

// Close lang dropdown when clicking outside
document.addEventListener('click', (e) => {
  if (!document.getElementById('langSel')?.contains(e.target)) {
    document.getElementById('langDropdown')?.classList.remove('open');
  }
});

// Apply saved language on load
document.addEventListener('DOMContentLoaded', () => setLang(currentLang));
setTimeout(() => setLang(currentLang), 500);
</script>
</body>
</html>
